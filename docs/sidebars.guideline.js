/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const generatedIndex = (title, slug, description) => ({
  type: 'generated-index',
  title,
  slug,
  description,
});

const sidebars = {
  guideline: [
    {
      type: 'category',
      label: '开发规范',
      collapsed: false,
      link: generatedIndex(
        '开发规范',
        '/',
        'TGOSKits 的代码质量、功能开发和 StarryOS ABI 规范。',
      ),
      items: [{type: 'autogenerated', dirName: '.'}],
    },
  ],
};

export default sidebars;
