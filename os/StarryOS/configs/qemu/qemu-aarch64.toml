args = [
  "-m",
  "512M",
  "-nographic",
  "-cpu",
  "cortex-a53",
  # StarryOS AArch64 uses the GICv3 interrupt path on QEMU virt.  Without an
  # explicit version QEMU may select GICv2, whose route model rejects the
  # logical CPU target during early boot.
  "-machine",
  "virt,gic-version=3",
  "-device",
  "nvme,drive=disk0,serial=tgoskits,max_ioqpairs=64,msix_qsize=65",
  "-drive",
  "id=disk0,if=none,format=raw,file=${workspace}/tmp/axbuild/rootfs/rootfs-aarch64-alpine.img",
  "-device",
  "virtio-net-pci,netdev=net0",
  "-netdev",
  "user,id=net0",
]
uefi = false
to_bin = true
success_regex = []
fail_regex = ["(?i)\\bpanic(?:ked)?\\b"]
