# Build-time config template for StarryOS on VisionFive2.
# Block storage is intentionally unavailable until JH7110 DWMMC migrates to
# the IRQ-driven block runtime.
target = "riscv64gc-unknown-none-elf"
features = [
  "ax-runtime/rtc",
  "ax-driver/rtc",
  "ax-driver/serial",
]
log = "Info"
