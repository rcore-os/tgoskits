# Block storage is intentionally unsupported until Phytium MCI is migrated to
# the IRQ-driven HardwareQueue contract.
features = [
    "fs",
]
log = "Info"
target = "aarch64-unknown-none-softfloat"
vm_configs = []
