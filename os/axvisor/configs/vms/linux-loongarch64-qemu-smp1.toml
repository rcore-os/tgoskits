# Vm base info configs
#
[base]
# Guest vm id.
id = 1
# Guest vm name.
name = "linux-loongarch64-qemu"
# Virtualization type.
vm_type = 1
# The number of virtual CPUs.
cpu_num = 1
# Guest vm physical cpu ids.
phys_cpu_ids = [0]

#
# Vm kernel configs
#
[kernel]
# The entry point is replaced by the LoongArch ELF loader when vmlinux.elf is used.
entry_point = 0x0020_0000
# The location of image: "memory" | "fs".
image_location = "fs"
# The file path of the kernel image.
kernel_path = "/guest/linux/qemu-loongarch64"
# The ELF LOAD paddr is used by the LoongArch loader; this is a fallback for flat images.
kernel_load_addr = 0x0020_0000
# The file path of the device tree blob (DTB).
dtb_path = "/guest/linux/linux-loongarch64-qemu-smp1.dtb"
# The load address of the device tree blob (DTB).
dtb_load_addr = 0x0010_0000
# The file path of the ramdisk image.
ramdisk_path = "/guest/linux/initramfs.cpio.gz"
# The load address of the ramdisk image.
ramdisk_load_addr = 0x0700_0000
# Kernel command line.
cmdline = "root=/dev/ram rw console=ttyS0 earlycon=uart,mmio,0x1fe001e0 init=/init nokaslr pci=off initcall_blacklist=raid6_select_algo,ixgbe_init_module,i8042_init,inet6_init"

# Memory regions with format (`base_paddr`, `size`, `flags`, `map_type`).
# For `map_type`, 0 means `MAP_ALLOC`, 1 means `MAP_IDENTICAL`, 2 means `MAP_RESERVED`.
memory_regions = [
  [0x0008_0000, 0x0018_0000, 0x7, 0], # Low bootinfo/DTB RAM 512K-2M MAP_ALLOC
  [0x0020_0000, 0x03e0_0000, 0x7, 0], # System RAM 2-64M MAP_ALLOC
  [0x0400_0000, 0x0200_0000, 0x7, 0], # System RAM 64-96M MAP_ALLOC
  [0x0600_0000, 0x0200_0000, 0x7, 0], # System RAM 96-128M MAP_ALLOC
]

#
# Device specifications
#
[devices]
# The interrupt mode.
interrupt_mode = "passthrough"

# Pass-through devices.
passthrough_devices = [
  ["/"],
]

# Passthrough addresses.
# Base-GPA  Length.
passthrough_addresses = [
]

# Devices that are not desired to be passed through to the guest
excluded_devices = [
]

# Emu_devices.
emu_devices = [
]
