[package]
name = "kvm-uapi"
version = "0.1.0"
edition.workspace = true
authors = ["The Axvisor Team"]
categories = ["virtualization", "no-std"]
description = "Linux KVM userspace ABI definitions for no_std hypervisors"
repository = "https://github.com/rcore-os/tgoskits"
keywords = ["kvm", "uapi", "virtualization", "no-std"]
license = "Apache-2.0"

[target.'cfg(target_arch = "x86_64")'.dependencies]
raw-cpuid = "11.0"
