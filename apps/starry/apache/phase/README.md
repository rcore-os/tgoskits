# Apache Phase Tests

Phase tests are focused Apache feature and StarryOS semantic checks. They are
development iteration entries and should not be expanded into pressure tests.

Before running a phase test on StarryOS, run the same script or equivalent
commands in Linux Alpine and keep the Linux result as the behavior oracle.
