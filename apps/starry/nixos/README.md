# StarryNixOS Stage-2 baseline

This application is an opt-in, x86_64-only **NixOS-style userspace
personality** for StarryOS. It boots the stage-2 initializer generated by a
locked NixOS declaration and lets systemd run as PID 1. It does not claim full
NixOS compatibility and does not replace the Alpine-based `apps/starry/nix`
application.

## Supported boundary

- Host: native `x86_64-linux` with Nix/Lix, QEMU x86_64, `e2fsck`, and enough
  store/disk space for the closure.
- Guest: `x86_64-unknown-none`, QEMU q35 UEFI, NVMe root disk.
- Managed artifact reference: `tmp/axbuild/rootfs/rootfs-x86_64-nixos.img`.
  Axbuild resolves it to
  `tmp/axbuild/rootfs/rootfs-x86_64-nixos.img/rootfs-x86_64-nixos.img`; the
  image is built only by `build-rootfs.sh` from `flake.lock`.
- Boot: `/init` is the generated NixOS stage-2 initializer; systemd becomes PID
  1 and a declarative timer starts the marker service after
  `multi-user.target` is active.

The initial profile has no installer, NixOS kernel, initrd, bootloader,
in-guest rebuild or generation switching, desktop, udev-dependent device
management, journald acceptance requirement, D-Bus service stack, networkd,
resolved, or logind support. Aarch64 requires a separate validated feature.

## Host prerequisites and pinned identity

The first construction host used during implementation reported:

- system: `x86_64-linux`;
- Nix implementation: Lix 2.95.2;
- QEMU: 11.0.2;
- e2fsprogs: 1.47.4;
- remote builders: none.

`flake.lock` is the input authority. Every successful publication records its
SHA-256 hash, the generated toplevel store path, systemd version, image hash,
and target architecture in the adjacent
`rootfs-x86_64-nixos.img.manifest` file. Cached inputs are acceptable only when
they resolve to the same lock identity.

## Build and validate

From the repository root:

```bash
apps/starry/nixos/build-rootfs.sh --self-test
TMPDIR="$PWD/.ci-cache/tmp" \
  direnv exec . cargo xtask starry app qemu -t nixos --arch x86_64
```

After a native Nix/Lix build has published a valid image and manifest, a CI
container may reuse it without installing Nix. Reuse is explicit and rechecks
the ext4 image, flake-lock hash, closure identity, target, and image hash:

```bash
TMPDIR="$PWD/.ci-cache/tmp" \
STARRY_NIXOS_REUSE_ROOTFS=1 \
  direnv exec . cargo xtask starry app qemu -t nixos --arch x86_64
```

Use the repository direnv environment directly. Do not run `nix develop`, and
do not rebuild or switch the host NixOS system. Keep optional repository-local
tool state limited to `.ci-cache/cargo`, `.ci-cache/rustup`, and
`.ci-cache/tmp`; build output remains in the repository's normal `target` and
`tmp/axbuild` locations.

Axbuild invokes the same builder automatically through the explicit
`AppOwned` rootfs contract. Builder failure, a target mismatch, a missing or
invalid image, or a provenance mismatch is terminal; there is no Alpine copy,
APK rewrite, overlay injection, or fallback image.

The sole success condition is the ordered sequence:

```text
STARRY_NIXOS_PHASE=pid1
STARRY_NIXOS_PHASE=activation
STARRY_NIXOS_PHASE=systemd
STARRY_NIXOS_PHASE=marker
STARRY_NIXOS_SYSTEM_PASSED
```

A shell prompt, partial sequence, failed unit, panic/fatal record, explicit
failure marker, or timeout is a failure. See `compatibility.md` for exact run
evidence and the compatibility ledger.

The 2026-08-07 8-vCPU acceptance run completed activation, started systemd as
PID 1, reached `multi-user.target`, ran the declarative marker service, and
emitted the complete ordered success sequence. The supported claim remains an
experimental x86_64 NixOS-style userspace personality rather than full NixOS:
the profile retains the documented unit-local udevd sandbox override and
machine-ID persistence exception, and the excluded capabilities above remain
unsupported.

## Rollback

Disable or remove this app and delete its generated managed image. The `nixos`
feature is selected only by this app's build configuration; all other StarryOS
applications retain their existing shell PID-1 command and rootfs preparation
behavior. Rollback never modifies the shared Alpine image.
