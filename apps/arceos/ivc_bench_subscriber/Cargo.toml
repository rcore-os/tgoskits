[package]
name = "arceos-ivc-bench-subscriber"
version = "0.1.0"
edition.workspace = true
authors = ["AxVisor Team"]
publish = false

[features]
default = []
arceos = [
  "dep:ax-mm",
  "dep:ax-std",
  "ax-std/arceos",
  "ax-std/paging",
  "ax-std/tls",
]

[dependencies]
ax-mm = { workspace = true, optional = true }
ax-std = { workspace = true, optional = true, default-features = false }
axhvc = { workspace = true }
axivc = { workspace = true }

[package.metadata.axstd]
features = ["log-level-info"]
