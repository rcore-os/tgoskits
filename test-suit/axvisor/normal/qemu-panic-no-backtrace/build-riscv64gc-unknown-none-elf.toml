features = [
    "test-panic-no-backtrace",
]
log = "Info"
target = "riscv64gc-unknown-none-elf"
vm_configs = []

# No BACKTRACE=y — the default-off path.
# The runtime emits panic text through its emergency writer and skips the raw
# frame walk entirely.
