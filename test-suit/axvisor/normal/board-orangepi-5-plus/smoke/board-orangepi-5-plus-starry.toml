board_type = "OrangePi-5-Plus-robot"
shell_prefix = "root@starry:/root #"
shell_init_cmd = "pwd && echo 'starry guest test pass'"
success_regex = [
  "(?m)^starry guest test pass\\s*$",
]
fail_regex = [
  "panicked at",
  "Failed to initialize guest VM",
]
timeout = 300
