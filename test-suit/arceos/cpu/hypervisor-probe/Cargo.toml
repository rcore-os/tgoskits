[package]
name = "arceos-cpu-hypervisor-probe"
version = "0.1.0"
edition.workspace = true
publish = false

[features]
default = ["ax-std"]
ax-std = ["dep:ax-std", "ax-std/arceos"]
expect-h = ["ax-std"]

[dependencies]
ax-cpu.workspace = true
ax-std = { workspace = true, optional = true, features = ["default"] }

[package.metadata.docs.rs]
targets = ["riscv64gc-unknown-linux-musl"]
