[package]
name = "arceos-backtrace-raw-basic"
version = "0.3.1"
edition.workspace = true
publish = false

[features]
ax-std = ["dep:ax-std"]
panic-path = []

[dependencies]
ax-driver.workspace = true
ax-hal.workspace = true
ax-std = { workspace = true, optional = true, features = ["paging", "backtrace"] }
axbacktrace = { workspace = true }
