[package]
name = "arceos-backtrace-raw-badfp"
version = "0.3.1"
edition.workspace = true
publish = false

[features]
ax-std = ["dep:ax-std"]

[dependencies]
ax-std = { workspace = true, optional = true, features = ["paging", "backtrace"] }
axbacktrace = { workspace = true }
