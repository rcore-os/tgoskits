[package]
name = "arceos-backtrace-panic"
version = "0.3.1"
edition.workspace = true
publish = false

[features]
ax-std = ["dep:ax-std"]

[dependencies]
ax-std = { workspace = true, optional = true, features = ["paging"] }
axbacktrace = { workspace = true }
