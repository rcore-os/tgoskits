# Guest oracle 期望（轨 B）

本目录存放 **`qemu-system-riscv64`** 下、**Alpine Linux 3.23.3 所对应 Linux 6.18 LTS 内核** 上跑探针得到的 **`*.line` / `*.cases`**。

- 锚点与版本钉死说明：**`docs/starryos-linux-guest-oracle-pin.md`**
- 与轨 A（`qemu-riscv64`）的 `expected/*.line` 或 `expected/user/` 并存；未接入脚本前可为空。
