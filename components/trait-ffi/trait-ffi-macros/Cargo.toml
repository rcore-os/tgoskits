[package]
name = "trait-ffi-macros"
version = "0.3.0"
edition.workspace = true
authors = ["周睿 <zrufo747@outlook.com>"]
license = "MIT"
repository.workspace = true
description = "Procedural macros for trait-ffi"

[lib]
proc-macro = true

[dependencies]
proc-macro2.workspace = true
quote.workspace = true
syn = { workspace = true, features = ["full", "extra-traits", "visit-mut", "visit"] }
convert_case = "0.8"
semver = "1.0"
