[package]
name = "trait-ffi-test-interface"
version = "0.0.0"
edition.workspace = true
license = "MIT"
publish = false

[features]
extra = []

[dependencies]
renamed-ffi = { package = "trait-ffi", path = "../../trait-ffi", version = "0.3.0" }
