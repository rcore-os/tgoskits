[package]
name = "trait-ffi-test-provider"
version = "0.0.0"
edition.workspace = true
license = "MIT"
publish = false

[dependencies]
renamed-interface = { package = "trait-ffi-test-interface", path = "../interface", features = ["extra"] }
