[package]
name = "icpc"
version = "0.1.0"
edition.workspace = true
description = "Industrial Control Plane Communication protocol (no_std)."
license = "Apache-2.0"
readme = "README.md"

[features]
default = []
alloc = []

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
