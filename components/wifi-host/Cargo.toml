[package]
name = "wifi-host"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "OS-independent Wi-Fi driver abstraction: WifiDriver (chip-facing) and WifiRuntime (OS-capability) traits."
# Board bring-up / in-tree integration only; not intended for crates.io.
publish = false

[dependencies]
rd-net = { workspace = true }
dma-api = { workspace = true }
