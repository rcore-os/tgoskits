[package]
name = "ax-printk"
version = "0.1.0"
repository = "https://github.com/rcore-os/tgoskits"
edition.workspace = true
authors = ["Yuekai Jia <equation618@gmail.com>"]
description = "Lockless multi-writer record ring buffer backing the kernel log (dmesg/kmsg)."
keywords = ["arceos", "ringbuffer", "lockless", "printk", "no-std"]
categories = ["os", "no-std", "data-structures"]
license.workspace = true

[dependencies]
axpanic.workspace = true
