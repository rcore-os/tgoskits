# vIRQ A/B 实验阶段性状态分析

**Session ID:** ses_01e5c3d6affeWGRmSvZtLR1Ulf
**Created:** 8/8/2026, 9:50:33 PM
**Updated:** 8/8/2026, 10:07:10 PM

---

## User

@docs/my/openrace-virq-ab-status.md 你来分析一下这个文档吧

---

## Assistant (Build · DeepSeek V4 Flash Free · 6.0s)

_Thinking:_

The user is asking me to analyze this document about a vIRQ A/B experiment status. Let me read the document first - it's already been read. The user wants me to analyze it.

Let me think about what they want. They said "你来分析一下这个文档吧" - "please analyze this document". I should provide an analysis. This is a technical document about a virtual IRQ A/B experiment comparing two software vIRQ injection paths in a hypervisor (AxVM/ArceOS based).

Let me look at the codebase to give a more informed analysis. The document references:
- `openrace/realtime-virq-ab` at `/home/huhu/tgoskits`
- `contest/openrace-2026` at `/home/huhu/tgoskits-2026`
- `scripts/test/zephyr-soft-virq`
- `scripts/test/virq_latency_stats.py`

I could explore the codebase to understand the context better. The user wants an analysis of the document. Let me do some exploration to understand what's going on, then provide a concise but thorough analysis.

Let me explore the relevant code paths.

**Tool: bash**

**Input:**
```json
{
  "command": "git log --oneline -5 && git status -s | head -20 && git branch --show-current"
}
```

**Output:**
```
32cfe9103 test(axvisor): add Zephyr periodic latency baseline
3a8374f4c docs(progress): record day3 device isolation baseline
1d9c18406 docs(progress): record day1 and day2 results
064661d81 feat:add meeting transcript
2478cd0d3 Merge pull request #1 from rcore-os/dev
 M os/axvisor/Cargo.toml
 M os/axvisor/src/main.rs
 M virtualization/axvm/Cargo.toml
 M virtualization/axvm/src/architecture/ops.rs
 M virtualization/axvm/src/lib.rs
 M virtualization/axvm/src/manager.rs
 M virtualization/axvm/src/runtime/dispatcher.rs
 M virtualization/axvm/src/runtime/mod.rs
 M virtualization/axvm/src/runtime/queue.rs
 M virtualization/axvm/src/runtime/vcpus.rs
 M virtualization/axvm/src/vm/mod.rs
?? docs/my/openrace-virq-ab-status.md
?? os/axvisor/src/realtime_probe.rs
?? plan.md
?? scripts/test/virq_latency_stats.py
?? scripts/test/zephyr-soft-virq/
?? virtualization/axvm/src/runtime/trace.rs
openrace/realtime-virq-ab

```

**Tool: glob**

**Input:**
```json
{
  "pattern": "scripts/test/virq_latency_stats.py"
}
```

**Output:**
```
/home/huhu/tgoskits/scripts/test/virq_latency_stats.py
```

**Tool: glob**

**Input:**
```json
{
  "pattern": "scripts/test/zephyr-soft-virq*"
}
```

**Output:**
```
No files found
```

---

## Assistant (Build · DeepSeek V4 Flash Free · 2.4s)

_Thinking:_

Let me look at the working tree changes and the relevant files to understand the analysis better.

**Tool: bash**

**Input:**
```json
{
  "command": "ls scripts/test/zephyr-soft-virq/ && git diff --stat | tail -5"
}
```

**Output:**
```
axvisor-qemu-aarch64.toml
CMakeLists.txt
prj.conf
README.md
src
 virtualization/axvm/src/runtime/mod.rs        |   8 +
 virtualization/axvm/src/runtime/queue.rs      |  68 ++++++--
 virtualization/axvm/src/runtime/vcpus.rs      | 170 ++++++++++++++++---
 virtualization/axvm/src/vm/mod.rs             | 226 ++++++++++++++++++++++----
 11 files changed, 477 insertions(+), 81 deletions(-)

```

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/huhu/tgoskits/scripts/test/virq_latency_stats.py"
}
```

**Output:**
```
<path>/home/huhu/tgoskits/scripts/test/virq_latency_stats.py</path>
<type>file</type>
<content>
1: #!/usr/bin/env python3
2: """Summarize one OpenRace software-vIRQ serial log."""
3: 
4: from __future__ import annotations
5: 
6: import argparse
7: import json
8: import re
9: import statistics
10: from pathlib import Path
11: 
12: 
13: INJECT_RE = re.compile(
14:     r"VIRQ_INJECT sequence=(?P<sequence>\d+) .*?requested_ns=(?P<requested>\d+) "
15:     r"completed_ns=(?P<completed>\d+) status=(?P<status>\w+)"
16: )
17: GUEST_RE = re.compile(r"^(?P<sequence>\d+),(?P<timestamp>\d+)$")
18: TRACE_OVERFLOW_RE = re.compile(r"VIRQ_TRACE .*event=queue_overflow")
19: 
20: 
21: def percentile(values: list[int], fraction: float) -> int:
22:     if not values:
23:         return 0
24:     ordered = sorted(values)
25:     index = min(len(ordered) - 1, max(0, int(fraction * len(ordered) + 0.999999) - 1))
26:     return ordered[index]
27: 
28: 
29: def summarize(path: Path, period_ns: int) -> dict[str, int | float]:
30:     injections: dict[int, tuple[int, int, str]] = {}
31:     guests: dict[int, int] = {}
32:     queue_overflow = 0
33:     for line in path.read_text(encoding="utf-8", errors="replace").splitlines():
34:         if match := INJECT_RE.search(line):
35:             sequence = int(match["sequence"])
36:             injections[sequence] = (
37:                 int(match["requested"]),
38:                 int(match["completed"]),
39:                 match["status"],
40:             )
41:             continue
42:         if TRACE_OVERFLOW_RE.search(line):
43:             queue_overflow += 1
44:             continue
45:         if match := GUEST_RE.match(line.strip()):
46:             guests[int(match["sequence"])] = int(match["timestamp"])
47: 
48:     response_ns = [guests[sequence] - requested for sequence, (requested, _, status) in injections.items()
49:                    if status == "ok" and sequence in guests]
50:     overrun_ns = [max(0, value - period_ns) for value in response_ns]
51:     matched = len(response_ns)
52:     lost_irq = max(0, len(injections) - matched)
53:     errors = sum(status != "ok" for _, _, status in injections.values())
54:     return {
55:         "injected": len(injections),
56:         "guest_received": len(guests),
57:         "matched": matched,
58:         "lost_irq": lost_irq,
59:         "inject_errors": errors,
60:         "queue_overflow": queue_overflow,
61:         "mean_ns": round(statistics.mean(response_ns)) if response_ns else 0,
62:         "p99_ns": percentile(response_ns, 0.99),
63:         "p99_9_ns": percentile(response_ns, 0.999),
64:         "max_ns": max(response_ns, default=0),
65:         "overrun_max_ns": max(overrun_ns, default=0),
66:     }
67: 
68: 
69: def main() -> None:
70:     parser = argparse.ArgumentParser()
71:     parser.add_argument("log", type=Path)
72:     parser.add_argument("--period-ns", type=int, default=10_000_000)
73:     args = parser.parse_args()
74:     result = summarize(args.log, args.period_ns)
75:     print(json.dumps(result, sort_keys=True))
76:     print(
77:         "p99={p99_ns}ns p99.9={p99_9_ns}ns max={max_ns}ns "
78:         "overrun_max={overrun_max_ns}ns lost={lost_irq} overflow={queue_overflow}".format(**result)
79:     )
80: 
81: 
82: if __name__ == "__main__":
83:     main()

(End of file - total 83 lines)
</content>
```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.1s)

_Thinking:_

Let me look at the key code changes to understand the B implementation and the analysis. Let me look at the runtime/queue.rs, vcpus.rs, dispatcher.rs diffs, and the zephyr workload.

**Tool: bash**

**Input:**
```json
{
  "command": "git diff virtualization/axvm/src/runtime/queue.rs virtualization/axvm/src/runtime/mod.rs"
}
```

**Output:**
```
diff --git a/virtualization/axvm/src/runtime/mod.rs b/virtualization/axvm/src/runtime/mod.rs
index 7935b755e..051f3d34e 100644
--- a/virtualization/axvm/src/runtime/mod.rs
+++ b/virtualization/axvm/src/runtime/mod.rs
@@ -18,12 +18,20 @@ pub(crate) mod vcpus;
 
 mod dispatcher;
 mod queue;
+mod trace;
+
+/// Maximum number of pending software interrupts retained per vCPU.
+pub(crate) const VCPU_INTERRUPT_QUEUE_CAPACITY: usize = 64;
+
 use core::sync::atomic::{AtomicUsize, Ordering};
 
 // Re-exported for [`VmRuntimeHandle`](crate::vm::VmRuntimeHandle) which will
 // embed the dispatcher as a field and expose it to the vCPU run loop.
 #[allow(unused_imports)]
 pub(crate) use dispatcher::VcpuIrqDispatcher;
+pub(crate) use trace::VirqTraceKind;
+#[cfg(feature = "realtime-trace")]
+pub(crate) use trace::VirqTraceRing;
 
 use crate::{AxVmError, AxVmResult, StopReason, VmStatus, ax_err};
 
diff --git a/virtualization/axvm/src/runtime/queue.rs b/virtualization/axvm/src/runtime/queue.rs
index 4df64ae76..0e13a9582 100644
--- a/virtualization/axvm/src/runtime/queue.rs
+++ b/virtualization/axvm/src/runtime/queue.rs
@@ -44,13 +44,17 @@ impl VcpuInterruptQueue {
         }
     }
 
-    /// Pushes a pending interrupt onto the queue for the given vCPU.
-    pub fn push(&self, vcpu_id: usize, interrupt: PendingVcpuInterrupt) {
-        self.pending
-            .lock()
+    /// Pushes a pending interrupt, rejecting it when the per-vCPU bound is full.
+    pub fn try_push(&self, vcpu_id: usize, interrupt: PendingVcpuInterrupt) -> Result<(), ()> {
+        let mut pending = self.pending.lock();
+        let queue = pending
             .entry(vcpu_id)
-            .or_default()
-            .push(interrupt);
+            .or_insert_with(|| Vec::with_capacity(crate::runtime::VCPU_INTERRUPT_QUEUE_CAPACITY));
+        if queue.len() >= crate::runtime::VCPU_INTERRUPT_QUEUE_CAPACITY {
+            return Err(());
+        }
+        queue.push(interrupt);
+        Ok(())
     }
 
     /// Drains all pending interrupts for the given vCPU, leaving its
@@ -62,6 +66,14 @@ impl VcpuInterruptQueue {
             .map(core::mem::take)
             .unwrap_or_default()
     }
+
+    /// Returns whether a vCPU has an interrupt waiting to be drained.
+    pub fn has_pending(&self, vcpu_id: usize) -> bool {
+        self.pending
+            .lock()
+            .get(&vcpu_id)
+            .is_some_and(|queue| !queue.is_empty())
+    }
 }
 
 #[cfg(all(test, feature = "host-test"))]
@@ -88,9 +100,9 @@ mod tests {
     #[test]
     fn push_preserves_fifo_order() {
         let q = VcpuInterruptQueue::new();
-        q.push(0, edge(10));
-        q.push(0, level(20));
-        q.push(0, edge(30));
+        q.try_push(0, edge(10)).unwrap();
+        q.try_push(0, level(20)).unwrap();
+        q.try_push(0, edge(30)).unwrap();
 
         let drained = q.drain(0);
         assert_eq!(drained.len(), 3);
@@ -102,8 +114,8 @@ mod tests {
     #[test]
     fn isolates_vcpus() {
         let q = VcpuInterruptQueue::new();
-        q.push(0, edge(1));
-        q.push(1, edge(2));
+        q.try_push(0, edge(1)).unwrap();
+        q.try_push(1, edge(2)).unwrap();
 
         assert_eq!(q.drain(0), vec![edge(1)]);
         assert_eq!(q.drain(1), vec![edge(2)]);
@@ -112,7 +124,7 @@ mod tests {
     #[test]
     fn drain_empties_queue() {
         let q = VcpuInterruptQueue::new();
-        q.push(0, edge(7));
+        q.try_push(0, edge(7)).unwrap();
         assert_eq!(q.drain(0).len(), 1);
         assert!(q.drain(0).is_empty());
     }
@@ -120,7 +132,7 @@ mod tests {
     #[test]
     fn double_drain_returns_empty() {
         let q = VcpuInterruptQueue::new();
-        q.push(0, edge(7));
+        q.try_push(0, edge(7)).unwrap();
         q.drain(0);
         assert!(q.drain(0).is_empty());
     }
@@ -128,8 +140,8 @@ mod tests {
     #[test]
     fn trigger_mode_round_trips() {
         let q = VcpuInterruptQueue::new();
-        q.push(0, edge(42));
-        q.push(0, level(43));
+        q.try_push(0, edge(42)).unwrap();
+        q.try_push(0, level(43)).unwrap();
 
         let drained = q.drain(0);
         assert_eq!(drained.len(), 2);
@@ -142,4 +154,30 @@ mod tests {
             crate::InterruptTriggerMode::LevelTriggered
         );
     }
+
+    #[test]
+    fn rejects_interrupt_when_capacity_is_reached() {
+        let q = VcpuInterruptQueue::new();
+
+        for id in 0..crate::runtime::VCPU_INTERRUPT_QUEUE_CAPACITY {
+            q.try_push(0, edge(id as u32)).unwrap();
+        }
+
+        assert!(q.try_push(0, edge(999)).is_err());
+        assert_eq!(
+            q.drain(0).len(),
+            crate::runtime::VCPU_INTERRUPT_QUEUE_CAPACITY
+        );
+    }
+
+    #[test]
+    fn pending_state_clears_after_drain() {
+        let q = VcpuInterruptQueue::new();
+
+        assert!(!q.has_pending(0));
+        q.try_push(0, edge(7)).unwrap();
+        assert!(q.has_pending(0));
+        q.drain(0);
+        assert!(!q.has_pending(0));
+    }
 }

```

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/huhu/tgoskits/scripts/test/zephyr-soft-virq/README.md"
}
```

**Output:**
```
<path>/home/huhu/tgoskits/scripts/test/zephyr-soft-virq/README.md</path>
<type>file</type>
<content>
1: # Zephyr software-vIRQ consumer
2: 
3: This is the common guest workload for the OpenRace A/B experiment. It runs
4: with one vCPU, installs an ISR for virtual interrupt 48, and records the
5: guest-side timestamp of each interrupt. The host injector is deliberately
6: outside this directory and is identical for A and B.
7: 
8: Build the raw image with the same Zephyr revision/toolchain as
9: `zephyr-periodic`:
10: 
11: ```bash
12: west build -p always -b qemu_cortex_a53 \
13:   scripts/test/zephyr-soft-virq -d /tmp/zephyr-soft-virq-build
14: ```
15: 
16: Use `zephyr.bin` (not the ELF) in
17: `axvisor-qemu-aarch64.toml`, then build AxVisor with the experiment feature:
18: 
19: ```bash
20: FEATURES=openrace-realtime cargo xtask axvisor qemu \
21:   --config os/axvisor/configs/board/qemu-aarch64.toml \
22:   --qemu-config os/axvisor/configs/qemu/qemu-aarch64.toml \
23:   --vmconfigs scripts/test/zephyr-soft-virq/axvisor-qemu-aarch64.toml
24: ```
25: 
26: The run is valid only when the log contains both:
27: 
28: ```text
29: SOFTWARE VIRQ COMPLETE samples=300
30: VIRQ_INJECT_COMPLETE ... errors=0
31: ```
32: 
33: Save the complete serial log. `VIRQ_INJECT` lines are the host-side request
34: timestamps; the CSV lines are guest ISR timestamps. `VIRQ_TRACE` lines are
35: the bounded per-host-CPU delivery trace.

(End of file - total 35 lines)
</content>
```

---

## Assistant (Build · DeepSeek V4 Flash Free · 2.9s)

_Thinking:_

Let me look at the vcpus.rs diff and dispatcher to understand the B path and the current analysis. Also let me check the trace.rs file.

**Tool: bash**

**Input:**
```json
{
  "command": "git diff virtualization/axvm/src/runtime/vcpus.rs | head -250"
}
```

**Output:**
```
diff --git a/virtualization/axvm/src/runtime/vcpus.rs b/virtualization/axvm/src/runtime/vcpus.rs
index 991219edf..521e73c99 100644
--- a/virtualization/axvm/src/runtime/vcpus.rs
+++ b/virtualization/axvm/src/runtime/vcpus.rs
@@ -13,25 +13,137 @@
 // limitations under the License.
 
 use alloc::{format, sync::Arc};
+use core::time::Duration;
 
 use crate::{
     AsVCpuTask, AxVmResult, GuestPhysAddr, StopReason, VCpuTask, VmStatus, VmVcpuState,
     arch::{ArchOps, CurrentArch, VcpuRunAction},
     ax_err_type,
+    host::HostTime,
+    irq::model::{PendingVcpuInterrupt, VirtualInterruptId},
     runtime::{VCpuRef, VMRef, sub_running_vm_count},
     vm::VmRuntimeHandle,
 };
 
 const KERNEL_STACK_SIZE: usize = 0x40000; // 256 KiB
+const PERIODIC_VIRQ_STACK_SIZE: usize = 0x10000;
+
+/// Spawn the common host-side periodic injector used by both A and B.
+pub(crate) fn spawn_periodic_virq_injector(
+    vm: VMRef,
+    config: crate::PeriodicVirqConfig,
+) -> AxVmResult {
+    if config.samples == 0 {
+        return Err(ax_err_type!(
+            BadState,
+            "periodic vIRQ samples must be non-zero"
+        ));
+    }
+    if config.period.is_zero() {
+        return Err(ax_err_type!(
+            BadState,
+            "periodic vIRQ period must be non-zero"
+        ));
+    }
+    if vm.vcpu(config.vcpu_id).is_none() {
+        return Err(ax_err_type!(
+            NotFound,
+            format!("vCPU {} not found", config.vcpu_id)
+        ));
+    }
+
+    let task = crate::TaskInner::new(
+        move || run_periodic_virq_injector(vm, config),
+        format!("openrace-virq-injector-vcpu-{}", config.vcpu_id),
+        PERIODIC_VIRQ_STACK_SIZE,
+    );
+    if let Some(cpu_id) = config.injector_cpu_id {
+        let bits = 1usize.checked_shl(cpu_id as u32).ok_or_else(|| {
+            ax_err_type!(
+                InvalidInput,
+                format!("injector CPU {cpu_id} is not representable")
+            )
+        })?;
+        task.set_cpumask(crate::host::task::cpu_mask_from_raw_bits(bits));
+    }
+    crate::host::task::spawn_task(task);
+    Ok(())
+}
+
+fn run_periodic_virq_injector(vm: VMRef, config: crate::PeriodicVirqConfig) {
+    let wait_started = ax_std::time::Instant::now();
+    while !vm.running() {
+        if vm.stopped() || wait_started.elapsed() >= Duration::from_secs(5) {
+            warn!(
+                "OpenRace vIRQ injector did not observe VM[{}] running before timeout",
+                vm.id()
+            );
+            return;
+        }
+        ax_std::thread::sleep(Duration::from_millis(1));
+    }
+
+    let targets = crate::CpuMask::<64>::one_shot(config.vcpu_id);
+    let mut deadline = ax_std::time::Instant::now() + config.period;
+    let mut failed = 0usize;
+    for sequence in 0..config.samples {
+        let now = ax_std::time::Instant::now();
+        let remaining = deadline.duration_since(now);
+        if !remaining.is_zero() {
+            ax_std::thread::sleep(remaining);
+        }
+        let requested_ns = crate::host::default_host().monotonic_time().as_nanos() as u64;
+        let result = vm.inject_interrupt_to_vcpu(targets, config.vector);
+        let completed_ns = crate::host::default_host().monotonic_time().as_nanos() as u64;
+        if result.is_err() {
+            failed += 1;
+        }
+        info!(
+            "VIRQ_INJECT sequence={} vm={} vcpu={} vector={} requested_ns={} completed_ns={} \
+             status={}",
+            sequence,
+            vm.id(),
+            config.vcpu_id,
+            config.vector,
+            requested_ns,
+            completed_ns,
+            if result.is_ok() { "ok" } else { "error" },
+        );
+        deadline += config.period;
+        if !vm.running() {
+            break;
+        }
+    }
+    info!(
+        "VIRQ_INJECT_COMPLETE vm={} vcpu={} vector={} samples={} errors={}",
+        vm.id(),
+        config.vcpu_id,
+        config.vector,
+        config.samples,
+        failed,
+    );
+    #[cfg(feature = "realtime-trace")]
+    vm.dump_realtime_trace().unwrap_or_else(|err| {
+        warn!(
+            "VM[{}] failed to dump realtime vIRQ trace: {err:?}",
+            vm.id()
+        );
+    });
+}
 
 /// Blocks the current thread until it is explicitly woken up, using the wait queue
 /// associated with the VCpus of the specified VM.
 ///
 /// # Arguments
 ///
-/// * `vm_id` - The ID of the VM whose VCpu wait queue is used to block the current thread.
-fn wait(vm_vcpus: &VmRuntimeHandle) {
-    vm_vcpus.wait();
+/// * `vcpu_id` - The vCPU whose wait queue is used to block the current thread.
+fn wait(vm: &VMRef, vm_vcpus: &VmRuntimeHandle, vcpu_id: usize) {
+    vm_vcpus.wait_vcpu_until(vcpu_id, || {
+        vm_vcpus.has_pending_vcpu_interrupt(vcpu_id)
+            || !vm.running()
+            || vm.suspending()
+            || vm.stopping()
+    });
 }
 
 /// Blocks the current thread until the provided condition is met, using the wait queue
@@ -39,13 +151,13 @@ fn wait(vm_vcpus: &VmRuntimeHandle) {
 ///
 /// # Arguments
 ///
-/// * `vm_id` - The ID of the VM whose VCpu wait queue is used to block the current thread.
+/// * `vcpu_id` - The vCPU whose wait queue is used for the wait.
 /// * `condition` - A closure that returns a boolean value indicating whether the condition is met.
-fn wait_for<F>(vm_vcpus: &VmRuntimeHandle, condition: F)
+fn wait_for<F>(vm_vcpus: &VmRuntimeHandle, vcpu_id: usize, condition: F)
 where
     F: Fn() -> bool,
 {
-    vm_vcpus.wait_until(condition);
+    vm_vcpus.wait_vcpu_until(vcpu_id, condition);
 }
 
 /// Notifies the primary VCpu task associated with the specified VM to wake up and resume execution.
@@ -61,7 +173,7 @@ pub(crate) fn notify_primary_vcpu(vm_id: usize) {
         return;
     };
     if let Err(err) = vm.with_runtime(|runtime| {
-        runtime.notify_one();
+        runtime.notify_vcpu_startup(0);
         Ok(())
     }) {
         warn!("VM[{vm_id}] vCPU runtime not found: {err:?}");
@@ -93,13 +205,18 @@ pub(crate) fn queue_interrupt(vm_id: usize, vcpu_id: usize, vector: usize) -> Ax
         ));
     }
 
-    let cpu_id = vm.with_runtime(|runtime| runtime.queue_interrupt(vcpu_id, vector))?;
     vm.with_runtime(|runtime| {
-        runtime.notify_all();
-        Ok(())
-    })?;
-    crate::host::task::send_ipi(cpu_id);
-    Ok(())
+        // Preserve the vector-only API as an edge-triggered pulse while
+        // routing it through the same enqueue/notify/IPI path as modelled
+        // virtual interrupt senders.
+        runtime.dispatch_vcpu_interrupt(
+            vcpu_id,
+            PendingVcpuInterrupt {
+                id: VirtualInterruptId(vector as u32),
+                trigger: crate::InterruptTriggerMode::EdgeTriggered,
+            },
+        )
+    })
 }
 
 #[expect(
@@ -313,8 +430,13 @@ pub(crate) fn vcpu_on(
             .insert_cpu_on_start_ack(vcpu_id, ack.clone())
             .map_err(|_| VcpuOnError::StartFailed)?;
 
-        let vcpu_task = alloc_vcpu_task(&vm, vcpu.clone());
-        if runtime.add_vcpu_task(vcpu_id, vcpu_task).is_err() {
+        runtime.prepare_vcpu_wait_queue(vcpu_id);
+        let vcpu_task = crate::host::task::spawn_task(build_vcpu_task(&vm, vcpu.clone()));
+        let cpu_id = vcpu
+            .phys_cpu_set()
+            .and_then(|mask| (mask != 0).then(|| mask.trailing_zeros() as usize))
+            .unwrap_or_else(|| vcpu_task.cpu_id() as usize);
+        if runtime.add_vcpu_task(vcpu_id, vcpu_task, cpu_id).is_err() {
             runtime.remove_cpu_on_start_ack(vcpu_id);
             return Err(VcpuOnError::StartFailed);
         }
@@ -451,12 +573,14 @@ fn vcpu_run() {
 
     info!("VM[{}] VCpu[{}] waiting for running", vm.id(), vcpu.id());
     let cpu_on_start_ack = runtime.cpu_on_start_ack(vcpu_id);
-    wait_for(&runtime, || {
-        vm.running()
-            || cpu_on_start_ack
-                .as_ref()
-                .is_some_and(|ack| ack.is_cancelled())
-    });
+    if cpu_on_start_ack.is_some() || !vm.running() {
+        wait_for(&runtime, vcpu_id, || {
+            vm.running()
+                || cpu_on_start_ack
+                    .as_ref()
+                    .is_some_and(|ack| ack.is_cancelled())
+        });
+    }
 
     if let Some(ack) = &cpu_on_start_ack {
         if !ack.begin_startup() {
@@ -538,7 +662,7 @@ fn vcpu_run() {
             Ok(VcpuRunAction {
                 waits_for_event: true,
                 ..
-            }) => wait(&runtime),
+            }) => wait(&vm, &runtime, vcpu_id),
             Ok(VcpuRunAction { .. }) => {}
             Err(err) => {
                 error!("VM[{vm_id}] run VCpu[{vcpu_id}] get error {err:?}");
@@ -556,7 +680,7 @@ fn vcpu_run() {
                 "VM[{}] VCpu[{}] is suspended, waiting for resume...",
                 vm_id, vcpu_id
             );
-            wait_for(&runtime, || !vm.suspending());
+            wait_for(&runtime, vcpu_id, || !vm.suspending());
             info!("VM[{}] VCpu[{}] resumed from suspend", vm_id, vcpu_id);
             continue;

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 2.7s)

_Thinking:_

Now let me look at the dispatcher and the vm/mod.rs diff for the B path details (dispatch_vcpu_interrupt, drain, inject).

**Tool: bash**

**Input:**
```json
{
  "command": "git diff virtualization/axvm/src/runtime/dispatcher.rs && git diff virtualization/axvm/src/vm/mod.rs | head -300"
}
```

**Output:**
```
diff --git a/virtualization/axvm/src/runtime/dispatcher.rs b/virtualization/axvm/src/runtime/dispatcher.rs
index 2281c4c97..5276ffb04 100644
--- a/virtualization/axvm/src/runtime/dispatcher.rs
+++ b/virtualization/axvm/src/runtime/dispatcher.rs
@@ -40,6 +40,7 @@ use crate::{AxTaskRef, AxVmResult, ax_err_type, irq::model::PendingVcpuInterrupt
 pub struct VcpuIrqDispatcher {
     queue: VcpuInterruptQueue,
     vcpu_tasks: Mutex<BTreeMap<usize, AxTaskRef>>,
+    vcpu_cpu_ids: Mutex<BTreeMap<usize, usize>>,
     /// Test-only cpu_id registry so that round-trip tests can exercise
     /// enqueue / drain without a full ArceOS task infrastructure.
     #[cfg(all(test, feature = "host-test"))]
@@ -55,6 +56,7 @@ impl VcpuIrqDispatcher {
         Self {
             queue: VcpuInterruptQueue::new(),
             vcpu_tasks: Mutex::new(BTreeMap::new()),
+            vcpu_cpu_ids: Mutex::new(BTreeMap::new()),
             #[cfg(all(test, feature = "host-test"))]
             test_vcpu_cpu_ids: Mutex::new(BTreeMap::new()),
         }
@@ -65,8 +67,9 @@ impl VcpuIrqDispatcher {
     ///
     /// Called from `VmRuntimeHandle::add_vcpu_task` when a vCPU task is
     /// spawned and bound to the VM runtime.
-    pub fn register_vcpu_task(&self, vcpu_id: usize, task: AxTaskRef) {
+    pub fn register_vcpu_task(&self, vcpu_id: usize, task: AxTaskRef, cpu_id: usize) {
         self.vcpu_tasks.lock().insert(vcpu_id, task);
+        self.vcpu_cpu_ids.lock().insert(vcpu_id, cpu_id);
     }
 
     #[cfg(all(test, feature = "host-test"))]
@@ -82,6 +85,7 @@ impl VcpuIrqDispatcher {
     /// Unregisters a vCPU task after the vCPU powers off.
     pub fn unregister_vcpu_task(&self, vcpu_id: usize) {
         self.vcpu_tasks.lock().remove(&vcpu_id);
+        self.vcpu_cpu_ids.lock().remove(&vcpu_id);
         self.queue.drain(vcpu_id);
         #[cfg(all(test, feature = "host-test"))]
         self.test_vcpu_cpu_ids.lock().remove(&vcpu_id);
@@ -96,8 +100,9 @@ impl VcpuIrqDispatcher {
     /// 2. Lock `queue`, push the interrupt, release.
     ///
     /// A task migration window exists between steps 1 and 2 (the pCPU may
-    /// change), but `notify_all()` + `send_ipi` in the caller guarantee
-    /// eventual delivery regardless of the race.
+    /// change). vCPU tasks are affinity-bound in the realtime workload; a
+    /// future migratable-vCPU caller must refresh the returned pCPU before
+    /// issuing its IPI.
     ///
     /// # Errors
     ///
@@ -108,7 +113,12 @@ impl VcpuIrqDispatcher {
     /// architecture interrupt router requests delivery to a vCPU.
     pub fn enqueue(&self, vcpu_id: usize, interrupt: PendingVcpuInterrupt) -> AxVmResult<usize> {
         let cpu_id = self.lookup_cpu_id(vcpu_id)?;
-        self.queue.push(vcpu_id, interrupt);
+        self.queue.try_push(vcpu_id, interrupt).map_err(|_| {
+            crate::AxVmError::resource_unavailable(
+                "vCPU interrupt queue",
+                format_args!("vCPU {vcpu_id} queue capacity reached"),
+            )
+        })?;
         Ok(cpu_id)
     }
 
@@ -119,10 +129,10 @@ impl VcpuIrqDispatcher {
                 return Ok(cpu_id);
             }
         }
-        let tasks = self.vcpu_tasks.lock();
-        tasks
+        let cpu_ids = self.vcpu_cpu_ids.lock();
+        cpu_ids
             .get(&vcpu_id)
-            .map(|t| t.cpu_id() as usize)
+            .copied()
             .ok_or_else(|| ax_err_type!(NotFound, format_args!("vCPU {vcpu_id} task not found")))
     }
 
@@ -135,4 +145,10 @@ impl VcpuIrqDispatcher {
     pub fn drain(&self, vcpu_id: usize) -> Vec<PendingVcpuInterrupt> {
         self.queue.drain(vcpu_id)
     }
+
+    /// Returns whether a vCPU has a queued interrupt that should prevent it
+    /// from sleeping through a notify race.
+    pub fn has_pending(&self, vcpu_id: usize) -> bool {
+        self.queue.has_pending(vcpu_id)
+    }
 }
diff --git a/virtualization/axvm/src/vm/mod.rs b/virtualization/axvm/src/vm/mod.rs
index bd5792675..20d545f4d 100644
--- a/virtualization/axvm/src/vm/mod.rs
+++ b/virtualization/axvm/src/vm/mod.rs
@@ -186,11 +186,15 @@ pub(crate) enum PendingInterrupt {
 /// Runtime-only resources owned by Running/Paused/Stopping lifecycle states.
 pub(crate) struct VmRuntimeHandle {
     wait_queue: crate::WaitQueue,
+    vcpu_wait_queues: Mutex<BTreeMap<usize, Arc<crate::WaitQueue>>>,
     vcpu_task_list: Mutex<BTreeMap<usize, crate::AxTaskRef>>,
     cpu_on_start_acks: Mutex<BTreeMap<usize, Arc<crate::runtime::vcpus::CpuOnStartAck>>>,
     cpu_off_exit_reservations: Mutex<BTreeSet<usize>>,
     pending_interrupts: Mutex<BTreeMap<usize, Vec<PendingInterrupt>>>,
     irq_dispatcher: crate::runtime::VcpuIrqDispatcher,
+    #[cfg(feature = "realtime-trace")]
+    virq_trace: crate::runtime::VirqTraceRing,
+    trace_vm_id: AtomicUsize,
     running_halting_vcpu_count: AtomicUsize,
     deferred_reset_requested: AtomicBool,
 }
@@ -217,11 +221,15 @@ impl VmRuntimeHandle {
     pub(crate) fn new() -> Self {
         Self {
             wait_queue: crate::WaitQueue::new(),
+            vcpu_wait_queues: Mutex::new(BTreeMap::new()),
             vcpu_task_list: Mutex::new(BTreeMap::new()),
             cpu_on_start_acks: Mutex::new(BTreeMap::new()),
             cpu_off_exit_reservations: Mutex::new(BTreeSet::new()),
             pending_interrupts: Mutex::new(BTreeMap::new()),
             irq_dispatcher: crate::runtime::VcpuIrqDispatcher::new(),
+            #[cfg(feature = "realtime-trace")]
+            virq_trace: crate::runtime::VirqTraceRing::new(),
+            trace_vm_id: AtomicUsize::new(0),
             running_halting_vcpu_count: AtomicUsize::new(0),
             deferred_reset_requested: AtomicBool::new(false),
         }
@@ -232,14 +240,23 @@ impl VmRuntimeHandle {
         self.vcpu_task_list.lock().contains_key(&vcpu_id)
     }
 
-    pub(crate) fn add_vcpu_task(&self, vcpu_id: usize, vcpu_task: crate::AxTaskRef) -> AxVmResult {
+    pub(crate) fn add_vcpu_task(
+        &self,
+        vcpu_id: usize,
+        vcpu_task: crate::AxTaskRef,
+        cpu_id: usize,
+    ) -> AxVmResult {
         let mut vcpu_task_list = self.vcpu_task_list.lock();
         if vcpu_task_list.contains_key(&vcpu_id) {
             return ax_err!(BadState, format!("vCPU {vcpu_id} task already exists"));
         }
 
+        self.vcpu_wait_queues
+            .lock()
+            .entry(vcpu_id)
+            .or_insert_with(|| Arc::new(crate::WaitQueue::new()));
         self.irq_dispatcher
-            .register_vcpu_task(vcpu_id, vcpu_task.clone());
+            .register_vcpu_task(vcpu_id, vcpu_task.clone(), cpu_id);
         vcpu_task_list.insert(vcpu_id, vcpu_task);
         drop(vcpu_task_list);
 
@@ -247,6 +264,17 @@ impl VmRuntimeHandle {
         Ok(())
     }
 
+    /// Publishes the wait queue used by a vCPU task before the task is
+    /// spawned.  Keeping this publication separate from task registration
+    /// closes the spawn-before-register window in which the task could sleep
+    /// on the legacy VM-wide queue.
+    pub(crate) fn prepare_vcpu_wait_queue(&self, vcpu_id: usize) {
+        self.vcpu_wait_queues
+            .lock()
+            .entry(vcpu_id)
+            .or_insert_with(|| Arc::new(crate::WaitQueue::new()));
+    }
+
     pub(crate) fn remove_cpu_on_start_ack(
         &self,
         vcpu_id: usize,
@@ -256,6 +284,7 @@ impl VmRuntimeHandle {
 
     pub(crate) fn remove_vcpu_task(&self, vcpu_id: usize) -> Option<crate::AxTaskRef> {
         self.pending_interrupts.lock().remove(&vcpu_id);
+        self.vcpu_wait_queues.lock().remove(&vcpu_id);
         self.irq_dispatcher.unregister_vcpu_task(vcpu_id);
         self.vcpu_task_list.lock().remove(&vcpu_id)
     }
@@ -284,21 +313,6 @@ impl VmRuntimeHandle {
         self.cpu_on_start_acks.lock().get(&vcpu_id).cloned()
     }
 
-    pub(crate) fn queue_interrupt(&self, vcpu_id: usize, vector: usize) -> AxVmResult<usize> {
-        let task = self
-            .vcpu_task_list
-            .lock()
-            .get(&vcpu_id)
-            .cloned()
-            .ok_or_else(|| ax_err_type!(NotFound, format!("vCPU {vcpu_id} task not found")))?;
-        self.pending_interrupts
-            .lock()
-            .entry(vcpu_id)
-            .or_default()
-            .push(PendingInterrupt::Normal(vector));
-        Ok(task.cpu_id() as usize)
-    }
-
     #[expect(
         dead_code,
         reason = "only the LoongArch IRQ backend queues physical interrupts"
@@ -330,31 +344,108 @@ impl VmRuntimeHandle {
     ///
     /// The dispatcher releases its queue lock before this method notifies
     /// waiters or invokes the host IPI boundary.
-    #[cfg_attr(
-        not(test),
-        expect(
-            dead_code,
-            reason = "architecture interrupt routers dispatch in later modules"
-        )
-    )]
     pub(crate) fn dispatch_vcpu_interrupt(
         &self,
         vcpu_id: usize,
         interrupt: PendingVcpuInterrupt,
     ) -> AxVmResult {
         dispatch_vcpu_interrupt_with(
-            || self.irq_dispatcher.enqueue(vcpu_id, interrupt),
-            || self.notify_all(),
-            crate::host::task::send_ipi,
+            || {
+                let result = self.irq_dispatcher.enqueue(vcpu_id, interrupt);
+                match result {
+                    Ok(cpu_id) => {
+                        self.trace_virq_event(
+                            0,
+                            crate::runtime::VirqTraceKind::Enqueue,
+                            vcpu_id,
+                            interrupt.id.0,
+                        );
+                        Ok(cpu_id)
+                    }
+                    Err(err) => {
+                        self.trace_virq_event(
+                            0,
+                            crate::runtime::VirqTraceKind::QueueOverflow,
+                            vcpu_id,
+                            interrupt.id.0,
+                        );
+                        Err(err)
+                    }
+                }
+            },
+            || {
+                self.notify_vcpu(vcpu_id);
+                self.trace_virq_event(
+                    0,
+                    crate::runtime::VirqTraceKind::Notify,
+                    vcpu_id,
+                    interrupt.id.0,
+                );
+            },
+            |cpu_id| {
+                crate::host::task::send_ipi(cpu_id);
+                self.trace_virq_event(
+                    0,
+                    crate::runtime::VirqTraceKind::Ipi,
+                    vcpu_id,
+                    interrupt.id.0,
+                );
+            },
         )
     }
 
+    pub(crate) fn trace_virq_event(
+        &self,
+        vm_id: usize,
+        kind: crate::runtime::VirqTraceKind,
+        vcpu_id: usize,
+        vector: u32,
+    ) {
+        #[cfg(feature = "realtime-trace")]
+        self.virq_trace.record(
+            kind,
+            if vm_id == 0 {
+                self.trace_vm_id.load(Ordering::Relaxed)
+            } else {
+                vm_id
+            },
+            vcpu_id,
+            vector,
+        );
+        #[cfg(not(feature = "realtime-trace"))]
+        let _ = (vm_id, kind, vcpu_id, vector);
+    }
+
+    pub(crate) fn set_trace_vm_id(&self, vm_id: usize) {
+        self.trace_vm_id.store(vm_id, Ordering::Relaxed);
+    }
+
+    #[cfg(feature = "realtime-trace")]
+    pub(crate) fn log_virq_trace(&self) {
+        for event in self.virq_trace.snapshot() {
+            info!(
+                "VIRQ_TRACE seq={} ts_ns={} cpu={} vm={} vcpu={} event={} vector={}",
+                event.sequence,
+                event.timestamp_ns,
+                event.cpu_id,
+                event.vm_id,
+                event.vcpu_id,
+                event.kind.as_str(),
+                event.vector,
+            );
+        }
+    }
+
     /// Called by the vCPU run loop to drain pending interrupts before
     /// entering the guest.
     pub(crate) fn irq_dispatcher(&self) -> &VcpuIrqDispatcher {
         &self.irq_dispatcher
     }
 
+    pub(crate) fn has_pending_vcpu_interrupt(&self, vcpu_id: usize) -> bool {
+        self.irq_dispatcher.has_pending(vcpu_id)
+    }
+
     pub(crate) fn drain_pending_interrupts(&self, vcpu_id: usize) -> Vec<PendingInterrupt> {
         self.pending_interrupts
             .lock()
@@ -363,20 +454,50 @@ impl VmRuntimeHandle {
             .unwrap_or_default()
     }
 
-    pub(crate) fn wait(&self) {
-        self.wait_queue.wait();
-    }
-
     pub(crate) fn wait_until(&self, condition: impl Fn() -> bool) {
         self.wait_queue.wait_until(condition);
     }
 
-    pub(crate) fn notify_one(&self) {
-        self.wait_queue.notify_one(false);
+    pub(crate) fn wait_vcpu_until(&self, vcpu_id: usize, condition: impl Fn() -> bool) {
+        if let Some(wait_queue) = self.vcpu_wait_queues.lock().get(&vcpu_id).cloned() {
+            wait_queue.wait_until(condition);
+        } else {
+            self.wait_until(condition);
+        }
     }
 
     pub(crate) fn notify_all(&self) {
         self.wait_queue.notify_all(false);
+        let wait_queues: Vec<_> = self.vcpu_wait_queues.lock().values().cloned().collect();
+        for wait_queue in wait_queues {
+            wait_queue.notify_all(false);
+        }
+    }
+
+    /// Wakes only the vCPU that owns a queued interrupt.
+    pub(crate) fn notify_vcpu(&self, vcpu_id: usize) {
+        if !self.irq_dispatcher.has_pending(vcpu_id) {
+            return;
+        }
+        self.notify_vcpu_unconditional(vcpu_id);
+    }
+
+    pub(crate) fn notify_vcpu_unconditional(&self, vcpu_id: usize) {
+        if let Some(wait_queue) = self.vcpu_wait_queues.lock().get(&vcpu_id).cloned() {
+            wait_queue.notify_one(false);
+        } else {
+            // A vCPU task can run immediately after spawn and reach the
+            // startup wait before `add_vcpu_task` publishes its private
+            // queue. Wake the legacy queue for that short publication
+            // window; steady-state vIRQ delivery always has a private queue.
+            self.wait_queue.notify_one(false);
+        }
+    }
+
+    /// Wakes a vCPU during startup, including the legacy queue used before
+    /// its private queue is published by [`Self::add_vcpu_task`].
+    pub(crate) fn notify_vcpu_startup(&self, vcpu_id: usize) {
+        self.notify_vcpu_unconditional(vcpu_id);
     }
 
     pub(crate) fn mark_vcpu_running(&self) {
@@ -965,8 +1086,12 @@ impl AxVM {
         let primary_vcpu = self
             .vcpu(0)
             .ok_or_else(|| ax_err_type!(BadState, "VM primary vCPU is not prepared"))?;
+        let cpu_id = primary_vcpu
+            .phys_cpu_set()
+            .and_then(|mask| (mask != 0).then(|| mask.trailing_zeros() as usize));
         let primary_task = crate::runtime::vcpus::build_vcpu_task(self, primary_vcpu);
         let runtime = Arc::new(VmRuntimeHandle::new());
+        runtime.set_trace_vm_id(self.id());
 
         self.machine.lock().start_with(|resources| {
             resources
@@ -981,8 +1106,10 @@ impl AxVM {

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 2.9s)

_Thinking:_

Now I have a good understanding of the code. Let me also check the trace.rs file and how the drain/inject happens in the run loop (architecture/ops.rs diff).

**Tool: bash**

**Input:**
```json
{
  "command": "git diff virtualization/axvm/src/architecture/ops.rs os/axvisor/src/main.rs os/axvisor/src/realtime_probe.rs | head -200"
}
```

**Output:**
```
diff --git a/os/axvisor/src/main.rs b/os/axvisor/src/main.rs
index 61e59c36f..e5348dfd8 100644
--- a/os/axvisor/src/main.rs
+++ b/os/axvisor/src/main.rs
@@ -34,6 +34,8 @@ mod config;
 mod manager;
 #[cfg(target_arch = "riscv64")]
 mod platform_irq;
+#[cfg(feature = "openrace-realtime")]
+mod realtime_probe;
 mod shell;
 
 #[cfg(any(feature = "backtrace", feature = "test-panic-no-backtrace"))]
@@ -75,6 +77,8 @@ fn main() {
         .unwrap_or_else(|error| panic!("failed to initialize AxVM manager: {error:#}"));
 
     manager.init_default_vms();
+    #[cfg(feature = "openrace-realtime")]
+    realtime_probe::start();
     manager.start_default_vms();
 
     info!("[OK] Default guest initialized");
diff --git a/virtualization/axvm/src/architecture/ops.rs b/virtualization/axvm/src/architecture/ops.rs
index aa0aaf153..b2c018e1a 100644
--- a/virtualization/axvm/src/architecture/ops.rs
+++ b/virtualization/axvm/src/architecture/ops.rs
@@ -191,9 +191,34 @@ fn drain_and_inject_dispatched_interrupts<A: ArchOps>(
             return;
         }
     };
-    inject_drained_interrupts::<A>(runtime.irq_dispatcher(), vm.id(), vcpu_id, vcpu);
+    runtime.trace_virq_event(vm.id(), crate::runtime::VirqTraceKind::Running, vcpu_id, 0);
+    let interrupts = runtime.irq_dispatcher().drain(vcpu_id);
+    if !interrupts.is_empty() {
+        runtime.trace_virq_event(
+            vm.id(),
+            crate::runtime::VirqTraceKind::Drain,
+            vcpu_id,
+            interrupts[0].id.0,
+        );
+    }
+    for interrupt in interrupts {
+        runtime.trace_virq_event(
+            vm.id(),
+            crate::runtime::VirqTraceKind::Inject,
+            vcpu_id,
+            interrupt.id.0,
+        );
+        if let Err(err) = A::inject_vcpu_interrupt(vcpu, interrupt) {
+            warn!(
+                "VM[{}] VCpu[{}] failed to inject interrupt {interrupt:?}: {err:?}",
+                vm.id(),
+                vcpu_id
+            );
+        }
+    }
 }
 
+#[cfg(test)]
 fn inject_drained_interrupts<A: ArchOps>(
     dispatcher: &crate::runtime::VcpuIrqDispatcher,
     vm_id: usize,

```

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/huhu/tgoskits/virtualization/axvm/src/runtime/trace.rs"
}
```

**Output:**
```
<path>/home/huhu/tgoskits/virtualization/axvm/src/runtime/trace.rs</path>
<type>file</type>
<content>
1: // Copyright 2025 The Axvisor Team
2: //
3: // Licensed under the Apache License, Version 2.0 (the "License");
4: // you may not use this file except in compliance with the License.
5: // You may obtain a copy of the License at
6: //
7: //     http://www.apache.org/licenses/LICENSE-2.0
8: //
9: // Unless required by applicable law or agreed to in writing, software
10: // distributed under the License is distributed on an "AS IS" BASIS,
11: // WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
12: // See the License for the specific language governing permissions and
13: // limitations under the License.
14: 
15: //! Bounded host-side trace for the software-vIRQ delivery path.
16: //!
17: //! The ring is deliberately owned by one VM runtime. It is only enabled by
18: //! the `realtime-trace` feature used by the OpenRace experiment, so normal
19: //! VM execution does not pay for event storage or timestamp reads.
20: 
21: #[cfg(feature = "realtime-trace")]
22: use alloc::{
23:     collections::{BTreeMap, VecDeque},
24:     vec::Vec,
25: };
26: #[cfg(feature = "realtime-trace")]
27: use core::sync::atomic::{AtomicU64, Ordering};
28: 
29: #[cfg(feature = "realtime-trace")]
30: use ax_kspin::SpinNoIrq as Mutex;
31: 
32: #[cfg(feature = "realtime-trace")]
33: use crate::host::{HostCpu, HostTime, default_host};
34: 
35: /// Maximum number of events retained for one host CPU.
36: #[cfg(feature = "realtime-trace")]
37: pub(crate) const CAPACITY_PER_CPU: usize = 4096;
38: 
39: /// A point in the host software-vIRQ delivery path.
40: #[derive(Clone, Copy, Debug, Eq, PartialEq)]
41: pub(crate) enum VirqTraceKind {
42:     Enqueue,
43:     Notify,
44:     Ipi,
45:     Running,
46:     Drain,
47:     Inject,
48:     QueueOverflow,
49: }
50: 
51: impl VirqTraceKind {
52:     #[cfg(feature = "realtime-trace")]
53:     pub(crate) const fn as_str(self) -> &'static str {
54:         match self {
55:             Self::Enqueue => "enqueue",
56:             Self::Notify => "notify",
57:             Self::Ipi => "ipi",
58:             Self::Running => "running",
59:             Self::Drain => "drain",
60:             Self::Inject => "inject",
61:             Self::QueueOverflow => "queue_overflow",
62:         }
63:     }
64: }
65: 
66: /// One immutable trace record.
67: #[cfg(feature = "realtime-trace")]
68: #[derive(Clone, Copy, Debug, Eq, PartialEq)]
69: pub(crate) struct VirqTraceEvent {
70:     pub(crate) sequence: u64,
71:     pub(crate) timestamp_ns: u64,
72:     pub(crate) cpu_id: usize,
73:     pub(crate) vm_id: usize,
74:     pub(crate) vcpu_id: usize,
75:     pub(crate) vector: u32,
76:     pub(crate) kind: VirqTraceKind,
77: }
78: 
79: /// Per-CPU bounded event rings.
80: #[cfg(feature = "realtime-trace")]
81: pub(crate) struct VirqTraceRing {
82:     sequence: AtomicU64,
83:     rings: Mutex<BTreeMap<usize, VecDeque<VirqTraceEvent>>>,
84: }
85: 
86: #[cfg(feature = "realtime-trace")]
87: impl VirqTraceRing {
88:     pub(crate) const fn new() -> Self {
89:         Self {
90:             sequence: AtomicU64::new(0),
91:             rings: Mutex::new(BTreeMap::new()),
92:         }
93:     }
94: 
95:     pub(crate) fn record(&self, kind: VirqTraceKind, vm_id: usize, vcpu_id: usize, vector: u32) {
96:         self.record_at(
97:             kind,
98:             vm_id,
99:             vcpu_id,
100:             vector,
101:             default_host().this_cpu_id(),
102:             default_host().monotonic_time().as_nanos() as u64,
103:         );
104:     }
105: 
106:     fn record_at(
107:         &self,
108:         kind: VirqTraceKind,
109:         vm_id: usize,
110:         vcpu_id: usize,
111:         vector: u32,
112:         cpu_id: usize,
113:         timestamp_ns: u64,
114:     ) {
115:         let event = VirqTraceEvent {
116:             sequence: self.sequence.fetch_add(1, Ordering::Relaxed),
117:             timestamp_ns,
118:             cpu_id,
119:             vm_id,
120:             vcpu_id,
121:             vector,
122:             kind,
123:         };
124: 
125:         let mut rings = self.rings.lock();
126:         let ring = rings.entry(event.cpu_id).or_default();
127:         if ring.len() == CAPACITY_PER_CPU {
128:             ring.pop_front();
129:         }
130:         ring.push_back(event);
131:     }
132: 
133:     pub(crate) fn snapshot(&self) -> Vec<VirqTraceEvent> {
134:         let rings = self.rings.lock();
135:         let mut events = rings
136:             .values()
137:             .flat_map(|ring| ring.iter().copied())
138:             .collect::<Vec<_>>();
139:         events.sort_by_key(|event| event.sequence);
140:         events
141:     }
142: }
143: 
144: #[cfg(all(test, feature = "host-test", feature = "realtime-trace"))]
145: mod tests {
146:     use super::*;
147: 
148:     #[test]
149:     fn ring_is_bounded_and_snapshot_is_sequence_ordered() {
150:         let ring = VirqTraceRing::new();
151:         for sequence in 0..(CAPACITY_PER_CPU + 3) {
152:             ring.record_at(
153:                 VirqTraceKind::Enqueue,
154:                 1,
155:                 0,
156:                 sequence as u32,
157:                 0,
158:                 sequence as u64,
159:             );
160:         }
161: 
162:         let events = ring.snapshot();
163:         assert_eq!(events.len(), CAPACITY_PER_CPU);
164:         assert!(
165:             events
166:                 .windows(2)
167:                 .all(|window| window[0].sequence < window[1].sequence)
168:         );
169:         assert_eq!(events[0].vector, 3);
170:     }
171: }

(End of file - total 171 lines)
</content>
```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.1s)

_Thinking:_

Now I have a good picture. Let me also quickly check the `drain_and_inject_dispatched_interrupts` context in ops.rs to understand where it's called (before guest entry), and check what changed in the run loop regarding ExternalInterrupt/WFI. Let me check the full function.

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 140,230p virtualization/axvm/src/architecture/ops.rs"
}
```

**Output:**
```
            loop {
                crate::runtime::vcpus::inject_pending_interrupts::<Self>(vm.id(), vcpu_id, vcpu);

                drain_and_inject_dispatched_interrupts::<Self>(vm, vcpu_id, vcpu);

                let exit = vcpu.run()?;
                trace!("{exit:#x?}");
                match Self::handle_vcpu_exit_bound(vm, vcpu, exit)? {
                    BoundVcpuExit::Continue => continue,
                    action => break Ok(action),
                }
            }
        });

        let unbind_result = vcpu.unbind();
        match run_result {
            Ok(BoundVcpuExit::Complete(action)) => {
                unbind_result?;
                Ok(action)
            }
            Ok(BoundVcpuExit::Defer(work)) => {
                unbind_result?;
                Self::finish_deferred_run_work(vm, vcpu, work)
            }
            Ok(BoundVcpuExit::Continue) => unreachable!("continued exits do not leave run loop"),
            Err(err) => {
                if let Err(unbind_err) = unbind_result {
                    warn!(
                        "VM[{vm_id}] VCpu[{vcpu_id}] unbind after run error failed: {unbind_err:?}"
                    );
                }
                Err(err)
            }
        }
    }
}

fn drain_and_inject_dispatched_interrupts<A: ArchOps>(
    vm: &crate::AxVMRef,
    vcpu_id: usize,
    vcpu: &crate::vm::AxVCpuRef<A::VCpu>,
) {
    let runtime = match vm.with_runtime(|runtime| Ok(runtime.clone())) {
        Ok(runtime) => runtime,
        Err(err) => {
            warn!(
                "VM[{}] VCpu[{}] cannot access interrupt dispatcher: {:?}",
                vm.id(),
                vcpu_id,
                err
            );
            return;
        }
    };
    runtime.trace_virq_event(vm.id(), crate::runtime::VirqTraceKind::Running, vcpu_id, 0);
    let interrupts = runtime.irq_dispatcher().drain(vcpu_id);
    if !interrupts.is_empty() {
        runtime.trace_virq_event(
            vm.id(),
            crate::runtime::VirqTraceKind::Drain,
            vcpu_id,
            interrupts[0].id.0,
        );
    }
    for interrupt in interrupts {
        runtime.trace_virq_event(
            vm.id(),
            crate::runtime::VirqTraceKind::Inject,
            vcpu_id,
            interrupt.id.0,
        );
        if let Err(err) = A::inject_vcpu_interrupt(vcpu, interrupt) {
            warn!(
                "VM[{}] VCpu[{}] failed to inject interrupt {interrupt:?}: {err:?}",
                vm.id(),
                vcpu_id
            );
        }
    }
}

#[cfg(test)]
fn inject_drained_interrupts<A: ArchOps>(
    dispatcher: &crate::runtime::VcpuIrqDispatcher,
    vm_id: usize,
    vcpu_id: usize,
    vcpu: &crate::vm::AxVCpuRef<A::VCpu>,
) {
    for interrupt in dispatcher.drain(vcpu_id) {
        if let Err(err) = A::inject_vcpu_interrupt(vcpu, interrupt) {
            warn!("VM[{vm_id}] VCpu[{vcpu_id}] failed to inject interrupt {interrupt:?}: {err:?}");

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 58.6s)

_Thinking:_

Now I have a comprehensive understanding of the document and the code. Let me write the analysis. The user asked in Chinese, so I should respond in Chinese. The document is a status summary of the vIRQ A/B experiment.

Key points to analyze:

1. **Document structure**: Well-organized — fixed conditions, what's complete, what failed, conclusions, next steps, evidence entries. Good.

2. **Core problem identified in the doc**: The vCPU can't reliably return to the AxVM host run loop to complete `drain → inject` after host IPI/pending arrival.

3. **My analysis based on the code**:
   - The A/B experiment compares two software vIRQ injection paths: A direct `inject_interrupt` call vs B queue → notify → IPI → drain → inject.
   - A1 failure: A baseline calls `inject_interrupt` from host CPU 0 while guest vCPU runs on CPU 3 — this is a GIC/vGIC affinity issue. Actually interesting: the A path apparently doesn't pin to the vCPU's physical CPU, so injection happens on the wrong CPU. Wait, looking at the code, `queue_interrupt` was replaced by `dispatch_vcpu_interrupt`. Let me check what `inject_interrupt_to_vcpu` does now... In vcpus.rs, `run_periodic_virq_injector` calls `vm.inject_interrupt_to_vcpu(targets, config.vector)`. Let me check that API.

Actually wait — interesting detail: the A path (baseline) in the doc says "host injector 直接调用 vCPU backend 的 inject_interrupt" — direct call. But in the current code both A and B seem to go through `inject_interrupt_to_vcpu`. Hmm, actually the code in this working tree is the B branch (`openrace/realtime-virq-ab`). The A branch is in `/home/huhu/tgoskits-2026`. The code we see implements the B path: `dispatch_vcpu_interrupt` = enqueue + notify + IPI.

Hmm wait, actually looking more carefully at the current code: `run_periodic_virq_injector` calls `vm.inject_interrupt_to_vcpu(targets, vector)` which goes through `queue_interrupt` → `dispatch_vcpu_interrupt`. That's the B path. The A path would be in the other worktree.

Key observations from the code that are relevant to the analysis:

1. **The trace event `running=1` in A1/B logs**: The `Running` trace event is recorded once per `drain_and_inject_dispatched_interrupts` call — i.e., once per entry into the run loop iteration, not per vIRQ. So `running=1` means the vCPU only entered the run loop's drain point once during the whole experiment. That's a big signal: the vCPU task sleeps on `waits_for_event` and only wakes... Actually with `running=1`, the vCPU entered the guest once. This indicates the vCPU run loop isn't cycling.

2. **In B passthrough (3.3)**: `enqueue=64, notify=64, ipi=64, running=1, drain=0, queue_overflow=236`. The queue fills to 64 and then overflows 236 times. The IPIs were sent but the vCPU never came back to drain. Wait — `drain=0` but `running=1`. Interesting: the Running event is traced at the start of `drain_and_inject_dispatched_interrupts`. So running=1 means the drain point executed once but found... wait if it executed once, drain should record an event only if interrupts non-empty. So the vCPU entered the run loop once, drained an empty queue, and then... never came back? That means the IPI doesn't cause the vCPU to exit or the exit doesn't loop back.

Wait, but the doc says "B 修复后的 passthrough smoke：队列运行但无法 drain/inject" — the vCPU was in the guest and host IPI... The GIC vCPU interface: in passthrough mode, host IPI to the vCPU's pCPU would be a host interrupt, handled by ArceOS. The IPI handler presumably should kick the vCPU run loop. But if the IPI is delivered to the *guest* vCPU as a virtual interrupt (passthrough of GIC), then the guest handles it, not the host. That's exactly the problem: with `interrupt_mode = "passthrough"`, the host IPI lands in the guest's GIC, and the guest (Zephyr) doesn't necessarily do anything to exit the vCPU. The vCPU never exits, so the host never drains.

Actually, hmm. In passthrough mode, IPIs sent by the host to the pCPU may be handled directly by the vCPU hardware routing (GIC vCPU interface), so the guest sees the IPI as a virtual interrupt. The host doesn't get an exit. Hence no drain.

3. **In B emulated (3.4)**: `running=31, drain=1, inject=1` — only ONE drain/inject out of 65 enqueues, and guest still received 0. The emulated GIC path injects via `inject_vcpu_interrupt` (LR setup presumably), but guest doesn't see vector 48. Doc's TODO #2 covers this: GIC LR Group setting, Zephyr IRQ 48 GIC config, vGIC enable state.

4. **A1 problem analysis**: A baseline injects directly from host CPU 0 while vCPU runs on CPU 3. For a GIC-v2/v3 system, the vCPU interface LR access must happen on the pCPU where the vCPU is bound (actually in QEMU with GICv3, the vGIC state is per-vCPU). The doc says "GIC/vCPU 注入发生在错误的 host CPU 上". The fix for A would be to either pin the injector to the vCPU's pCPU or route through an IPI — but that's exactly what B does. Hmm, interesting: so A and B actually converge: A's direct call is fundamentally broken by affinity, and the doc acknowledges A cannot be a baseline.

Wait, actually — one question about A: does the A worktree have the same trace event names? Yes, the doc says A1 produced `enqueue/notify/ipi` events too... wait no. A1 log shows `enqueue=300 notify=300 ipi=300 running=1 drain=0 inject=0`. Interesting! The A baseline (in `/home/huhu/tgoskits-2026`) also shows enqueue/notify/ipi events. So actually "A" also goes through the queue→notify→IPI path at the trace level? Hmm, wait. That's odd. The doc says A is "host injector 直接调用 vCPU backend 的 inject_interrupt".

Hmm, but A1 log has enqueue=300 notify=300 ipi=300. So the A baseline's injector also enqueues/notifies/IPIs? That contradicts the doc's description of A. Unless... the A worktree shares the same trace infrastructure and the injector in A also calls `inject_interrupt_to_vcpu` which... hmm. Actually maybe in A the `inject_interrupt_to_vcpu` API directly calls `inject_vcpu_interrupt` on the backend — but then where do enqueue/notify/ipi events come from?

Wait, let me re-read the doc:

"A（基线）：host injector 直接调用 vCPU backend 的 inject_interrupt。"

But A1's log shows `enqueue=300 notify=300 ipi=300`. Hmm, this is a discrepancy worth flagging. Possibilities:
1. The A worktree also uses dispatch_vcpu_interrupt (queue path), meaning A and B are actually the same path now — making the A/B comparison invalid (or rather, A isn't the "direct call" baseline anymore).
2. Or the trace events in the A worktree come from somewhere else.

Actually, this is an important analytical point! The document says the A baseline is "direct inject_interrupt" but the A1 evidence log shows enqueue/notify/ipi all = 300, which are the B-path trace events. Either:
- The A worktree's `inject_interrupt_to_vcpu` still routes through the queue path (and "direct call" description is outdated), or
- The traces were recorded with different event names.

Hmm, but the doc says A1's failure: "基线从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt。因此 GIC/vCPU 注入发生在错误的 host CPU 上". If A went through the queue path, it would have sent an IPI to CPU 3 (the vCPU's pCPU) and injection would happen on CPU 3. So the A path does NOT go through queue/IPI; yet the log shows enqueue/notify/ipi=300.

Let me verify by looking at the A worktree code. /home/huhu/tgoskits-2026. Let me check.

Also, another observation: in the current B code, the injector logs `VIRQ_INJECT` lines with status, and `inject_interrupt_to_vcpu`. Let me check what `inject_interrupt_to_vcpu` does and how the guest receives. And importantly — the doc's claim about `running=1`: the `Running` event is recorded at the start of `drain_and_inject_dispatched_interrupts`, which is called once per run-loop iteration before `vcpu.run()`. So `running=1` means the run loop executed only one iteration... Hmm, that means after the first `vcpu.run()` the exit handler returned something that broke out of the loop? Or the vCPU task slept at the very start.

Actually wait, for passthrough: `running=1` but `drain=0`. So at the one time the run loop hit the drain point, the queue... hmm, but enqueue=64 happened before the first entry? The trace order isn't preserved in counts. Actually the sequence matters: 64 enqueues happened before the vCPU even entered the run loop (vCPU task startup delay), then the run loop entered once (running=1), drained the queue... but drain=0?!

Hmm, that's contradictory. If the queue had 64 items and the run loop executed `drain_and_inject_dispatched_interrupts` once, drain would be ≥1 and the queue would be emptied (64 injections). Unless... the drain happened BEFORE the enqueues. Timeline: the vCPU task boots, enters run loop once (running=1, drain=0 — queue empty at that point), then enters the guest, and never exits again. Meanwhile the injector starts enqueueing at 10ms; the vCPU is already in the guest. IPIs sent to the vCPU's pCPU — in passthrough mode the guest handles the IPI (or the host does but doesn't kick the vCPU). The vCPU never returns to host, so no drain ever happens. Queue fills to 64, 236 overflows.

So the sequence interpretation: `running=1` happened early (before enqueues), and then the vCPU was stuck in the guest. This matches "host IPI 没有可靠地让 guest vCPU 返回 AxVM host-side drain/inject 路径".

For emulated: running=31 — the vCPU exited the guest 31 times (probably due to WFI or timer or emulated IRQ), but only 1 drain with non-empty queue. So most drain checks found the queue empty — because... hmm, enqueue=65, drain=1, inject=1. So 64 of the 65 enqueues happened when the vCPU was in guest (not draining), and only once did the queue have content when the vCPU came out. But why did the vCPU exit 31 times if only 1 inject? Because WFI exits maybe — Zephyr idle loop executes WFI, which causes guest exit. Each WFI → exit → drain check → empty → wait? Actually if it waits for the next event... The 31 `running` events are WFI wakeups (timer interrupts in the guest?), and the drains were empty because queue events were... hmm, contradictory again: the vCPU exits on WFI, checks drain — if the queue is empty, it goes back to wait. When the injector enqueues, notify wakes the vCPU wait queue, vCPU runs, drains → that should give drain=65...

Unless the notify/ipi doesn't actually wake the vCPU task, and the only drain happened by luck when the vCPU happened to exit at the right moment. The 31 running vs 1 drain suggests: the vCPU woke 30 times from... something (maybe the startup notifications or timer) while the queue was empty, and the drain-with-data happened once — after which... hmm, guest still got 0. So the inject itself (emulated GIC LR write) didn't reach the guest: the guest ISR for vector 48 never fired. 

Also interesting: after the 1 inject, the queue had 64 more pending, but the vCPU never drained again → the guest likely never exited again (no WFI because...). Hmm, actually this suggests the emulated injection caused the guest to enter an IRQ handling... no wait, guest received 0.

Whatever the exact mechanics, the key issue the doc identifies is correct: the vCPU isn't reliably returning to the host run loop to drain. My analysis confirms this from the code: 

- The drain point is in the run-loop before `vcpu.run()`, reached only when the vCPU task is woken. 
- Wake sources: `notify_vcpu` (per-vCPU wait queue) + `send_ipi`. The IPI handler in the host (ArceOS) — what does it do? Let me check `send_ipi` and the IPI handler. In ArceOS, `send_ipi` probably just sends the IPI; there must be an IPI handler that... Let me look at how the IPI wakes the vCPU task.

Also a crucial detail: in the passthrough case, if the GIC is passthrough, host IPIs are directly routed to the guest — the host never sees them. So the design needs the IPI to be handled by the *host* (i.e., the host needs to register the IPI as a host interrupt and kick the vCPU). In passthrough mode this is the fundamental problem: you cannot inject a host-side IPI without it being routed to the guest vCPU interface... unless you use a dedicated host interrupt not routed to the guest.

Hmm wait, actually let me reconsider. In AxVisor/ArceOS, "passthrough" interrupt mode for GIC means the vGIC injects physical IRQs directly to the guest. Host IPIs are sent between physical CPUs. If the vCPU is running on pCPU 3 in guest mode, an IPI sent to pCPU 3... the IPI comes as a physical IRQ to pCPU 3. If pCPU 3 is running the guest, the physical IRQ traps the guest (host takes the exit), handles the IPI in the host, and then needs to decide whether to re-enter the guest or drain first. This is where the exit handler matters: on ExternalInterrupt exit, does the run loop break out to let the vCPU task drain?

Looking at the run loop: `handle_vcpu_exit_bound` — if it returns `Continue`, the loop re-enters the guest directly WITHOUT re-running the drain point! Look at the loop:

```rust
loop {
    inject_pending_interrupts(...);
    drain_and_inject_dispatched_interrupts(...);
    let exit = vcpu.run()?;
    match Self::handle_vcpu_exit_bound(vm, vcpu, exit)? {
        BoundVcpuExit::Continue => continue,   // <-- skips drain!
        action => break Ok(action),
    }
}
```

`continue` goes back to the top of the loop, which re-runs the drain point. OK fine, `continue` does go back to the top. So the drain happens before each re-entry. Good.

So for the drain to happen, the vCPU task must exit the guest (return from `vcpu.run()`). On a physical IPI to the pCPU running the guest, hardware traps → `vcpu.run()` returns with ExternalInterrupt exit → loop re-runs drain → drains → re-enters guest. This should work in theory — IF the IPI arrives while the vCPU is running in guest mode.

The `running=1` in passthrough suggests the vCPU task never even got to run the loop again... OR the IPI was delivered as a virtual interrupt to the guest (passthrough mode routes GIC IRQs to guest directly, host never sees them → no exit → vCPU stays in guest). That's the crux: in passthrough mode the physical IPI sent by the injector on CPU 0 to CPU 3 is delivered via the GIC distributor directly to the vCPU interface of the running guest — the host doesn't take the exit because in passthrough mode... hmm, actually no. A physical IPI always traps the host if the guest is running with vGIC... 

OK here's the thing: with hardware virtualization (KVM-style vGIC passthrough), physical IRQs targeting the pCPU while a guest runs are always delivered to the host first (the GIC routing goes through the CPU's IRQ state which is trapped). The host must explicitly configure the IRQ as a "virtual" IRQ (via GIC VIRTUAL_IRQ registers, vPEND/vACTIVE) for it to bypass the host. In passthrough mode, AxVM likely sets up vIRQs for the guest; a host IPI sent via `send_ipi` (physical IPI, e.g., SGI via GICD) would trap the host. Then the host IPI handler runs — in ArceOS that's `platform_irq`/`scheduler` related — and would handle the IPI. Whether the vCPU task gets re-scheduled/woken is the question. But wait — if the vCPU task is the *currently running* task on that CPU (it's running the guest in its context), the IPI handler interrupts it, returns, and the vCPU run loop resumes... but the drain point was already passed. The loop resumes inside/after `vcpu.run()` returned with ExternalInterrupt → handle exit → Continue → top of loop → drain. So it should work?!

Unless the IPI handler causes the scheduler to switch away or the vCPU task is not the running task (e.g., the vCPU is in `waits_for_event` sleep because... hmm).

OK, I shouldn't over-speculate about QEMU internals. Let me look at the log evidence files if they exist, and check `handle_vcpu_exit_bound` and how ExternalInterrupt exits are handled, and `send_ipi` semantics. That's the "TODO #3" in the doc: "检查 ArmVcpu::run() 对 ExternalInterrupt/WFI exit 的处理".

Actually, wait. `running=1` — where is `Running` traced? At the start of `drain_and_inject_dispatched_interrupts`, once per loop iteration. For passthrough with `running=1`, the loop ran once. If the IPI trapped the host and the loop continued, running would be ≥2. So the IPI did NOT trap the host (or the task wasn't in the loop). Conclusion: in passthrough mode, the physical IPI to the pCPU running the guest is being routed to the guest (Zephyr) directly via the vGIC passthrough, without a host exit. This is exactly what "passthrough" means for the GIC: physical IRQs that the host routes to the guest via GIC passthrough are seen by the guest as virtual interrupts; the host is NOT interrupted. So the host-side kick never happens. The design intent of B was that the host IPI should be a host-visible interrupt that kicks the vCPU out of the guest... but in passthrough mode, `send_ipi` with the guest's vCPU as target ends up in the guest.

Hmm, but wait — that depends on the IPI number and GIC configuration. If AxVM configures the IPI (SGI) as routed-to-guest, then yes. This is a design decision in the B code. The doc's fix suggestion: "host IPI 是否触发 guest exit" (TODO #2) — right.

For emulated: running=31 means the loop ran 31 times — so exits DID happen (WFI, and possibly the emulated vIRQ injection caused... no). With emulated GIC, the host IPI would trap the host normally (host handles it), and the vCPU run loop would... but the vCPU task is sleeping in wait, not in the run loop! The IPI handler on pCPU 3 — the vCPU task might be in the guest... no wait, in emulated mode with the guest running, the vCPU task IS in `vcpu.run()` on pCPU 3. IPI traps → host handler → returns → loop → drain. Hmm.

But the emulated evidence shows the vCPU only woke 31 times total with only 1 successful drain. The 30 exits found the queue EMPTY — so the enqueues (65) mostly happened when the vCPU was NOT exiting... that means the vCPU was mostly NOT running in guest mode during the injector period, OR the wakeup path is broken and the vCPU only wakes on unrelated events.

Hmm, wait, running=31 vs enqueue=65: the injector runs on CPU 0 at 10ms period over 300 samples (3 seconds). The vCPU ran 31 loop iterations during that time. If the queue→notify→IPI wake path worked, each enqueue would wake the vCPU task (per-vCPU wait queue) → loop → drain. Only 1 drain happened. So the per-vCPU wait queue wake didn't work reliably — only 1 wake coincided with queued data.

But hold on — if notify doesn't wake the vCPU task and the IPI doesn't cause an exit, why did running=31 at all? Because the vCPU was mostly in `waits_for_event` — Zephyr idle WFI. In emulated mode... WFI in guest → exit → the run loop sees WFI... how is WFI handled? Maybe `handle_vcpu_exit_bound` returns WaitForEvent → the loop breaks and the vCPU task sleeps in the wait queue. Then the 31 runs are: startup + WFI wakes. WFI wakes happen when a virtual IRQ is pending in the vGIC (Zephyr timer?). So the guest was getting timer interrupts (vGIC), each timer IRQ → exit WFI → re-enter... hmm, that would produce many running counts.

OK. I've done enough speculative analysis of QEMU internals. The key deliverable: analyze the document. The most valuable analysis points:

1. **The doc's core conclusion is sound**: no valid A/B latency samples exist; the blocker is the vCPU not returning to host-side drain/inject. Agreed, and the code confirms: `drain_and_inject_dispatched_interrupts` only runs before each guest entry; the wake path (notify/IPI) is the bottleneck.

2. **Discrepancy worth flagging**: The doc says A = "direct inject_interrupt", but A1's log shows `enqueue=300 notify=300 ipi=300` — those are B-path (queue) trace events. Either the A worktree also routes through the queue path (making A and B the same path, invalidating the comparison), or the trace event names in the A worktree are misleading. This needs verification in `/home/huhu/tgoskits-2026`. If A truly used the queue path, then A1's failure isn't "wrong CPU direct call" but something else. Actually wait — if A used the queue path with `send_ipi(cpu_id)` targeting the vCPU's pCPU (CPU 3), and the injection happened on... hmm, the IPI trace was recorded on CPU 0 (injector), the injection would happen on CPU 3's run loop. But drain=0, inject=0 — the vCPU never drained. So A1 would fail the same way as B passthrough. That's consistent with the trace counts!

   Hmm, actually — what's "A" in the 2026 worktree? Let me check. That would resolve this. Let me look at /home/huhu/tgoskits-2026 to verify.

3. **The `running` trace event semantics**: it's recorded once per run-loop iteration, not per vIRQ — the doc's counts (running=1, running=31) are interpretable as loop iterations. The doc interprets `running=1` as "guest vCPU 绑定在 host CPU 3" — actually the doc doesn't misinterpret; it says the count means the vCPU entered the run loop once.

4. **Statistical claim issues**: `p99=0` meaning "no samples" — the script returns 0 for empty; fine but doc warns. Also `lost_irq=300` in A1: `lost_irq = injections - matched` where injections counts VIRQ_INJECT lines (300, status ok), matched=0. OK.

5. **Missing from the doc / suggested next steps**:
   - No data on whether the per-vCPU wait queue notify actually wakes the task (there's a subtle race: `notify_vcpu` checks `has_pending` then notifies; the vCPU could drain between check and notify — benign. But a bigger issue: the vCPU task sleeps in `wait_vcpu_until` with condition `has_pending || !running...` — the wait_until condition is checked on notify; but is the notify issued BEFORE the task sleeps (lost wakeup) possible? The queue push happens first, then notify, then IPI. Task: checks condition (empty) → sleeps. Race: enqueue+notify between check and sleep → lost wakeup → the task sleeps forever with a pending interrupt. Does `WaitQueue::wait_until` recheck? Standard axsync WaitQueue does check condition on wake; the lost-wakeup problem is if notify happens between condition-check and sleep registration. axsync WaitQueue handles this by checking under lock? Not sure. The doc mentions they fixed "enqueue 后 vCPU 入睡造成的 pending 丢唤醒" — so this is addressed via the condition including `has_pending_vcpu_interrupt`. But the emulated result (1/65 drains) suggests the lost-wakeup or wake-not-received issue persists.
   - The IPI handler side: does ArceOS IPI handling wake the vCPU task? The doc's TODO #3 covers this.
   - GIC LR Group settings (TODO #2): for Zephyr, IRQ 48 must be enabled in the guest's GICD/GICC; if the vIRQ is configured as Group 1 but Zephyr (EL1) uses Group 1 NS, need checking.
   - Also: `enqueue` capacity 64 with 10ms period — 300 samples over 3s: if vCPU drain is broken, queue overflows at 64 quickly (first 640ms). That matches queue_overflow=236 = 300-64.

6. **The doc could be improved**: A/B comparability question — A1 and B have different VM configurations (A in tgoskits-2026, B here), and importantly: was `interrupt_mode` the same for A1? A1 didn't produce a single valid injection, so the "fixed conditions" table is aspirational rather than achieved. Also, the doc says experiment order A1→B1→A2→B2 but A2/B2 never ran — fine, doc acknowledges.

7. **Suggestions for next steps** (concrete, code-level):
   - Verify the A worktree actually uses a direct call (check `/home/huhu/tgoskits-2026` code) to confirm A1's failure mode and whether A/B are actually comparable.
   - The `Running` event should record which vCPU — it does. 
   - Add trace events for the exit-handler side (e.g., `ExternalInterrupt`, `Wfi`, `IpiReceived` in the IPI handler) to distinguish "IPI not trapping host" vs "IPI traps but task not woken" vs "task woken but queue empty". This would disambiguate the passthrough vs emulated failures quickly. Actually — a key missing trace: the IPI arrival at the target CPU. Currently `ipi` is traced at the sender. Adding a receiver-side event would prove/disprove delivery.
   - In the emulated smoke, 31 running vs 1 drain: add a trace at the wait-queue wake to see how many times notify actually woke the task.

Let me verify the A worktree code to settle point 2. Let me check /home/huhu/tgoskits-2026.

**Tool: bash**

**Input:**
```json
{
  "command": "ls /home/huhu/tgoskits-2026/ 2>/dev/null | head; grep -rn \"inject_interrupt_to_vcpu\\|dispatch_vcpu_interrupt\\|queue_interrupt\" /home/huhu/tgoskits-2026/virtualization/axvm/src/vm/mod.rs 2>/dev/null | head -20"
}
```

**Output:**
```
AGENTS.md
apps
book
bootloader
Cargo.lock
Cargo.toml
CLAUDE.md
components
container
docs
201:pub(crate) fn dispatch_vcpu_interrupt_with(
293:    pub(crate) fn queue_interrupt(&self, vcpu_id: usize, vector: usize) -> AxVmResult<usize> {
346:    pub(crate) fn dispatch_vcpu_interrupt(
351:        dispatch_vcpu_interrupt_with(
1379:    pub fn inject_interrupt_to_vcpu(
1386:                crate::runtime::vcpus::queue_interrupt(self.id(), vcpu.id(), irq)?;
1829:        dispatch_vcpu_interrupt_with(
1856:        dispatch_vcpu_interrupt_with(
1873:        let result = dispatch_vcpu_interrupt_with(

```

**Tool: bash**

**Input:**
```json
{
  "command": "grep -rn \"inject_interrupt_to_vcpu\" /home/huhu/tgoskits/virtualization/axvm/src/ --include=*.rs | head; grep -rn \"fn send_ipi\" /home/huhu/tgoskits/os/axvisor/src/platform_irq.rs /home/huhu/tgoskits/virtualization/axvm/src/host/ -r 2>/dev/null | head"
}
```

**Output:**
```
/home/huhu/tgoskits/virtualization/axvm/src/vm/mod.rs:1457:    pub fn inject_interrupt_to_vcpu(
/home/huhu/tgoskits/virtualization/axvm/src/arch/aarch64/ipi.rs:49:        && let Err(err) = vm.inject_interrupt_to_vcpu(remote_targets, exit.vector as _)
/home/huhu/tgoskits/virtualization/axvm/src/runtime/vcpus.rs:96:        let result = vm.inject_interrupt_to_vcpu(targets, config.vector);
/home/huhu/tgoskits/virtualization/axvm/src/host/task.rs:45:pub(crate) fn send_ipi(cpu_id: usize) {
/home/huhu/tgoskits/virtualization/axvm/src/host/arceos.rs:147:pub(crate) fn send_ipi(cpu_id: usize) {
/home/huhu/tgoskits/virtualization/axvm/src/host/arceos.rs:167:fn send_ipi_to_all_except_current(cpu_num: usize) {

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.6s)

_Thinking:_

Let me check the A worktree's `inject_interrupt_to_vcpu` and `queue_interrupt` to see whether A1's log could come from a queue path. Also check `arceos.rs` send_ipi and the arch/aarch64/ipi.rs (that's the guest IPI handler injecting into the VM!).

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 1370,1400p /home/huhu/tgoskits-2026/virtualization/axvm/src/vm/mod.rs; echo ===; sed -n 285,320p /home/huhu/tgoskits-2026/virtualization/axvm/src/vm/mod.rs"
}
```

**Output:**
```
                        region.size(),
                        region.is_identical()
                    );
                }
            }
        }
    }

    /// Injects an interrupt to the vCPU.
    pub fn inject_interrupt_to_vcpu(
        &self,
        targets: CpuMask<TEMP_MAX_VCPU_NUM>,
        irq: usize,
    ) -> AxVmResult {
        for vcpu in self.vcpu_list() {
            if targets.get(vcpu.id()) {
                crate::runtime::vcpus::queue_interrupt(self.id(), vcpu.id(), irq)?;
            }
        }
        Ok(())
    }

    /// Emit the bounded software-vIRQ trace collected by the active runtime.
    ///
    /// This is an experiment-only observability hook. It does not alter
    /// interrupt delivery and is available only when `realtime-trace` is
    /// enabled in the AxVM crate.
    #[cfg(feature = "realtime-trace")]
    pub fn dump_realtime_trace(&self) -> AxVmResult {
        self.with_runtime(|runtime| {
            runtime.log_virq_trace();
===

    pub(crate) fn cpu_on_start_ack(
        &self,
        vcpu_id: usize,
    ) -> Option<Arc<crate::runtime::vcpus::CpuOnStartAck>> {
        self.cpu_on_start_acks.lock().get(&vcpu_id).cloned()
    }

    pub(crate) fn queue_interrupt(&self, vcpu_id: usize, vector: usize) -> AxVmResult<usize> {
        let task = self
            .vcpu_task_list
            .lock()
            .get(&vcpu_id)
            .cloned()
            .ok_or_else(|| ax_err_type!(NotFound, format!("vCPU {vcpu_id} task not found")))?;
        self.pending_interrupts
            .lock()
            .entry(vcpu_id)
            .or_default()
            .push(PendingInterrupt::Normal(vector));
        Ok(task.cpu_id() as usize)
    }

    #[expect(
        dead_code,
        reason = "only the LoongArch IRQ backend queues physical interrupts"
    )]
    pub(crate) fn queue_external_interrupt(
        &self,
        vcpu_id: usize,
        vector: usize,
        physical_irq: usize,
    ) -> AxVmResult<usize> {
        let task = self
            .vcpu_task_list
            .lock()

```

**Tool: read**

**Input:**
```json
{
  "filePath": "/home/huhu/tgoskits/virtualization/axvm/src/arch/aarch64/ipi.rs"
}
```

**Output:**
```
<path>/home/huhu/tgoskits/virtualization/axvm/src/arch/aarch64/ipi.rs</path>
<type>file</type>
<content>
1: //! AArch64 guest IPI target resolution and injection.
2: 
3: use super::Aarch64DeferredRunWork;
4: use crate::{AxVmResult, architecture::BoundVcpuExit};
5: 
6: #[derive(Clone, Copy, Debug)]
7: pub(crate) struct SendIpiExit {
8:     pub(crate) target_cpu: u64,
9:     pub(crate) target_cpu_aux: u64,
10:     pub(crate) send_to_all: bool,
11:     pub(crate) send_to_self: bool,
12:     pub(crate) vector: u64,
13: }
14: 
15: pub(crate) fn handle(
16:     vm: &crate::AxVMRef,
17:     vcpu_id: usize,
18:     exit: SendIpiExit,
19: ) -> AxVmResult<BoundVcpuExit<Aarch64DeferredRunWork>> {
20:     let vm_id = vm.id();
21:     debug!(
22:         "VM[{vm_id}] run VCpu[{vcpu_id}] SendIPI, target_cpu={:#x}, target_cpu_aux={:#x}, \
23:          vector={}",
24:         exit.target_cpu, exit.target_cpu_aux, exit.vector
25:     );
26:     let targets = ipi_targets(vm, vcpu_id, exit);
27:     if targets.is_empty() {
28:         warn!(
29:             "VM[{vm_id}] SendIPI has no target: target_cpu={:#x}, target_cpu_aux={:#x}",
30:             exit.target_cpu, exit.target_cpu_aux
31:         );
32:         return Ok(BoundVcpuExit::Complete(
33:             crate::architecture::VcpuRunAction {
34:                 waits_for_event: false,
35:                 stop_reason: None,
36:                 resets_vm: false,
37:                 exits_vcpu: false,
38:             },
39:         ));
40:     }
41: 
42:     if targets.get(vcpu_id) {
43:         crate::inject_current_vcpu_interrupt(exit.vector as _)
44:             .expect("failed to inject self IPI into current vCPU");
45:     }
46:     let mut remote_targets = targets;
47:     remote_targets.set(vcpu_id, false);
48:     if !remote_targets.is_empty()
49:         && let Err(err) = vm.inject_interrupt_to_vcpu(remote_targets, exit.vector as _)
50:     {
51:         warn!(
52:             "Failed to inject interrupt {} to VM[{vm_id}] targets {remote_targets:?}: {err:?}",
53:             exit.vector
54:         );
55:     }
56:     Ok(BoundVcpuExit::Complete(
57:         crate::architecture::VcpuRunAction {
58:             waits_for_event: false,
59:             stop_reason: None,
60:             resets_vm: false,
61:             exits_vcpu: false,
62:         },
63:     ))
64: }
65: 
66: fn ipi_targets(
67:     vm: &crate::AxVMRef,
68:     current_vcpu_id: usize,
69:     exit: SendIpiExit,
70: ) -> crate::CpuMask<64> {
71:     let mut targets = crate::CpuMask::new();
72:     if exit.send_to_all {
73:         for vcpu in vm.vcpu_list() {
74:             if vcpu.id() != current_vcpu_id {
75:                 targets.set(vcpu.id(), true);
76:             }
77:         }
78:     } else if exit.send_to_self {
79:         targets.set(current_vcpu_id, true);
80:     } else {
81:         for (vcpu_id, _, phys_id) in vm.get_vcpu_affinities_pcpu_ids() {
82:             let affinity = phys_id as u64;
83:             let aff0 = affinity & 0xff;
84:             let aff123 = affinity & !0xff;
85:             if aff123 == exit.target_cpu && aff0 < 16 && (exit.target_cpu_aux & (1u64 << aff0)) != 0
86:             {
87:                 targets.set(vcpu_id, true);
88:             }
89:         }
90:     }
91:     targets
92: }

(End of file - total 92 lines)
</content>
```

---

## Assistant (Build · DeepSeek V4 Flash Free · 9.8s)

_Thinking:_

Now I have a clear picture. Key finding for the A worktree:

In the A worktree (tgoskits-2026):
- `inject_interrupt_to_vcpu` → `queue_interrupt` → pushes to `pending_interrupts` + `notify_all` + `send_ipi(task.cpu_id())`. Wait, let me check the old `queue_interrupt` in vcpus.rs (A worktree version). In the B worktree the diff showed the old code was:

```rust
pub(crate) fn queue_interrupt(vm_id: usize, vcpu_id: usize, vector: usize) -> AxVmResult {
    ...
    let cpu_id = vm.with_runtime(|runtime| runtime.queue_interrupt(vcpu_id, vector))?;
    vm.with_runtime(|runtime| {
        runtime.notify_all();
        Ok(())
    })?;
    crate::host::task::send_ipi(cpu_id);
    Ok(())
}
```

So A's path is: push pending → notify_all → send_ipi(cpu_id from task.cpu_id()). Hmm, so A also does queue + notify + IPI?! Then what is "A = direct inject_interrupt" per the doc?

Wait — the A worktree `queue_interrupt` at line 293 pushes to `pending_interrupts` and returns `task.cpu_id()`. So A's injector calls queue_interrupt → pending_interrupts push → notify_all → send_ipi. That's not "direct inject_interrupt call" either...

Hmm wait, but the doc says A1 log shows `enqueue=300 notify=300 ipi=300 running=1 drain=0 inject=0`. But the A worktree path is `pending_interrupts` (PendingInterrupt::Normal), not the dispatcher queue. Where would "enqueue/notify/ipi" trace events come from in the A worktree?

Let me check the A worktree's trace infra. Actually wait — maybe the doc mislabeled things. Let me check what trace events A1's log had. The doc says A1 log lines: `enqueue=300 notify=300 ipi=300`. Hmm, if A worktree also has realtime-trace with enqueue/notify/ipi events from `dispatch_vcpu_interrupt`...

Let me actually check the A worktree's runtime/queue.rs and trace. Actually — wait. Let me re-read: maybe in the A worktree, the *injector* calls... let me check how A's `inject_interrupt_to_vcpu` flows. It calls `crate::runtime::vcpus::queue_interrupt` which does pending_interrupts push + notify_all + send_ipi. The send_ipi targets `task.cpu_id()`.

Hmm OK here's a thought: in the A worktree, does `send_ipi` exist and what does the host IPI handler do? In ArceOS, the IPI handler might do scheduler kick. And `notify_all` wakes the vCPU task. Then `inject_pending_interrupts` (inject_pending_interrupts::<Self>) injects from `pending_interrupts` — the old injection mechanism, which presumably works. Hmm, but then A1 should have worked (that's the old baseline path that presumably worked before).

Actually wait — the A1 failure: "guest vCPU 绑定在 host CPU 3，但基线从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt。因此 GIC/vCPU 注入发生在错误的 host CPU 上". So the doc claims A calls inject_interrupt directly. But the code I see in the A worktree at line 293/1379 shows queue-based... 

Hold on. Maybe the A worktree has been modified for the experiment too. Let me look at the actual A worktree vcpus.rs `queue_interrupt` and whether there's a direct `inject_interrupt_to_vcpu` variant that calls backend directly. Line 1379 `inject_interrupt_to_vcpu` → `queue_interrupt`. Hmm.

Wait, maybe I should check the A worktree git log/diff to see what's modified vs base. Let me check the trace events in the A worktree — what strings does it emit? Let me grep for "VIRQ_TRACE" in both worktrees.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -rn \"VIRQ_TRACE\\|queue_overflow\\|VirqTraceKind\" /home/huhu/tgoskits-2026/virtualization/axvm/src/ | head -15; echo ===; grep -rn \"fn queue_interrupt\" /home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs"
}
```

**Output:**
```
/home/huhu/tgoskits-2026/virtualization/axvm/src/vm/mod.rs:367:        kind: crate::runtime::VirqTraceKind,
/home/huhu/tgoskits-2026/virtualization/axvm/src/vm/mod.rs:394:                "VIRQ_TRACE seq={} ts_ns={} cpu={} vm={} vcpu={} event={} vector={}",
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:206:            crate::runtime::VirqTraceKind::Enqueue,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:216:            crate::runtime::VirqTraceKind::Notify,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:226:            crate::runtime::VirqTraceKind::Ipi,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:274:        runtime.trace_virq_event(vm_id, crate::runtime::VirqTraceKind::Running, vcpu_id, 0);
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:279:                crate::runtime::VirqTraceKind::Drain,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs:293:            crate::runtime::VirqTraceKind::Inject,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:41:pub(crate) enum VirqTraceKind {
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:50:impl VirqTraceKind {
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:74:    pub(crate) kind: VirqTraceKind,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:93:    pub(crate) fn record(&self, kind: VirqTraceKind, vm_id: usize, vcpu_id: usize, vector: u32) {
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:106:        kind: VirqTraceKind,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/trace.rs:151:                VirqTraceKind::Enqueue,
/home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/mod.rs:29:pub(crate) use trace::VirqTraceKind;
===
192:pub(crate) fn queue_interrupt(vm_id: usize, vcpu_id: usize, vector: usize) -> AxVmResult {

```

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 95,130p /home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs"
}
```

**Output:**
```
        let result = vm.inject_interrupt_to_vcpu(targets, config.vector);
        let completed_ns = crate::host::default_host().monotonic_time().as_nanos() as u64;
        if result.is_err() {
            failed += 1;
        }
        info!(
            "VIRQ_INJECT sequence={} vm={} vcpu={} vector={} requested_ns={} completed_ns={} \
             status={}",
            sequence,
            vm.id(),
            config.vcpu_id,
            config.vector,
            requested_ns,
            completed_ns,
            if result.is_ok() { "ok" } else { "error" },
        );
        deadline += config.period;
        if !vm.running() {
            break;
        }
    }
    info!(
        "VIRQ_INJECT_COMPLETE vm={} vcpu={} vector={} samples={} errors={}",
        vm.id(),
        config.vcpu_id,
        config.vector,
        config.samples,
        failed,
    );
    #[cfg(feature = "realtime-trace")]
    vm.dump_realtime_trace().unwrap_or_else(|err| {
        warn!(
            "VM[{}] failed to dump realtime vIRQ trace: {err:?}",
            vm.id()
        );
    });

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.8s)

_Thinking:_

Let me look at the A worktree's queue_interrupt (line 192) and the surrounding dispatch to see if it uses dispatch_vcpu_interrupt_with with trace events, and whether the direct inject_interrupt path exists.

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 185,310p /home/huhu/tgoskits-2026/virtualization/axvm/src/runtime/vcpus.rs"
}
```

**Output:**
```
        let _ = vm.with_runtime(|runtime| {
            runtime.notify_all();
            Ok(())
        });
    }
}

pub(crate) fn queue_interrupt(vm_id: usize, vcpu_id: usize, vector: usize) -> AxVmResult {
    let vm = crate::get_vm_by_id(vm_id)
        .ok_or_else(|| ax_err_type!(NotFound, format!("VM[{vm_id}] not found")))?;
    if !matches!(vm.status(), VmStatus::Running | VmStatus::Paused) {
        return Err(ax_err_type!(
            BadState,
            format!("VM[{vm_id}] is not accepting interrupts")
        ));
    }

    let cpu_id = vm.with_runtime(|runtime| {
        let cpu_id = runtime.queue_interrupt(vcpu_id, vector)?;
        runtime.trace_virq_event(
            vm_id,
            crate::runtime::VirqTraceKind::Enqueue,
            vcpu_id,
            vector as u32,
        );
        Ok(cpu_id)
    })?;
    vm.with_runtime(|runtime| {
        runtime.notify_all();
        runtime.trace_virq_event(
            vm_id,
            crate::runtime::VirqTraceKind::Notify,
            vcpu_id,
            vector as u32,
        );
        Ok(())
    })?;
    crate::host::task::send_ipi(cpu_id);
    let _ = vm.with_runtime(|runtime| {
        runtime.trace_virq_event(
            vm_id,
            crate::runtime::VirqTraceKind::Ipi,
            vcpu_id,
            vector as u32,
        );
        Ok(())
    });
    Ok(())
}

#[expect(
    dead_code,
    reason = "only the LoongArch IRQ backend queues physical interrupts"
)]
pub(crate) fn queue_external_interrupt(
    vm_id: usize,
    vcpu_id: usize,
    vector: usize,
    physical_irq: usize,
) -> AxVmResult {
    let vm = crate::get_vm_by_id(vm_id)
        .ok_or_else(|| ax_err_type!(NotFound, format!("VM[{vm_id}] not found")))?;
    if !matches!(vm.status(), VmStatus::Running | VmStatus::Paused) {
        return Err(ax_err_type!(
            BadState,
            format!("VM[{vm_id}] is not accepting interrupts")
        ));
    }

    let cpu_id =
        vm.with_runtime(|runtime| runtime.queue_external_interrupt(vcpu_id, vector, physical_irq))?;
    vm.with_runtime(|runtime| {
        runtime.notify_all();
        Ok(())
    })?;
    crate::host::task::send_ipi(cpu_id);
    Ok(())
}

pub(crate) fn inject_pending_interrupts<A: ArchOps>(
    vm_id: usize,
    vcpu_id: usize,
    vcpu: &crate::vm::AxVCpuRef<A::VCpu>,
) {
    let Some(vm) = crate::get_vm_by_id(vm_id) else {
        warn!("VM[{vm_id}] not found, cannot drain VCpu[{vcpu_id}] interrupts");
        return;
    };
    let Ok((runtime, interrupts)) = vm.with_runtime(|runtime| {
        runtime.trace_virq_event(vm_id, crate::runtime::VirqTraceKind::Running, vcpu_id, 0);
        let interrupts = runtime.drain_pending_interrupts(vcpu_id);
        if let Some(interrupt) = interrupts.first() {
            runtime.trace_virq_event(
                vm_id,
                crate::runtime::VirqTraceKind::Drain,
                vcpu_id,
                pending_interrupt_vector(interrupt),
            );
        }
        Ok((runtime.clone(), interrupts))
    }) else {
        warn!("VM[{vm_id}] vCPU runtime not found, cannot drain VCpu[{vcpu_id}] interrupts");
        return;
    };

    for interrupt in interrupts {
        runtime.trace_virq_event(
            vm_id,
            crate::runtime::VirqTraceKind::Inject,
            vcpu_id,
            pending_interrupt_vector(&interrupt),
        );
        A::inject_pending_interrupt(&vm, vcpu, interrupt);
    }
}

fn pending_interrupt_vector(interrupt: &crate::vm::PendingInterrupt) -> u32 {
    match interrupt {
        crate::vm::PendingInterrupt::Normal(vector)
        | crate::vm::PendingInterrupt::External { vector, .. } => *vector as u32,
    }
}

/// Cleans up VCpu resources for a VM that is being deleted.
/// This removes the VM's entry from the global VCpu wait queue.
///

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 71.3s)

_Thinking:_

Now the picture is completely clear, and there's a significant discrepancy to flag in my analysis:

**The A worktree's path is NOT "direct inject_interrupt call"**. It's:
`queue_interrupt` → `pending_interrupts` push (unbounded) + trace Enqueue → `notify_all` + trace Notify → `send_ipi(cpu_id)` + trace Ipi → vCPU run loop `inject_pending_interrupts` → trace Running/Drain/Inject → `A::inject_pending_interrupt`.

So A and B are BOTH "enqueue → notify → IPI → drain → inject" paths. The difference between A and B is:
- A: `pending_interrupts` unbounded Vec per vCPU, VM-wide `notify_all`, `send_ipi` to `task.cpu_id()`, injection via `inject_pending_interrupt` (old path, presumably GIC emulated or whatever `A::inject_pending_interrupt` does)
- B: bounded per-vCPU queue, per-vCPU wait queue + notify_vcpu, `send_ipi` to affinity-configured pCPU, injection via dispatcher `inject_vcpu_interrupt`.

So the doc's description of A ("host injector 直接调用 vCPU backend 的 inject_interrupt") is WRONG or outdated — A1's log evidence (`enqueue=300 notify=300 ipi=300`) proves A goes through the same queue/notify/IPI staging. The real difference is the *injection backend* (`inject_pending_interrupt` vs `inject_vcpu_interrupt`) and the queue/wake mechanism. This is an important analytical finding:

1. **Doc/description mismatch**: A1 evidence contradicts the doc's own A definition. The A/B comparison as described ("direct call" vs "queue") is not what was actually measured. Either the doc's §1 description of A is wrong, or the A worktree at measurement time was different. As a result, the claimed "A 基线没有把 vIRQ 注入到 guest 正确的 host CPU" root cause ("从 host CPU 0 直接调用") is questionable.

Wait, actually let me reconsider. Maybe in the A worktree, at A1 time, the injection was via `inject_pending_interrupt` which is the "direct call" — the queue_interrupt path pushes to pending_interrupts and the vCPU's run loop calls `A::inject_pending_interrupt` — but the doc says "host injector 直接调用 vCPU backend 的 inject_interrupt"... The `send_ipi(task.cpu_id())` in A uses `task.cpu_id()`, which is the pCPU the task was created on (per the doc's §2.2: "vCPU affinity 使用 vCPU 配置的物理 CPU mask，不再依赖 task 创建瞬间的 task.cpu_id()" — the B-side fix). In A1, the vCPU task's `cpu_id()` was... the doc says the guest vCPU was bound to host CPU 3 but the IPI went to CPU... hmm, wait, the doc says "guest vCPU 绑定在 host CPU 3，但基线从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt".

Hmm, but with the A code: `queue_interrupt` → `runtime.queue_interrupt` returns `task.cpu_id()` — if the vCPU task was pinned to CPU 3, task.cpu_id() would be 3, and send_ipi(3). So the doc's claim that "注入发生在错误的 host CPU" doesn't match the code either — the IPI should target CPU 3.

Unless: the vCPU task was NOT pinned in the A config, and task.cpu_id() returned the creation CPU (0?). Hmm, actually the A1 failure analysis in the doc says injection happened on the wrong CPU. With `A::inject_pending_interrupt` called from the vCPU run loop... 

Wait, actually — hold on. There's another possibility. In the A worktree, is there a "direct" injection path used for A? Let me check: `run_periodic_virq_injector` in the A worktree calls `vm.inject_interrupt_to_vcpu(targets, vector)` → `queue_interrupt` — the queue path. So the injector used the queue path in A1. The trace events confirm (enqueue=300, notify=300, ipi=300).

So the doc's §1 description of A as "直接调用 inject_interrupt" is factually inconsistent with the A1 evidence. What actually distinguishes A from B:
- A: unbounded pending_interrupts Vec, VM-wide wait queue notify_all, pCPU from task.cpu_id(), inject via inject_pending_interrupt
- B: bounded queue (capacity 64), per-vCPU wait queue, pCPU from vCPU phys_cpu_set affinity, inject via dispatcher.

And the actual A1 failure: enqueue=300, notify=300, ipi=300, running=1, drain=0, inject=0. The vCPU run loop only ran once (running=1) — the IPIs didn't make the vCPU return to the host loop, or the vCPU task wasn't woken properly, or the guest didn't exit. Same fundamental blocker as B passthrough! Not "wrong CPU direct call".

Hmm wait, but the doc says guest received 0/300 and that A1's root cause was "GIC/vCPU 注入发生在错误的 host CPU 上". But if the A1 vCPU task woke only once (running=1), the injection... the injection happens in the run loop after wake. If the run loop ran once with an empty pending list, then no injection. The "wrong CPU" theory doesn't explain enqueue=300 but drain=0 — the queue was never drained because the vCPU task never woke. So A1's actual failure is the same wake failure as B1! The "wrong host CPU" explanation is a hypothesis, but the evidence (running=1, drain=0) points to the vCPU never being kicked/woken — identical to B's passthrough failure.

Hmm, but wait — could it be that in A, the IPI was sent to task.cpu_id() which was CPU 0 (task creation CPU) instead of the vCPU's affinity CPU 3? Then the IPI would go to CPU 0 (injector's CPU, also where the vCPU task was spawned?), the vCPU task on CPU 3 would not be woken... but the notify_all was still done — the vCPU task in the wait queue should still wake via notify_all! The wait queue is global; notify_all wakes the task wherever it is. So notify_all should have woken the vCPU task regardless of IPI target. Unless the vCPU task was already running the guest (not in the wait queue), in which case it would never come back to drain → running=1 → stuck in guest → matches.

Hmm, but wait: if the vCPU task was in the guest running on CPU 3, and the IPI was sent to the wrong CPU (0), then nothing kicks the vCPU out of the guest. notify_all wakes... nothing (task not sleeping). So vCPU stays in guest. That matches running=1! So actually the doc's A1 root cause could be right in a specific way: the *IPI* (the thing that forces a guest exit... well, actually in emulated GIC, does an IPI even force a guest exit? no — a host IPI forces a physical IRQ to CPU 3; if CPU 3 is running the guest, the physical IRQ traps it unless it's routed to the guest) — if the IPI targeted the wrong CPU, the vCPU on CPU 3 never gets kicked. With notify_all being no-op (task in guest, not sleeping), the vCPU runs the guest forever without ever re-entering the host run loop to drain.

OK so actually both explanations are consistent with the evidence, and the "wrong host CPU" claim is plausible: A sent the IPI to task.cpu_id() ≠ 3. But the doc says "从 host CPU 0 直接调用 inject_interrupt" which is wrong as a description of the mechanism.

Hmm wait, actually — I need to double check one more time whether there's a truly direct call path in the A worktree that the injector used at A1 time. Maybe at A1 time, the A worktree code was different (the A worktree has its own modifications — git log?). Let me check the A worktree's git log for recent commits and its diff.

Actually, I've spent enough. The point stands that the doc's §1 description of A contradicts A1's evidence log, and this deserves flagging. Let me also quickly verify: is the `pending_interrupts` queue in A unbounded? Yes: `.or_default().push(...)` — unbounded Vec.

Also for B emulated smoke: inject=1 — the one injection went through `A::inject_vcpu_interrupt` — which for the emulated GIC writes to vGIC LR. The guest still got 0. Doc's TODO #2 mentions GIC LR Group settings etc.

Also check: `inject_current_vcpu_interrupt` (self-IPI) — this uses a different path. Not relevant.

One more important thing to verify: what does `notify_vcpu` do vs the IPI — in B, is the IPI sent to the vCPU's pCPU, and in passthrough mode, would that physical IPI be routed to the guest (Zephyr sees an SGI)? If the GIC is in passthrough mode, the host does NOT trap the IPI; the guest vCPU receives it directly as a vIRQ (if it's configured as such) or... hmm. In passthrough mode, the vGIC routes physical interrupts directly to the guest vCPU via the GIC virtualization extensions (hardware injection). A physical SGI sent to pCPU 3 while guest runs → if the GIC is configured for hardware injection of that IRQ, the guest sees it without host trap. So the host-side "kick" never happens → vCPU never returns to host loop → no drain → matches B passthrough evidence exactly (running=1, drain=0, overflow 236).

So for B passthrough, the fix direction: the IPI used for the kick must be a host-visible interrupt (not routed to the guest), or the GIC IRQ must be configured so that the physical IPI traps the host. The doc's TODO #2 "host IPI 是否触发 guest exit" is exactly this question. Good.

For B emulated: exits happen (running=31) — WFI/timer exits probably — but only 1 drain with data. This means the enqueue→notify→wake path mostly fails to wake the vCPU task while it's in the guest, and the guest rarely exits on its own. Wait, if running=31 with 65 enqueues, and only 1 drain — the vCPU exited the guest 31 times; most of those found an empty queue. That means the enqueues happened at times when the vCPU was sleeping in the wait queue but notify didn't wake it?! If the vCPU was sleeping in the wait queue and notify_all... in B it's notify_vcpu on per-vCPU queue. Hmm — notify_vcpu checks `has_pending` then notifies. Race: enqueue → vCPU in guest → notify (no-op, not sleeping) → IPI to CPU 3 → in emulated mode, the physical IPI DOES trap the host (host handles IPI) → the vCPU task... is it preempted? Does the IPI handler kick the scheduler so the vCPU task re-enters its loop? If the IPI handler just acknowledges and returns, the vCPU continues in the guest — no drain. Only a WFI exit brings it back.

So in emulated mode too, the IPI doesn't cause a drain — because the vCPU continues running after the IPI handler returns (vCPU task stays in `vcpu.run()`). The drain point is only reached on guest exits (WFI, etc.). The 1 successful drain happened when a WFI exit coincided with queued data. The 31 exits with empty queue were WFI wakeups from timer/other vIRQs... hmm wait, but then after the 1 drain+inject, the vIRQ was injected into the vGIC LR... guest still saw 0. And the remaining 64 events stayed queued (never drained since no more exits? or exits stopped happening).

Hmm wait, why would exits stop? After injecting vector 48 into the LR... the guest should take the IRQ, exit... no, taking a vIRQ doesn't require exit (hardware injects directly from LR). The guest ISR would fire — guest received 0 though. So the injection into LR didn't deliver. Doc's TODO #2.

But why only 1 drain of 65? If WFI exits happen every time the guest idles (Zephyr idle loop = WFI), and the injector runs every 10ms... the guest would WFI constantly. Each WFI → exit → drain check → empty (if queue empty at that moment) → re-enter. The notify should wake the vCPU task between WFIs... 

OH WAIT. I see it now. Look at the run loop in ops.rs:

```rust
loop {
    inject_pending_interrupts(...);
    drain_and_inject_dispatched_interrupts(...);
    let exit = vcpu.run()?;
    match handle_vcpu_exit_bound(...) {
        Continue => continue,
        action => break Ok(action),
    }
}
```

If WFI is handled as `VcpuRunAction { waits_for_event: true, .. }` (break, then vcpu_run() calls `wait(&vm, &runtime, vcpu_id)`), the vCPU task sleeps in the per-vCPU wait queue. On enqueue: notify_vcpu wakes it → re-runs → drain. That should work... unless `notify_vcpu`'s `has_pending` check races, or the wake is lost because the condition check in `wait_until` happens... The doc claims the "pending 丢唤醒" bug was fixed. But emulated smoke shows 1/65.

Hmm, alternatively the WFI exit might be handled as Continue (no wait), and the guest re-enters with the physical IRQ pending... no.

Another possibility: in emulated mode, WFI exit → `VcpuRunAction.waits_for_event` → vcpu_run() waits → but wait — when the guest executes WFI and there's a pending vIRQ in the vGIC (from timer), WFI wakes... it's all guest-side.

I can't fully determine from here, and it's QEMU+ArceOS internals. The important analysis for the doc:

1. **A/B comparison as documented is not actually A/B on the stated axis** — A1's evidence shows A also went through enqueue/notify/ipi (queue staging), contradicting the §1 "direct call" description. The actual A/B difference is: unbounded pending list + VM-wide notify + task.cpu_id() pCPU + inject_pending_interrupt vs bounded queue + per-vCPU notify + affinity pCPU + dispatcher inject. The claimed A1 root cause ("直接调用导致 CPU 错位") doesn't match the code path.

Actually hold on, let me reconsider once more. Maybe the A worktree at A1 time was running an OLDER commit where the injector DID call the backend directly, and the A worktree code I'm looking at now (which contains enqueue/notify/ipi traces) was modified later... Let me check the A worktree git log quickly.

**Tool: bash**

**Input:**
```json
{
  "command": "git log --oneline -8 && git status -s | head && git diff --stat HEAD | tail -3",
  "workdir": "/home/huhu/tgoskits-2026"
}
```

**Output:**
```
32cfe9103 test(axvisor): add Zephyr periodic latency baseline
3a8374f4c docs(progress): record day3 device isolation baseline
1d9c18406 docs(progress): record day1 and day2 results
064661d81 feat:add meeting transcript
2478cd0d3 Merge pull request #1 from rcore-os/dev
f1774c1f6 docs: restructure architecture docs into subdirectories and add axvis… (#1792)
e2fe59dd6 fix(ci): prevent stale dependency locks during release (#1837)
b4632eaca fix(arm-gic-driver): use hardware CPU targets for GICv2 routing (#1803)
 M os/axvisor/Cargo.toml
 M os/axvisor/src/main.rs
 M virtualization/axvm/Cargo.toml
 M virtualization/axvm/src/lib.rs
 M virtualization/axvm/src/manager.rs
 M virtualization/axvm/src/runtime/mod.rs
 M virtualization/axvm/src/runtime/vcpus.rs
 M virtualization/axvm/src/vm/mod.rs
?? os/axvisor/src/realtime_probe.rs
?? plan.md
 virtualization/axvm/src/runtime/vcpus.rs | 160 ++++++++++++++++++++++++++++++-
 virtualization/axvm/src/vm/mod.rs        |  62 ++++++++++++
 8 files changed, 252 insertions(+), 4 deletions(-)

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 16.6s)

_Thinking:_

Interesting — both worktrees are at the same commit `32cfe9103` ("test(axvisor): add Zephyr periodic latency baseline") with uncommitted modifications. So the A worktree (tgoskits-2026) has the same baseline but modified for the A path. And the B worktree (tgoskits) has the B modifications.

Let me quickly diff the A worktree's vcpus.rs queue_interrupt vs the base commit to understand what A changed... Actually, the key question is settled: A's path (as it stands now in the A worktree) is queue-based with enqueue/notify/ipi trace events, NOT direct `inject_interrupt`. The A1 log evidence confirms this.

Wait, but maybe A1 was run BEFORE the A worktree got the queue/trace modifications, when the injector called the backend directly. Hmm. The doc says A1 evidence log: enqueue=300 etc. So at A1 time, the A worktree HAD the trace events. So at A1 time, A's path already was queue-based. Then the doc's §1 description of A is simply wrong/outdated. OR... the doc means something else by "directly calls inject_interrupt": maybe the A path's final injection (`A::inject_pending_interrupt`) vs B's `inject_vcpu_interrupt`. Hmm, "host injector 直接调用 vCPU backend 的 inject_interrupt" — no, that says the *host injector* directly calls the *vCPU backend's inject_interrupt*. That's clearly not what the code does.

Hmm hold on, let me actually check whether the A worktree has a truly direct path somewhere. `inject_current_vcpu_interrupt` — "failed to inject self IPI into current vCPU" — that's the self-IPI path. Let me check what that does and where queue_interrupt's pending list gets injected... Also let me check the A worktree's `dispatch_vcpu_interrupt_with` callers at lines 1829/1856/1873.

Actually — wait. I should also double-check: maybe in the A worktree, the realtime probe uses a DIFFERENT injector that directly calls `inject_vcpu_interrupt` or `inject_current_vcpu_interrupt` on the backend. Let me look at the A worktree's realtime_probe.rs.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"inject\\|spawn_periodic\\|injector\" os/axvisor/src/realtime_probe.rs | head -20; echo ===; grep -n \"inject_current_vcpu_interrupt\\|fn inject_pending_interrupt\\b\" virtualization/axvm/src/architecture/ops.rs virtualization/axvm/src/lib.rs",
  "workdir": "/home/huhu/tgoskits-2026"
}
```

**Output:**
```
1://! Fixed-input host injector for the OpenRace realtime A/B experiment.
14:/// Start the exact same bounded injector in A and B.
21:        injector_cpu_id: Some(INJECTOR_CPU_ID),
23:    match AxvmRuntime::start_periodic_virq_injector(VM_ID, config) {
25:            "OpenRace realtime injector armed vm={} vcpu={} vector={} period_ms={} samples={} \
26:             injector_cpu={}",
34:        Err(err) => warn!("OpenRace realtime injector was not armed: {err:?}"),
===
virtualization/axvm/src/architecture/ops.rs:41:    fn inject_pending_interrupt(
virtualization/axvm/src/lib.rs:72:    inject_current_vcpu_interrupt, register_vm,

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 8.4s)

_Thinking:_

Both A and B use the same injector (`start_periodic_virq_injector` → `inject_interrupt_to_vcpu`). So the A/B difference is entirely in the injection *backend path*:

- A worktree: `inject_interrupt_to_vcpu` → `queue_interrupt` → `runtime.queue_interrupt` (unbounded `pending_interrupts`) + `notify_all` + `send_ipi(task.cpu_id())` → run loop `inject_pending_interrupts` → `A::inject_pending_interrupt`.

Hmm wait, but then A is not "direct inject_interrupt". Unless... let me check what `A::inject_pending_interrupt` does — maybe it's the vGIC path and it's called on the vCPU's pCPU (in the run loop). And in A1, the doc claims the injection happened on the wrong CPU... 

Hmm, hold on. Actually wait. Let me look at the actual A worktree diff vs base to see what the A experiment modified. `git diff --stat HEAD` showed vcpus.rs +160, vm/mod.rs +62. Let me see the actual A diff in vm/mod.rs and vcpus.rs — maybe A's `inject_interrupt_to_vcpu` DOES directly call the backend in the A worktree. Line 1379 in the A worktree showed `queue_interrupt`... that IS the current A worktree code.

Wait, hmm, no. Hold on. Let me look at the A worktree's vm/mod.rs line 1379 context — the snippet I got shows `inject_interrupt_to_vcpu` calling `queue_interrupt`. And the doc's A1 log has enqueue/notify/ipi = 300 each. But wait — hmm, that contradicts §1 again.

Hmm, actually wait. Wait wait. Let me reconsider. Maybe I misread. Let me look at A's diff for vcpus.rs `queue_interrupt` more carefully... The A worktree `queue_interrupt` DOES the trace events. OK.

Hmm, so was the doc's §1 "A（基线）：host injector 直接调用 vCPU backend 的 inject_interrupt" simply wrong? OR is there a config switch in the A worktree where A1 was run with a different code state (before the trace was added)?

The doc §2.1 says "已增加 per-host-CPU bounded trace，能够记录注入链路各阶段" — added to the COMMON experiment components. So the trace exists in both. And A1's log has the trace events. So at A1 time, A was the queue path.

Hmm, but then what does "A 基线从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt" mean? Maybe the doc author considered `queue_interrupt → notify_all → send_ipi` as "direct" because... no. Actually, hmm, maybe in the A worktree, at A1 time, `inject_interrupt_to_vcpu` called `inject_current_vcpu_interrupt` or directly the vGIC `inject_interrupt` on the vCPU backend object — and the trace events came from a slightly different arrangement.

I cannot fully resolve this without the A1 log file itself. The log is at /tmp/ab-A1.log. Let me check if it exists!

**Tool: bash**

**Input:**
```json
{
  "command": "ls -la /tmp/ab-*.log 2>/dev/null; echo ===; sed -n 1,40p /tmp/ab-A1.log 2>/dev/null"
}
```

**Output:**
```
-rw-rw-r-- 1 huhu huhu 179000  8月  8 21:11 /tmp/ab-A1.log
-rw-rw-r-- 1 huhu huhu  65647  8月  8 21:35 /tmp/ab-B1-debug.log
-rw-rw-r-- 1 huhu huhu 128039  8月  8 21:37 /tmp/ab-B1-emu.log
-rw-rw-r-- 1 huhu huhu  68715  8月  8 21:14 /tmp/ab-B1.log
-rw-rw-r-- 1 huhu huhu 123492  8月  8 21:28 /tmp/ab-B1-new2.log
-rw-rw-r-- 1 huhu huhu 209385  8月  8 21:26 /tmp/ab-B1-new.log
-rw-rw-r-- 1 huhu huhu 123420  8月  8 21:30 /tmp/ab-B1-smoke.log
-rw-rw-r-- 1 huhu huhu 123415  8月  8 21:34 /tmp/ab-B1-target.log
===
    Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.18s
     Running `target/debug/tg-xtask axvisor qemu --config os/axvisor/configs/board/qemu-aarch64.toml --qemu-config os/axvisor/configs/qemu/qemu-aarch64.toml --vmconfigs scripts/test/zephyr-soft-virq/axvisor-qemu-aarch64.toml`
[2026-08-08T13:11:28Z INFO  axbuild::context] Workspace root: /home/huhu/tgoskits-2026
[2026-08-08T13:11:28Z INFO  axbuild::context] Workspace member dir for axvisor: /home/huhu/tgoskits-2026/os/axvisor
file already exists and passed checksum verification
image archive verified at /home/huhu/tgoskits-2026/tmp/axbuild/rootfs/rootfs-aarch64-alpine.img.tar.xz
image already extracted and up to date at /home/huhu/tgoskits-2026/tmp/axbuild/rootfs/rootfs-aarch64-alpine.img
Using build config: os/axvisor/configs/board/qemu-aarch64.toml
[2026-08-08T13:11:30Z INFO  ostool::run::qemu] Using QEMU config file: os/axvisor/configs/qemu/qemu-aarch64.toml
[axbuild] qemu build+run package=axvisor target=scripts/targets/std/pie/aarch64-unknown-linux-musl.json config=os/axvisor/configs/board/qemu-aarch64.toml ...
sh -c /home/huhu/tgoskits-2026/tmp/axbuild/std/prebuild/prebuild-aarch64-unknown-linux-musl-4058bcd4b8fcff56.sh
AR_aarch64_unknown_linux_musl=aarch64-linux-musl-ar
AXVISOR_VM_CONFIGS=/home/huhu/tgoskits-2026/scripts/test/zephyr-soft-virq/axvisor-qemu-aarch64.toml
AX_ARCH=aarch64
AX_LOG=info
AX_TARGET=aarch64-unknown-none-softfloat
CARGO_UNSTABLE_JSON_TARGET_SPEC=true
CC_aarch64_unknown_linux_musl=aarch64-linux-musl-cc
CFLAGS_aarch64_unknown_linux_musl=-mgeneral-regs-only -fno-stack-protector
CXXFLAGS_aarch64_unknown_linux_musl=-mgeneral-regs-only -fno-stack-protector
cargo build --config /home/huhu/tgoskits-2026/tmp/axbuild/std/config-aarch64-unknown-linux-musl-dynamic.toml -p axvisor --target scripts/targets/std/pie/aarch64-unknown-linux-musl.json -Z unstable-options --target-dir /home/huhu/tgoskits-2026/target --features ax-std/fs,ax-std/nvme,fs,openrace-realtime,log/release_max_level_info -Z json-target-spec --bin axvisor --release --message-format json-render-diagnostics
warning: unused import: `NestedPageTableOps`
  --> virtualization/axvm/src/vm/mod.rs:33:30
   |
33 | use axaddrspace::{AddrSpace, NestedPageTableOps};
   |                              ^^^^^^^^^^^^^^^^^^
   |
   = note: `#[warn(unused_imports)]` (part of `#[warn(unused)]`) on by default

warning: function `with_vm` is never used
  --> virtualization/axvm/src/manager.rs:73:15
   |
73 | pub(crate) fn with_vm<F, R>(vm_id: VMId, f: F) -> Option<R>
   |               ^^^^^^^
   |
   = note: `#[warn(dead_code)]` (part of `#[warn(unused)]`) on by default

warning: function `active_vcpu_mask` is never used
  --> virtualization/axvm/src/manager.rs:82:15
   |

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.3s)

_Thinking:_

The A1 log exists. Let me look at the actual trace lines and VIRQ_INJECT lines in the A1 log to nail down the A path behavior. Let me grep the trace events.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -o \"event=[a-z_]*\" /tmp/ab-A1.log | sort | uniq -c; echo ===; grep \"VIRQ_TRACE\\|VIRQ_INJECT\\|SOFTWARE VIRQ\" /tmp/ab-A1.log | head -8; echo ===; grep \"event=\" /tmp/ab-A1.log | head -6; echo ===; grep \"VCpu\\[0\\]\" /tmp/ab-A1.log | grep -v \"INFO  axvm\" | head -5"
}
```

**Output:**
```
    300 event=enqueue
    300 event=ipi
    300 event=notify
      1 event=running
===
SOFTWARE VIRQ READY vector=48 samples=300
[37m[  0.840847 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=0 vm=2 vcpu=0 vector=48 requested_ns=839879728 completed_ns=840802912 status=ok[m
[m[37m[  0.849777 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=1 vm=2 vcpu=0 vector=48 requested_ns=849757792 completed_ns=849776080 status=ok[m
[m[37m[  0.859781 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=2 vm=2 vcpu=0 vector=48 requested_ns=859762496 completed_ns=859779648 status=ok[m
[m[37m[  0.869803 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=3 vm=2 vcpu=0 vector=48 requested_ns=869776160 completed_ns=869800112 status=ok[m
[m[37m[  0.879864 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=4 vm=2 vcpu=0 vector=48 requested_ns=879776080 completed_ns=879858960 status=ok[m
[m[37m[  0.889947 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=5 vm=2 vcpu=0 vector=48 requested_ns=889869744 completed_ns=889941600 status=ok[m
[m[37m[  0.899795 0:15 axvm::runtime::vcpus:100] [32mVIRQ_INJECT sequence=6 vm=2 vcpu=0 vector=48 requested_ns=899748208 completed_ns=899790560 status=ok[m
===
[m[37m[  3.834703 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=0 ts_ns=832369504 cpu=3 vm=2 vcpu=0 event=running vector=0[m
[m[37m[  3.835025 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=1 ts_ns=840202592 cpu=0 vm=2 vcpu=0 event=enqueue vector=48[m
[m[37m[  3.835375 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=2 ts_ns=840595792 cpu=0 vm=2 vcpu=0 event=notify vector=48[m
[m[37m[  3.835646 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=3 ts_ns=840726256 cpu=0 vm=2 vcpu=0 event=ipi vector=48[m
[m[37m[  3.835777 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=4 ts_ns=849763088 cpu=0 vm=2 vcpu=0 event=enqueue vector=48[m
[m[37m[  3.835919 0:15 axvm::vm:393] [32mVIRQ_TRACE seq=5 ts_ns=849765024 cpu=0 vm=2 vcpu=0 event=notify vector=48[m
===
[m[37m[  0.828105 0:2 axvm::runtime::vcpus:535] [32mSpawning task for VM[2] VCpu[0][m
[m[37m[  0.828505 0:2 axvm::runtime::vcpus:552] [32mVCpu task Task(16, "VM[2]-VCpu[0]") created cpumask: [3, ][m
[m[37m[  0.831430 3:16 axvm::runtime::vcpus:606] [32mVM[2] VCpu[0] waiting for running[m
[m[37m[  0.831923 3:16 axvm::runtime::vcpus:644] [32mVM[2] VCpu[0] running...[m

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 16.8s)

_Thinking:_

Excellent! Now I have hard evidence. In A1:

- `seq=0 running ts_ns=832369504 cpu=3` — the vCPU run loop entered once, on CPU 3 (the vCPU's pCPU!), at t=0.83s. Then the vCPU entered the guest.
- `seq=1 enqueue ts_ns=840202592 cpu=0` — first enqueue at t=0.84s on CPU 0.
- The vCPU never came back to the host run loop (`running=1`).
- All 300 enqueue/notify/ipi events happen on cpu=0.

So A1's actual failure: the vCPU entered the guest ONCE at t=0.831s and never exited again during the whole experiment. The enqueue/notify/ipi happened 300 times on CPU 0, but the vCPU task was in the guest and never came back to drain.

So the doc's §3.1 root-cause claim — "基线从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt，因此 GIC/vCPU 注入发生在错误的 host CPU 上" — does NOT match the code path or the trace. The trace shows the A path DID stage through a queue (enqueue/notify/ipi events), and the IPI... let me check where the IPI was sent. The trace `ipi` events show `cpu=0` — the event is recorded at the sender (injector on CPU 0). The send_ipi target would be `task.cpu_id()`.

Wait — the vCPU task "VM[2]-VCpu[0]" has `cpumask: [3, ]` — pinned to CPU 3. What is `task.cpu_id()` at that point? For an ArceOS task pinned to CPU 3, `cpu_id()` should return 3. But this is exactly the bug the B-side doc claims was fixed: "vCPU affinity 使用 vCPU 配置的物理 CPU mask，不再依赖 task 创建瞬间的 task.cpu_id()". In A, `send_ipi(task.cpu_id())` — if `cpu_id()` returned the *creation* CPU (0, since the task was spawned from CPU 0 by the startup sequence) rather than the affinity mask CPU (3)... then the IPI went to CPU 0 (or wherever), and the vCPU on CPU 3 never got kicked out of the guest.

Actually wait — hmm, task spawn happened on CPU 0 ("Spawning task for VM[2] VCpu[0]" logged on cpu 0:2). But the task "waiting for running" is logged on 3:16, so the task migrated to CPU 3 (or was created bound to 3 and logged there). `task.cpu_id()` semantics: in ArceOS, `cpu_id()` returns the current CPU... hmm, for a task struct, `cpu_id()` typically returns the CPU the task is currently running on or is bound to. If it returned 3, the IPI would go to CPU 3. Then why didn't the vCPU exit?

The IPI goes to CPU 3 while the guest is running on CPU 3. The physical IPI traps the host (unless routed to guest in passthrough). Then the ArceOS IPI handler runs. What does it do? If it's a scheduler-related IPI, it would mark a task ready... but the vCPU task isn't sleeping — it's the running task in the guest. After the handler returns, the vCPU run loop... continues in guest. No drain. Because nothing tells the run loop to exit the guest!

So even with a correctly-targeted IPI: **the IPI interrupts the vCPU task's `vcpu.run()` (host kernel context), the IPI handler returns, and execution resumes inside the guest entry — the drain point is NOT re-executed**. For the drain to happen, the exit handler must break out of the guest loop.

Hmm wait, no. If the IPI traps the host while in guest mode, the hardware exits guest mode (EL2 trap), the IPI handler runs, returns → the run loop continues → the drain is only at the top of the loop — which was already passed. The vCPU re-enters the guest. So a host IPI alone never triggers a drain UNLESS the IPI handler sets a flag / causes `vcpu.run()` to return an ExternalInterrupt exit.

Hmm, but in the emulated B case, running=31 — exits DID happen (WFI? timer?). Let me check the B1-emu log for the exit reasons.

Actually wait, let me reconsider. `vcpu.run()` in QEMU with KVM-style... this is QEMU emulation (qemu-system-aarch64) with a GIC model. Host IPIs... hmm, in QEMU, "host" is ArceOS running as guest on QEMU? No wait — this is AxVisor: ArceOS is the hypervisor running on QEMU's virt machine. ArceOS runs multiple vCPUs (SMP), guests run on top. "host IPI" = ArceOS inter-processor interrupt via GIC (physical SGI), handled by ArceOS's IPI handler.

In QEMU, when the ArceOS vCPU (a QEMU vCPU thread) is executing guest code (EL1) and a physical SGI arrives, QEMU emulates the GIC: the SGI is delivered to the ArceOS vCPU as a physical interrupt. ArceOS handles it in its IRQ context. But the guest execution — QEMU would have already exited the guest to deliver the physical IRQ to the hypervisor... In QEMU, EL2→EL1 transitions: when a physical IRQ is pending and the hypervisor runs the guest, QEMU either (a) injects it directly into the guest if the GIC model routes it so (hardware passthrough style), or (b) exits to EL2 with ExternalInterrupt if the guest doesn't have IRQ masking... With GICv2/v3 emulation and the standard QEMU KVM-style, physical IRQs are not automatically routed to the guest — the hypervisor gets the exit and forwards. Hmm, actually with QEMU TCG (non-KVM), the GIC is emulated and IRQ delivery to the guest happens if the hypervisor sets up the vGIC. The "passthrough" mode in this AxVM context likely means: the vGIC routes physical interrupts directly into the guest (hardware virtualization via QEMU's GICv3 model?).

OK — I've gone deep enough into speculative territory. The analysis deliverable is about the document. But I've now got REALLY solid evidence for the key findings. Let me also quickly check the emulated log to see the exits, then check what `inject_pending_interrupt` does in A vs `inject_vcpu_interrupt` in B (the actual injection backends), so I can describe the true A/B difference. Let me grep the A worktree ops.rs.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"fn inject_pending_interrupt\\|fn inject_vcpu_interrupt\" virtualization/axvm/src/architecture/ops.rs virtualization/axvm/src/vm/mod.rs virtualization/axvm/src/arch/aarch64/*.rs 2>/dev/null | head; echo ===; grep \"event=\" /tmp/ab-B1-emu.log | grep -o \"event=[a-z_]*\" | sort | uniq -c; echo ===; grep -o \"event=\\(running\\|drain\\|inject\\)\" /tmp/ab-B1-emu.log | head; grep \"VCpu\\[0\\] waiting\\|running\\.\\.\\.\\|waits_for_event\\|WaitForEvent\\|WFI\\|exit\" /tmp/ab-B1-emu.log | head -12",
  "workdir": "/home/huhu/tgoskits-2026"
}
```

**Output:**
```
virtualization/axvm/src/architecture/ops.rs:41:    fn inject_pending_interrupt(
virtualization/axvm/src/architecture/ops.rs:81:    fn inject_vcpu_interrupt(
virtualization/axvm/src/architecture/ops.rs:398:    fn inject_vcpu_interrupt_preserves_level_trigger_at_backend_boundary() {
===
      1 event=drain
     65 event=enqueue
      1 event=inject
     65 event=ipi
     65 event=notify
    235 event=queue_overflow
     31 event=running
===
event=running
event=running
event=running
event=running
event=running
event=running
event=running
event=running
event=drain
event=inject
[m[37m[  0.810091 3:16 axvm::runtime::vcpus:574] [32mVM[2] VCpu[0] waiting for running[m
[m[37m[  0.810655 3:16 axvm::runtime::vcpus:614] [32mVM[2] VCpu[0] running...[m

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 17.0s)

_Thinking:_

Now I have enough evidence. Let me quickly check what the exit flow does in the emulated B log to see if the vCPU kept exiting (running=31 means the loop ran 31 times — so exits happened, e.g., WFI). The vCPU is probably in a WFI→exit→re-enter cycle, and the enqueues happen while it's in guest; the notify should wake it from the per-vCPU wait queue... wait, no — running=31 means the loop top was reached 31 times. If the vCPU were sleeping in the wait queue, "running" would increment only when woken and re-entering. 31 runs vs 65 enqueues and only 1 drain — this means the wakes from notify mostly... hmm, actually wait. If notify woke the vCPU each time, we'd expect drain≈65. Only 1 drain means the vCPU was NOT woken by notify — the 31 runs were spontaneous (WFI exits / other events).

Hmm interesting — so even in emulated mode, the notify/wake path fails. Why? `notify_vcpu` → checks has_pending → per-vCPU WaitQueue notify_one. The vCPU task sleeps in `wait_vcpu_until(vcpu_id, condition)` with condition `has_pending || !running || ...`. When enqueue happens: push → notify → the waiter's condition re-check happens on notify_one... 

Wait — actually, maybe the vCPU is NOT in the wait queue when the enqueue happens. Timeline in emulated mode: guest boots, Zephyr runs, idles on WFI. WFI exit → the run loop... does WFI wait? If Zephyr has interrupts pending (e.g., tick timer), WFI returns immediately. The guest mostly runs, occasionally WFI-exits. When the vCPU is in the guest and an enqueue+notify occurs, the notify finds the task NOT sleeping (it's running in the guest) → no-op. The IPI is sent to CPU 3 → physical IPI → in emulated mode this traps the hypervisor... but the vCPU continues running the guest after the handler — never reaches the drain point! The guest keeps running with a pending queue entry. Only when the guest happens to WFI-exit does the drain point get reached — and that exit coincided with queue content exactly once (drain=1).

Hmm wait, but that doesn't add up either: if the vCPU WFI-exited often (31 times), each exit would hit the drain point with a non-empty queue (since the queue fills at 10ms intervals and doesn't drain)... unless the drain point checks happen BEFORE the enqueues mostly — no, the 31 runs span the whole 3s period (enqueue seq 0-64 during that period; 65 enqueues then 235 overflows).

OK here's a cleaner model: the vCPU exits the guest on WFI, the run loop hits `drain_and_inject` → drain (mostly empty at that moment? no...) → then re-enters the guest. But the WFI exit → wait: WFI with no pending IRQ → `waits_for_event: true` → the vCPU task sleeps in the per-vCPU wait queue! Then enqueue → notify → should wake it → drain. That would give drain≈65. It gave 1.

Unless the WFI exits are handled WITHOUT waiting (Zephyr's timer keeps a pending IRQ so WFI completes immediately), so the vCPU never sleeps in the host wait queue, and notify is always a no-op. Then the IPI must be the kick mechanism — and in emulated mode, the physical IPI... the IPI handler must break the guest loop and return to the drain point. Does the IPI handler do that? Looking at the B emulated results: only 1 drain — so NO, the IPI doesn't cause a drain either. The 1 drain was pure luck (a WFI exit coinciding with queued data).

Hmm wait, actually — but that contradicts... if the vCPU is in the guest and the IPI traps the host (emulated GIC: physical IRQs go to the hypervisor), after the IPI handler, execution resumes in the guest. The drain point is skipped. So BOTH A and B share this fundamental design gap: **a host IPI does not cause the vCPU run loop to reach the drain point**; the drain happens only on guest exits. The guest (Zephyr idle loop) exits on WFI; but if Zephyr's timer IRQ is pending, WFI returns immediately without exit... or exits once and the run loop's `waits_for_event` handling decides...

Actually, let me check the actual WFI handling in the arch code. Let me look at handle_vcpu_exit_bound for WFI. Actually, I recall from the diff of vcpus.rs: `Ok(VcpuRunAction { waits_for_event: true, .. }) => wait(&vm, &runtime, vcpu_id)` — the run loop waits. So WFI exits DO sleep in the wait queue. Then notify SHOULD wake...

Hmm, hold on, unless in emulated mode the WFI doesn't produce `waits_for_event` because the emulated vGIC has a pending vIRQ (Zephyr tick). Or — QEMU WFI behavior: with emulated GIC and pending IRQ, WFI returns without exit.

OK. I really can't fully close this loop without deeper tracing, and that's exactly what the doc's TODO #3/#4 identify ("检查 ArmVcpu::run() 对 ExternalInterrupt/WFI exit 的处理"). My job is to analyze the DOC, not solve the bug. I have more than enough for a high-quality analysis.

One more check — the A worktree's inject_pending_interrupt vs B's inject_vcpu_interrupt, to state the real A/B difference. Let me look at both backends.

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 30,110p virtualization/axvm/src/architecture/ops.rs",
  "workdir": "/home/huhu/tgoskits-2026"
}
```

**Output:**
```
    }

    #[allow(dead_code)]
    fn set_vcpu_on_args(vcpu: &crate::vm::AxVCpuRef<Self::VCpu>, _vcpu_id: usize, arg: usize) {
        vcpu.set_gpr(0, arg);
    }

    fn before_first_run(_vm: &crate::AxVMRef, _vcpu: &crate::vm::AxVCpuRef<Self::VCpu>) {}

    fn before_vcpu_run(_vm: &crate::AxVMRef, _vcpu: &crate::vm::AxVCpuRef<Self::VCpu>) {}

    fn inject_pending_interrupt(
        _vm: &crate::AxVMRef,
        vcpu: &crate::vm::AxVCpuRef<Self::VCpu>,
        interrupt: crate::vm::PendingInterrupt,
    ) {
        match interrupt {
            crate::vm::PendingInterrupt::Normal(vector) => {
                trace!(
                    "Injecting queued interrupt {vector:#x} into VM[{}] VCpu[{}]",
                    vcpu.vm_id(),
                    vcpu.id()
                );
                if let Err(err) = vcpu.inject_interrupt(vector) {
                    warn!(
                        "Failed to inject queued interrupt {vector:#x} into VM[{}] VCpu[{}]: \
                         {err:?}",
                        vcpu.vm_id(),
                        vcpu.id()
                    );
                }
            }
            crate::vm::PendingInterrupt::External {
                vector,
                physical_irq,
            } => {
                warn!(
                    "VM[{}] VCpu[{}] dropped unsupported external interrupt vector={vector:#x}, \
                     physical_irq={physical_irq:#x}",
                    vcpu.vm_id(),
                    vcpu.id()
                );
            }
        }
    }

    /// Injects a pending `PendingVcpuInterrupt` into the target vCPU.
    ///
    /// Called in the **target vCPU's run loop** so that accesses to banked
    /// system registers (GIC LR, x86 vLAPIC, etc.) happen on the correct
    /// physical CPU.
    fn inject_vcpu_interrupt(
        vcpu: &crate::vm::AxVCpuRef<Self::VCpu>,
        interrupt: PendingVcpuInterrupt,
    ) -> AxVmResult {
        vcpu.inject_interrupt_with_trigger(interrupt.id.0 as usize, interrupt.trigger)
    }

    fn after_external_interrupt(
        _vm: &crate::AxVMRef,
        _vcpu: &crate::vm::AxVCpuRef<Self::VCpu>,
        vector: usize,
    ) {
        crate::host::arceos::dispatch_host_irq(vector);
        crate::check_timer_events();
    }

    /// Releases architecture runtime state after the VM's last vCPU exits.
    ///
    /// The VM reference is required for architecture state that is published
    /// through VM-local device services rather than indexed in global tables.
    fn on_last_vcpu_exit(_vm: &crate::AxVMRef) {}

    fn after_mmio_write(_vm: &crate::AxVMRef) {}

    fn handle_vcpu_exit_bound(
        vm: &crate::AxVMRef,
        vcpu: &crate::vm::AxVCpuRef<Self::VCpu>,
        exit: <Self::VCpu as VmArchVcpuOps>::Exit,
    ) -> AxVmResult<BoundVcpuExit<Self::DeferredRunWork>>;


```

---

## Assistant (Build · DeepSeek V4 Flash Free · 7.2s)

_Thinking:_

Now I have the complete picture. Let me summarize the true A/B difference:

**A (tgoskits-2026)**: 
- `inject_interrupt_to_vcpu` → `queue_interrupt` → unbounded `pending_interrupts` Vec + trace Enqueue → `notify_all()` (VM-wide wait queue) + trace Notify → `send_ipi(task.cpu_id())` + trace Ipi
- Injection backend: run loop `inject_pending_interrupts` → `A::inject_pending_interrupt` → `vcpu.inject_interrupt(vector)` (edge only, no trigger info)

**B (tgoskits)**:
- `inject_interrupt_to_vcpu` → `dispatch_vcpu_interrupt` → bounded per-vCPU queue (capacity 64) + Enqueue trace → per-vCPU wait queue notify_vcpu + Notify trace → send_ipi(affinity-mapped pCPU) + Ipi trace
- Injection backend: `drain_and_inject_dispatched_interrupts` → `A::inject_vcpu_interrupt` → `vcpu.inject_interrupt_with_trigger(id, trigger)`

Both share the same fundamental kick mechanism (notify + IPI). A1's log PROVES A also went through enqueue/notify/ipi staging on CPU 0 (injector), so the doc's §1 description of A as "host injector 直接调用 vCPU backend 的 inject_interrupt" contradicts A1's own evidence.

And A1's real observed failure: vCPU ran the loop once on CPU 3 (running=1 at t=0.832s, before the first enqueue at t=0.840s), then never came back to the host loop. 300 IPIs to... (the target — need to verify where the A IPI went; task.cpu_id() — the task was created on CPU 0... but the log shows the task on CPU 3 for "waiting for running". Hmm — `send_ipi(cpu_id)` where cpu_id = `task.cpu_id()` returned by runtime.queue_interrupt at enqueue time (t=0.84s). The task was by then running on CPU 3 (in the guest). If task.cpu_id() returns the current CPU... it would be 3. Then the IPI DID go to CPU 3. And the vCPU was in the guest on CPU 3. The physical IPI → traps ArceOS → IPI handler → ... → vCPU resumes guest. No drain. So the doc's "wrong host CPU" root cause is unproven/wrong — the actual failure is that the kick (notify/IPI) never makes the run loop drain, in both A and B.

Hmm, wait, actually — I should double check what ArceOS's IPI handler does here. `crate::host::task::send_ipi` in arceos.rs. Let me look quickly. Line 147.

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 120,180p virtualization/axvm/src/host/arceos.rs; echo ===; grep -rn \"ipi\\|IPI\" os/axvisor/src/platform_irq.rs | head -10"
}
```

**Output:**
```
pub(crate) type ArceOsIrqError = modules::ax_hal::irq::IrqError;
pub(crate) type ArceOsWaitQueueHandle = api::task::AxWaitQueueHandle;
pub(crate) use modules::ax_task::TaskExt as ArceOsTaskExt;

pub(crate) fn current_task() -> ArceOsCurrentTask {
    modules::ax_task::current()
}

pub(crate) fn spawn_task(task: ArceOsTaskInner) -> ArceOsAxTaskRef {
    modules::ax_task::spawn_task(task)
}

pub(crate) fn yield_now() {
    thread::yield_now();
}

pub(crate) fn wait_queue_wait_until(
    queue: &api::task::AxWaitQueueHandle,
    condition: impl Fn() -> bool,
) {
    api::task::ax_wait_queue_wait_until(queue, condition, None);
}

pub(crate) fn wait_queue_wake(queue: &api::task::AxWaitQueueHandle, count: u32) {
    api::task::ax_wait_queue_wake(queue, count);
}

pub(crate) fn send_ipi(cpu_id: usize) {
    if modules::ax_hal::percpu::this_cpu_id() == cpu_id {
        return;
    }
    modules::ax_hal::irq::send_ipi(
        modules::ax_hal::irq::ipi_irq(),
        modules::ax_hal::irq::IpiTarget::Other { cpu_id },
    );
}

pub(crate) fn run_on_cpu_sync(
    cpu_id: usize,
    f: unsafe fn(*mut ()),
    arg: *mut (),
) -> Result<(), ArceOsIrqError> {
    // SAFETY: the caller guarantees that `arg` stays valid until the target CPU
    // has executed `f`; `ax_hal` provides the synchronous completion boundary.
    unsafe { modules::ax_hal::irq::run_on_cpu_sync(modules::ax_hal::irq::CpuId(cpu_id), f, arg) }
}

fn send_ipi_to_all_except_current(cpu_num: usize) {
    if cpu_num <= 1 {
        return;
    }
    let cpu_id = modules::ax_hal::percpu::this_cpu_id();
    modules::ax_hal::irq::send_ipi(
        modules::ax_hal::irq::ipi_irq(),
        modules::ax_hal::irq::IpiTarget::AllExceptCurrent { cpu_id, cpu_num },
    );
}

#[cfg(any(feature = "fs", feature = "host-fs"))]
pub fn shutdown_host_filesystems() -> AxVmResult {
    modules::ax_fs_ng::shutdown_filesystems()
===

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 51.5s)

_Thinking:_

The IPI goes to `ipi_irq()`. In ArceOS, the IPI handler typically performs scheduler related work (wake up tasks on that CPU). Let me check what the ArceOS IPI handler does — whether the guest context (vCPU in guest mode) is interrupted. When a physical IRQ arrives while the ArceOS CPU is in guest mode (EL1 guest), the hardware traps to EL2, QEMU delivers the IRQ to the ArceOS hypervisor — the vCPU's `run()` returns with ExternalInterrupt... In this AxVM architecture, when the guest runs and a physical IRQ comes in, does `vcpu.run()` return? Let me look at the run loop and how ExternalInterrupt is handled — `after_external_interrupt` calls `dispatch_host_irq(vector)`. So there IS handling of physical IRQs during guest execution: they trap, dispatch to host IRQ handlers (including the IPI handler), and then... `handle_vcpu_exit_bound` returns Continue → the run loop continues → top of loop → DRAIN! 

Wait — so if a physical IPI traps the host while the guest is running, the loop DOES reach the drain point! So in theory, the kick should work: IPI → ExternalInterrupt exit → handle → Continue → loop top → drain. Then why did A1 show running=1??

Possibilities:
1. The physical IPI did NOT trap the host — it was routed directly to the guest as a virtual IRQ (passthrough-style GIC). But A1 used the same config as B's emulated? Doc says A1's config was the A baseline, presumably emulated GIC (the default). Hmm, in A1's trace, the vCPU ran once at t=0.83s and never again. If Zephyr was in WFI (idle), WFI would exit... and the guest would be waiting for IRQs. Zephyr with no IRQ received → WFI → host exit → run loop → `waits_for_event` → vCPU task sleeps in wait queue. Then enqueue → notify_all → wake → drain → should work!!

Unless... `notify_all` doesn't wake the vCPU task. In A: `runtime.notify_all()` — the VM-wide wait queue. The vCPU task sleeps via `wait_for(&runtime, condition)` → `wait_until` → the VM-wide `wait_queue.wait_until`. And notify_all → wait_queue.notify_all. Should wake.

Hmm, BUT — maybe the vCPU task was NOT sleeping in the wait queue during the enqueues: the guest was RUNNING (not WFI'd). What does Zephyr do at boot with no interrupts? Its idle loop: WFI. So the guest should quickly WFI-exit... unless the QEMU GIC emulation returns from WFI without exiting (pending vIRQ from the Zephyr timer!). Zephyr's tick timer fires constantly (every 10ms or so) — vIRQ pending → WFI returns immediately in guest → the guest never exits... but then running would stay 1 (no more exits) — MATCHES A1!

Hmm, so in A1: the guest boots, enters the guest at t=0.832s, Zephyr runs, timer ticks keep WFI from exiting, so the vCPU NEVER exits the guest for the rest of the experiment. The notify_all + IPI kicks don't cause an exit (notify: task not sleeping; IPI: physical IRQ... in QEMU with the emulated GIC — wait, physical IRQ while guest running DOES trap... unless the IRQ is masked or the GIC routes it as vIRQ).

Hold on — if the physical IPI trapped the host, the loop would hit the drain. It didn't (running=1). So the IPI did NOT trap the host. Why? Possibilities: (a) the IPI went to the wrong CPU (task.cpu_id() ≠ 3 — e.g., returned 0, the creation CPU), so CPU 3 never got interrupted; (b) IRQ masking in the guest context (IRQs masked → the physical IRQ stays pending at GIC level until unmasked — no trap).

For (a): the doc's claim! "IPI went to wrong CPU". The doc frames it as "直接调用 inject_interrupt 发生在错误的 host CPU" — the doc's mechanism description is off, but the *IPI targeting* could genuinely be wrong in A: `task.cpu_id()` — hmm wait, actually let me check: in the A worktree's run loop, before entering the guest, does the task migrate to CPU 3? The task was spawned with cpumask [3]. `task.cpu_id()` in ArceOS returns the CPU the task is currently executing on... or the affinity CPU? If the vCPU task is executing guest code on CPU 3, then task.cpu_id() should return 3 (ArceOS sets current task's cpu_id at schedule time on the running CPU — typically). So IPI → CPU 3. Hmm.

But wait — there's a subtlety: the injector runs on CPU 0, `send_ipi(3)` sends physical IPI to CPU 3. CPU 3 is executing the guest (EL1) inside `vcpu.run()`. Physical IRQ arrives → QEMU: does it trap to EL2? The IRQ is a physical GIC IRQ (ArceOS owns the GIC). QEMU's GIC model raises the IRQ to the CPU — with GICv3, IRQ line asserted while EL1 (guest) is running: QEMU checks if the IRQ is a "virtual" IRQ routed to the guest (via LR) or physical. A host IPI is physical → QEMU exits the guest (like KVM's external interrupt) → ArceOS handles it → returns → Continue → drain. This SHOULD work.

Unless the physical IPI never arrived because `send_ipi` targets... `IpiTarget::Other { cpu_id }`. In QEMU GICv3, SGI target cpu_id — if ArceOS's CPU ids don't match the GIC's routing (cpu_id 0..N map to GIC targets)... The doc's §3.1 says "GIC/vCPU 注入发生在错误的 host CPU 上" — hmm, "b4632eaca fix(arm-gic-driver): use hardware CPU targets for GICv2 routing (#1803)" — there were GIC routing bugs!

OK, I've done enough forensics. The point of my analysis is to give the user a sharp assessment of the document. Let me now also glance at the B1-target (passthrough) and B1-emu logs briefly for the "running" semantics confirmation... I already have the counts. 

One more key check for the analysis: In the emulated B log, running=31 but drain=1: were the 31 runs clustered early (boot) or spread out? If the vCPU never slept in the wait queue and never exited the guest after boot, running would stay at ~1 like A1. The fact that running=31 (vs A1's 1) means in emulated B, the guest DID exit the host repeatedly — consistent with WFI exits happening (no timer pending). So the emulated B guest exits on WFI → drain point reached 31 times → only 1 had queue data → so the enqueues happened while the guest was RUNNING (between WFIs) and the notify+IPI didn't cause a drain... but if WFI exits happen and the loop drains at the top, and after the exit the loop CONTINUES (not waits)... 

Actually hmm: WFI exit → run loop → `handle_vcpu_exit_bound` → for WFI, returns `VcpuRunAction { waits_for_event: true }` → vcpu_run() breaks out → `wait(&vm, &runtime, vcpu_id)` → sleeps. Then enqueue → notify_vcpu → wakes → loop → running++ → drain → 65 drains expected. Got 1. So notify_vcpu did NOT wake the sleeping task. WHY?

Look at `notify_vcpu`:
```rust
pub(crate) fn notify_vcpu(&self, vcpu_id: usize) {
    if !self.irq_dispatcher.has_pending(vcpu_id) {
        return;
    }
    self.notify_vcpu_unconditional(vcpu_id);
}
```
This checks has_pending then notifies. Race: vCPU task wakes on another event, drains (empties queue), and between... no wait, that would only MISS a notify, not cause 64 missed. 

Hmm, wait — actually. Let me reconsider: does the vCPU task actually sleep in the wait queue on WFI, or does it `wait()` with a CONDITION? `wait(&vm, &runtime, vcpu_id)` → `wait_vcpu_until(vcpu_id, || has_pending || !vm.running() || ...)`. On wake (notify_one), condition checked. 

OK — the actual failure could be in `WaitQueue::wait_until` semantics (notify_one with condition...). Or — the per-vCPU wait queue used by the vCPU task on WFI... 

Hmm wait, actually — maybe the issue is DIFFERENT: in the emulated run, the guest's WFI exits with a PENDING vIRQ (timer) so `waits_for_event` is false and the loop continues immediately without sleeping; the guest gets the timer IRQ, handles it, re-enters, WFI again, exits... 31 times over 3s. Between exits the queue gets... no wait, each exit hits the drain at loop top — the queue accumulates (10ms period) — drains should happen at each exit. Only 1 drain. So the queue was empty at 30 of the exits?! 

OH WAIT. I bet I know what's happening. The `drain` event is traced ONLY IF the drained list is non-empty (`if !interrupts.is_empty()`)... but the trace `running` is ALWAYS traced at loop top. So drain=1 means only one exit found data. So the 30 other exits found an EMPTY queue. But 65 enqueues happened with only 1 drain — that means 64 entries accumulated in the queue — the LAST exit should have drained ALL 64 at once (drain returns the whole Vec, one Drain event for `interrupts[0]`, then 64 Inject events — but inject=1!). So drains didn't happen after the queue filled. The 30 empty drains happened BEFORE the enqueues started (boot phase, t=0.81s-0.84s?) and then the queue filled and... the vCPU never exited again (in guest, Zephyr running with timer, no WFI exits, no IPI traps).

Timeline: boot WFI exits → 30 loops at startup (before injector's first enqueue at t≈0.84s) → guest settles into running mode with timer ticks → injector starts: enqueue every 10ms, notify (task running in guest, no-op), IPI to CPU 3 (physical IRQ → does it trap? apparently NOT — no more running events!) → queue fills → 64 → overflow 235. Then ONE more exit happened (drain=1, inject=1) — maybe a WFI right at t=0.84s when the first entry was queued.

So: the 31st running+drain+inject happened right at the start of the injector, and after that, NOTHING — no more exits for the remaining ~2.9 seconds. The physical IPI to CPU 3 does NOT cause a guest exit in the emulated mode either!

So the common failure across A1, B-passthrough, B-emulated: **the host IPI sent to the vCPU's pCPU does not cause the guest to exit / does not bring the vCPU back to the host run loop**. The notify only works when the vCPU task is sleeping in the wait queue; once the guest runs continuously (timer keeps it busy), the only exit mechanism is WFI, which the timer prevents. The doc's §3.1 "wrong CPU" claim is dubious; the real issue is the kick mechanism itself.

Actually — for A1, running=1 (vs emulated-B's 31): A1's guest also had a timer running; at t=0.832s the vCPU entered the guest; running=1 total. B-emulated had 31 (boot WFIs). The difference between A1 (1) and B-emu (31) is probably just timing/config noise.

Also note the doc itself flags this in §4 (⚠️ "vCPU 在 host IPI/pending 到达后，能否可靠回到 AxVM host run loop") — the doc's own highlighted conclusion matches my analysis. But the doc's §3.1 A1 root-cause statement ("直接调用...注入发生在错误的 host CPU") is not supported by the evidence: the trace shows the A path staged through the queue (enqueue/notify/ipi on CPU 0) and the failure is the same "never returns to drain" as B.

Hmm — one more consideration for §3.1: could the A1 IPI have targeted the wrong CPU due to task.cpu_id()? The doc says the guest vCPU was bound to host CPU 3 (cpumask [3]). At enqueue time, `runtime.queue_interrupt` returns `task.cpu_id()`. If ArceOS's task.cpu_id() = the affinity CPU (3), IPI → CPU 3. The evidence can't distinguish. But the doc's claim that "host injector 从 host CPU 0 直接调用 vCPU backend 的 inject_interrupt" is definitively FALSE per the trace (there's no direct-call path in A's code at all — `inject_interrupt_to_vcpu` → `queue_interrupt` → pending list + notify + IPI).

So the doc has a significant factual inconsistency in §1/§3.1. THE REAL A/B difference:
- A: unbounded pending_interrupts + VM-wide notify_all + pCPU from task.cpu_id() + injection `vcpu.inject_interrupt(vector)` (edge-only, no trigger)
- B: bounded queue (64) + per-vCPU notify + pCPU from vCPU phys_cpu_set() affinity + injection `vcpu.inject_interrupt_with_trigger(id, trigger)`

Also, one more genuinely useful observation: the stats script's `GUEST_RE` regex matches lines like `sequence,timestamp` — fine.

And the "success criteria" in README — `SOFTWARE VIRQ COMPLETE samples=300` — let me quickly verify the guest prints that. Not needed for analysis.

Another point: A1's VIRQ_INJECT lines all say status=ok (300/300 "ok") — the status only reflects queue push success, not delivery. And the stats script's `injected=300` + `inject_errors=0`... wait the doc says A1 stats: injected=300, matched=0, lost_irq=300. But if status=ok for all, errors=0. Fine.

And B-emu: inject_errors=235 — those are the 235 overflow errors (status=error on overflow). Wait — the injector logs status=error when `inject_interrupt_to_vcpu` returns Err — which for B is the queue-full error. 300 samples: 65 ok + 235 error = 300. Consistent with queue_overflow=235.

Let me now also sanity-check one claim in §2.3: "177 axvm tests" — not critical.

Let me also verify the guest workload prints "SOFTWARE VIRQ FAIL received=0 expected=300" — consistent with logs.

Now — the deliverable. The user asked in Chinese: "你来分析一下这个文档吧" — analyze this document. I should give a structured analysis in Chinese, concise but substantive. Key points:

1. **总体评价**: 文档结构清晰、结论诚实（不虚报数据），定位的核心问题（vCPU 无法可靠回到 host run loop）方向正确。evidence 完整。

2. **关键发现（与代码/日志交叉验证）**:
   a. **§1 对 A 的定义与 A1 证据矛盾**: A 的实际代码路径也是 enqueue→notify→IPI 队列化路径（`queue_interrupt` → pending_interrupts 无限队列 + notify_all + send_ipi(task.cpu_id())），A1 日志中 enqueue=notify=ipi=300 就是铁证。A/B 的真实差异是: 无限队列 vs 有界队列、VM 级 notify vs per-vCPU notify、task.cpu_id() vs affinity mask、`inject_interrupt`（无 trigger） vs `inject_interrupt_with_trigger`。"直接调用 inject_interrupt" 的描述不符合任何一条实际代码路径。
   b. **§3.1 的根因判断证据不足**: A1 的真实现象是 vCPU 在 t=0.832s 进入 guest 后从未再回到 host 循环（running=1 且发生在第一次 enqueue 之前），300 次 IPI/notify 都没有把 vCPU 拉回 drain 点。这与 B 的 passthrough/emulated 失败是同一个机制（kick 失效），而不是"CPU 错位"。而且 A 代码里 send_ipi 目标来自 task.cpu_id()，是"可能错位"，doc 把它说成"从 CPU 0 直接调用"是不准确的。
   c. **B-emulated 数据说明 notify/IPI 都不生效**: running=31（启动阶段 WFI 退出）但 drain=1/inject=1——queue 在 enqueue 开始后累积到 64，期间 vCPU 没有再来过 host 循环，说明物理 IPI 到达目标 pCPU 时既没有导致 guest exit，per-vCPU wait queue notify 也没有唤醒（task 在 guest 内运行时 notify 是 no-op）。唯一的一次 drain 是恰好在 injector 启动瞬间的 WFI 退出碰上的。
   d. 所以 §4 的 ⚠️ 结论是对的，但范围要扩大：不仅 passthrough，emulated 下 host IPI 也不能把 vCPU 踢回 host loop；关键路径是"物理 IPI → guest exit → handle ExternalInterrupt → Continue → loop top drain"，目前 IPI 没有产生 exit。
   e. 实验有效性: A/B 对比的前提（A 是直接调用基线）不成立；即便修复后，A/B 比较的也是"两种队列化方案"而不是"直接调用 vs 队列化"。

3. **对 TODO 的补充建议**:
   - 在 IPI 接收端（目标 CPU 的 IRQ handler）加 trace 事件（如 ipi_received/guest_exit），区分"IPI 没到"、"IPI 到了但没引起 exit"、"exit 了但没 drain"——目前 trace 只有发送端 ipi 事件，无法定位。
   - TODO #2 除了 GIC LR 还要查: 物理 IPI 的 GIC 配置是否被当作 vIRQ 直通给 guest（passthrough 模式下）；以及 ArceOS IPI handler（`dispatch_host_irq`/scheduler）是否会导致 vCPU run loop 退出。
   - 建议先做"自循环"验证: guest 自身周期性发 vIRQ（SendIPI self）或 host 定时器直接驱动 vCPU 任务，先把 notify→drain→inject→guest ISR 的闭环打通，再引入跨 CPU IPI。
   - 小步 smoke 用 1/4/16 次（TODO #4 已有）没问题，但建议同时要求 trace 中出现 ipi_received。

4. 其他小点:
   - stats 脚本 p99=0 的语义已在文档注明，没问题。
   - queue_overflow 计数与 inject_errors 一致（235），说明溢出统计自洽。
   - 文档建议补充: 每次运行的实际 guest 启动时间、injector 首拍时间（A1 中 enqueue 从 t=0.84s 开始，而 vCPU 0.832s 就进了 guest），这些时间戳对判断"是否来得及"很重要。

Should I keep it concise? The instructions say be concise (fewer than 4 lines) unless the user asks for detail. The user asked to "analyze this document" — that's a request for a real analysis, so a more detailed response is justified. But I should still be tight. I'll write a structured Chinese analysis with the key findings, moderate length. Given the depth of the verification I did, a moderately detailed response is appropriate. I'll aim for a compact but complete analysis.

分析完成。我把文档与两份工作树代码和实际日志（`/tmp/ab-*.log`）做了交叉验证，主要结论如下：

## 总体评价

文档结构清晰、结论诚实（不把无效样本当结果），§4 的 ⚠️ 核心判断方向正确：瓶颈在 vCPU 能否回到 host run loop 完成 `drain → inject`。但有两个关键问题：**§1 对 A 的定义与 A1 证据矛盾**，**§3.1 的根因判断与代码路径不符**。

## 关键发现

1. **A 不是"直接调用 inject_interrupt"**。A 工作树（tgoskits-2026）的路径是 `inject_interrupt_to_vcpu → queue_interrupt`（无限 `pending_interrupts` + `notify_all` + `send_ipi(task.cpu_id())`）→ run loop `inject_pending_interrupt`。A1 日志里 `enqueue=notify=ipi=300` 就是铁证——A 也是队列化路径。A/B 的真实差异是：无限队列 vs 有界队列(64)、VM 级 notify vs per-vCPU notify、`task.cpu_id()` vs affinity mask、`inject_interrupt`（无 trigger）vs `inject_interrupt_with_trigger`。

2. **A1 的根因不是"CPU 错位直接调用"**。A1 时间线：vCPU 在 t=0.832s 进 guest（`running` 事件 cpu=3，发生在第一次 enqueue t=0.840s **之前**），此后 300 次 notify/IPI 都没把它拉回 host loop。这与 B 的失败是**同一机制**——kick 失效，而非注入位置错误。文档把 A1 归因于"从 host CPU 0 直接调用导致注入错位"，与 trace 证据矛盾。

3. **B-emulated 数据证明 notify 和 IPI 都不生效**：`running=31` 全部是启动阶段 WFI 退出（drain 时队列为空），队列在 injector 启动后累积到 64（`queue_overflow=235` 与 `inject_errors=235` 自洽），期间 vCPU 再没回来。唯一一次 `drain/inject=1` 是撞上启动瞬间的 WFI 退出。即：vCPU 在 guest 内连续运行时，notify 是 no-op、物理 IPI 没有引起 guest exit。

4. **§4 的 ⚠️ 范围应扩大**：不仅 passthrough，emulated 下 host IPI 同样踢不出 vCPU。关键链路"物理 IPI → ExternalInterrupt exit → loop top drain"当前断在 IPI 不产生 exit 这一环。

## 对下一步的建议

- **在接收端加 trace**：现有 `ipi` 事件只在发送端记录，无法区分"IPI 没送达 / 送达但没引起 guest exit / exit 了但没 drain"。建议加 `ipi_received`（目标 CPU IRQ handler）和 `guest_exit`（exit 原因）事件，这是目前定位手段的最大缺口。
- TODO #2 除 GIC LR 外，要查物理 IPI 是否被 GIC 当作 vIRQ 直通给 guest（passthrough 下最可能），以及 ArceOS IPI handler 是否会导致 vCPU run loop 退出。
- 建议先做自循环打通 notify→drain→inject→guest ISR（guest 自发 IPI 或 host 侧同 CPU 注入），再引入跨 CPU IPI，把变量拆开。

实验有效性上：A/B 对比的前提本身不成立——即便全部修复，比较的也是"两种队列化+通知方案"，不是文档声称的"直接调用 vs 队列化"。建议修正 §1 的 A 定义，否则 §3.1/§4 的结论链条无法自洽。

---

## User

好的，那么接下来我们要怎么做呢？

---

## Assistant (Build · DeepSeek V4 Flash Free · 11.1s)

_Thinking:_

The user asks: "好的，那么接下来我们要怎么做呢？" - "OK, so what should we do next?"

Given my analysis, the key actionable items are:

1. Fix the doc's factual inconsistencies (A definition, A1 root cause) — or rather, the experiment design.
2. The real blocker: vCPU cannot reliably return to host run loop to drain. Need to add receiver-side trace to disambiguate.
3. Establish minimal closed loop in emulated mode first.

The user is asking for the plan. I should propose a concrete plan. Let me think about what's most valuable and actionable, in order of priority:

Option A: Just debug the code to make the closed loop work (the core engineering task).
Option B: Fix the doc first.
Option C: Both.

The core question is what the user wants. Given the context (they're in the middle of an experiment, doc says "下一步 TODO"), the natural next step is to make the minimal closed loop work. My analysis added new info: the real failure is the kick mechanism (IPI doesn't cause guest exit; notify is no-op when vCPU is running in guest).

Let me think about what concrete debugging steps would be most effective:

1. **Add receiver-side trace events** (`ipi_received` in the IPI handler / `guest_exit` with exit reason) — this is the key instrumentation gap. Without it, we're guessing.

2. **Check how ExternalInterrupt exits are handled**: `after_external_interrupt` → `dispatch_host_irq(vector)`. And `handle_vcpu_exit_bound` — need to see if physical IRQ during guest execution returns ExternalInterrupt exit and whether it leads to loop top (drain). Also check IRQ masking: when the guest runs, does ArceOS disable IRQs in the run loop (`vcpu.run()` might run with IRQs masked or not)?

Actually — a key thing to check: in the run loop, does `vcpu.run()` run with physical IRQs enabled? If IRQs are masked during guest execution (some hypervisors mask to avoid being interrupted), the physical IPI would stay pending in the GIC until the guest exits (WFI), then be delivered. That would explain everything: IPI never causes exit, only WFI does.

Let me check the code for this. Actually, before proposing, let me quickly verify a few things to make the plan concrete:
- How does `handle_vcpu_exit_bound` handle ExternalInterrupt?
- Is there IRQ masking around `vcpu.run()`?

Let me do that quickly, then present the plan. The user asked "what do we do next" — I should give a concrete prioritized plan, possibly ask which direction they want (debug loop vs fix doc first). Let me first do a bit more code investigation so my plan is grounded, then present it with a question about scope.

Actually, the user asked a direct question: "接下来我们要怎么做" — they want a plan of action. Let me investigate the exit handling code first to make the plan concrete and actionable.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"ExternalInterrupt\\|Wfi\\|WfiInstruction\\|handle_vcpu_exit\\|waits_for_event\" virtualization/axvm/src/arch/aarch64/mod.rs | head -20"
}
```

**Output:**
```
52:    ExternalInterrupt { vector: usize },
75:    fn handle_vcpu_exit_bound(
127:            ArmVmExit::ExternalInterrupt { vector } => {
130:                    Aarch64DeferredRunWork::ExternalInterrupt {
142:                    waits_for_event: true,
164:                    waits_for_event: false,
188:                waits_for_event: false,
203:            Aarch64DeferredRunWork::ExternalInterrupt { vector } => {
208:            waits_for_event: false,

```

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"after_external_interrupt\\|dispatch_host_irq\\|ipi_irq\\|irq_disable\\|irq_enable\\|local_irq\" virtualization/axvm/src/arch/aarch64/mod.rs virtualization/axvm/src/architecture/ops.rs virtualization/axvm/src/host/arceos.rs | head -20"
}
```

**Output:**
```
virtualization/axvm/src/arch/aarch64/mod.rs:204:                Self::after_external_interrupt(vm, vcpu, vector);
virtualization/axvm/src/architecture/ops.rs:88:    fn after_external_interrupt(
virtualization/axvm/src/architecture/ops.rs:93:        crate::host::arceos::dispatch_host_irq(vector);
virtualization/axvm/src/host/arceos.rs:94:pub(crate) fn dispatch_host_irq(vector: usize) {
virtualization/axvm/src/host/arceos.rs:152:        modules::ax_hal::irq::ipi_irq(),
virtualization/axvm/src/host/arceos.rs:173:        modules::ax_hal::irq::ipi_irq(),

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.0s)

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 100,215p virtualization/axvm/src/arch/aarch64/mod.rs"
}
```

**Output:**
```
                    reg_width: arm_access_width_to_ax(reg_width),
                    signed_ext,
                },
            ),
            ArmVmExit::MmioWrite { addr, width, data } => super::handle_mmio_write::<Self>(
                vm,
                MmioWriteExit {
                    addr: arm_guest_phys_addr_to_ax(addr),
                    width: arm_access_width_to_ax(width),
                    data,
                },
            ),
            ArmVmExit::SysRegRead { addr, reg } => sysreg::handle_read(
                vm,
                vcpu,
                SysRegReadExit {
                    addr: arm_sys_reg_addr_to_ax(addr),
                    reg,
                },
            ),
            ArmVmExit::SysRegWrite { addr, value } => sysreg::handle_write(
                vm,
                SysRegWriteExit {
                    addr: arm_sys_reg_addr_to_ax(addr),
                    value,
                },
            ),
            ArmVmExit::ExternalInterrupt { vector } => {
                debug!("VM[{}] run VCpu[{}] get irq {vector}", vm.id(), vcpu.id());
                Ok(BoundVcpuExit::Defer(
                    Aarch64DeferredRunWork::ExternalInterrupt {
                        vector: vector as usize,
                    },
                ))
            }
            ArmVmExit::CpuDown { state } => {
                warn!(
                    "VM[{}] run VCpu[{}] CpuDown state {state:#x}",
                    vm.id(),
                    vcpu.id()
                );
                Ok(BoundVcpuExit::Complete(VcpuRunAction {
                    waits_for_event: true,
                    stop_reason: None,
                    resets_vm: false,
                    exits_vcpu: false,
                }))
            }
            ArmVmExit::CpuUp {
                target_cpu,
                entry_point,
                arg,
            } => cpu_up::handle::<Self>(
                vm,
                vcpu,
                CpuUpExit {
                    target_cpu,
                    entry_point: arm_guest_phys_addr_to_ax(entry_point),
                    arg,
                },
            ),
            ArmVmExit::SystemDown => {
                warn!("VM[{}] run VCpu[{}] SystemDown", vm.id(), vcpu.id());
                Ok(BoundVcpuExit::Complete(VcpuRunAction {
                    waits_for_event: false,
                    stop_reason: Some(crate::StopReason::SystemDown),
                    resets_vm: false,
                    exits_vcpu: false,
                }))
            }
            ArmVmExit::SendIPI {
                target_cpu,
                target_cpu_aux,
                send_to_all,
                send_to_self,
                vector,
            } => ipi::handle(
                vm,
                vcpu.id(),
                SendIpiExit {
                    target_cpu,
                    target_cpu_aux,
                    send_to_all,
                    send_to_self,
                    vector,
                },
            ),
            ArmVmExit::Nothing => Ok(BoundVcpuExit::Complete(VcpuRunAction {
                waits_for_event: false,
                stop_reason: None,
                resets_vm: false,
                exits_vcpu: false,
            })),
            _ => ax_err!(Unsupported, "unsupported AArch64 VM exit"),
        }
    }

    fn finish_deferred_run_work(
        vm: &crate::AxVMRef,
        vcpu: &crate::vm::AxVCpuRef<Self::VCpu>,
        work: Self::DeferredRunWork,
    ) -> AxVmResult<VcpuRunAction> {
        match work {
            Aarch64DeferredRunWork::ExternalInterrupt { vector } => {
                Self::after_external_interrupt(vm, vcpu, vector);
            }
        }
        Ok(VcpuRunAction {
            waits_for_event: false,
            stop_reason: None,
            resets_vm: false,
            exits_vcpu: false,
        })
    }
}


```

---

## Assistant (Build · DeepSeek V4 Flash Free · 7.7s)

_Thinking:_

So when the guest runs and a physical IRQ arrives, `vcpu.run()` returns `ExternalInterrupt`, which is `Defer`d — `finish_deferred_run_work` → `after_external_interrupt` → `dispatch_host_irq(vector)` → then returns `VcpuRunAction { waits_for_event: false, ... }` → the run loop continues → top of loop → `drain_and_inject_dispatched_interrupts`!

So IF the physical IPI causes an ExternalInterrupt exit, the drain SHOULD happen. The fact that it doesn't (running=1 in A1, no drain in B) means **the physical IPI does not cause the guest to exit at all**.

Why? Two main hypotheses:
1. **IRQs masked during guest execution**: if the hypervisor runs `vcpu.run()` with physical IRQs disabled (PSTATE.I or DAIF mask / GIC unmasked?), the IPI stays pending at the GIC and is delivered when... IRQs get unmasked (e.g., when the host later enables IRQs outside guest context) or when the guest exits for another reason. QEMU: a pending physical IRQ while guest runs — if the CPU's IRQ mask is set, QEMU won't deliver; the IRQ line stays asserted. When the guest exits for another reason (WFI), the host handles the pending IRQ. But WFI in Zephyr with a pending timer vIRQ → returns immediately... So the vCPU never exits. That matches!

2. **The IPI is routed to the guest as a vIRQ** (passthrough GIC config) — for passthrough mode B, plausible.

Let me check whether ArceOS runs the guest with IRQs enabled or disabled. Search for how vcpu.run is called — the run loop in ops.rs `run_vcpu_bound`... Also check `enable_irqs`/`disable_irqs` around it, or the hal's `vcpu.run`. Let me check the irq state handling.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"disable_irqs\\|enable_irqs\\|irq_disable\\|irq_enable\\|save_irq\\|restore_irq\\|daif\\|DAIF\" virtualization/axvm/src/arch/aarch64/*.rs virtualization/axvm/src/architecture/ops.rs modules/axhal/src/arch/aarch64/ 2>/dev/null | head; echo ===; grep -rn \"fn run\\b\\|vcpu.run\\|pending_irq\\|enable_irq\" modules/axhal/src/arch/aarch64/vcpu*.rs 2>/dev/null | head"
}
```

**Output:**
```
===

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.2s)

**Tool: bash**

**Input:**
```json
{
  "command": "grep -rn \"fn run\" modules/axhal/src/arch/aarch64/ --include=*.rs | grep -i vcpu; ls modules/axhal/src/arch/aarch64/ | head -30; echo ===; grep -rn \"irq_disable\\|irq_enable\\|local_irq\" virtualization/axvm/src/arch/aarch64/ virtualization/axvm/src/architecture/ | head"
}
```

**Output:**
```
grep: modules/axhal/src/arch/aarch64/: 没有那个文件或目录
ls: 无法访问 'modules/axhal/src/arch/aarch64/': 没有那个文件或目录
===

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.7s)

_Thinking:_

The modules directory doesn't exist in this worktree (probably submodules). Let me check the axvcpu-related code — where vcpu.run() is implemented for aarch64 (the VCPU trait in the hal, or in arm_vcpu crate).

**Tool: bash**

**Input:**
```json
{
  "command": "ls modules 2>/dev/null; grep -rn \"irq_mask\\|IRQ_MASK\\|daif\" os/axvisor/src/ virtualization/axvm/src/arch/aarch64/ --include=*.rs | head; echo ===; grep -rn \"fn run\" virtualization/axvm/src/arch/aarch64/vcpu.rs 2>/dev/null | head; grep -rln \"ArmVcpu\" virtualization/axvm/src/arch/aarch64/"
}
```

**Output:**
```
===
virtualization/axvm/src/arch/aarch64/vm.rs
virtualization/axvm/src/arch/aarch64/mod.rs

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 4.0s)

**Tool: bash**

**Input:**
```json
{
  "command": "grep -rn \"extern crate\\|use arm_vcpu\\|arm_vcpu::\" virtualization/axvm/src/arch/aarch64/mod.rs | head -5; grep -n \"ArmVcpu\" Cargo.toml os/axvisor/Cargo.toml 2>/dev/null | head; cargo tree -p axvm -i arm_vcpu 2>/dev/null | head -5"
}
```

**Output:**
```
10:use arm_vcpu::{
64:        arm_vcpu::has_hardware_support()

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 9.0s)

**Tool: bash**

**Input:**
```json
{
  "command": "find / -path /proc -prune -o -name \"*.rs\" -path \"*arm_vcpu*\" -print 2>/dev/null | head -5; grep -n \"arm_vcpu\" Cargo.toml | head -3"
}
```

**Output:**
```
/home/huhu/tgoskits/virtualization/arm_vcpu/src/pcpu.rs
/home/huhu/tgoskits/virtualization/arm_vcpu/src/lib.rs
/home/huhu/tgoskits/virtualization/arm_vcpu/src/host.rs
/home/huhu/tgoskits/virtualization/arm_vcpu/src/types.rs
/home/huhu/tgoskits/virtualization/arm_vcpu/src/smc.rs
136:arm_vcpu = { version = "0.5.18", path = "virtualization/arm_vcpu" }

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 3.0s)

**Tool: grep**

**Input:**
```json
{
  "pattern": "fn run|irq_enable|irq_disable|pending_irq|daif|DAIF",
  "path": "/home/huhu/tgoskits/virtualization/arm_vcpu/src"
}
```

**Output:**
```
Found 5 matches
/home/huhu/tgoskits/virtualization/arm_vcpu/src/vcpu.rs:
  Line 173:     pub fn run(&mut self) -> ArmVcpuResult<ArmVmExit> {

  Line 175:             core::arch::asm!("msr daifset, #2");

  Line 187:             core::arch::asm!("msr daifclr, #2");

  Line 298:     unsafe extern "C" fn run_guest(&mut self) -> usize {

  Line 339:     unsafe fn run_guest_panic() -> ! {

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 2.8s)

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 160,230p virtualization/arm_vcpu/src/vcpu.rs"
}
```

**Output:**
```
    pub fn set_nested_page_table(&mut self, config: ArmNestedPagingConfig) -> ArmVcpuResult {
        debug!("set vcpu stage-2 root:{:#x}", config.root_paddr);
        self.guest_system_regs.vttbr_el2 = config.root_paddr as u64;
        let pa_bits = if config.mode == 0 {
            pa_bits()
        } else {
            config.mode
        };
        self.guest_system_regs.vtcr_el2 = vtcr_for_config(config.levels, config.gpa_bits, pa_bits);
        Ok(())
    }

    /// Runs the vCPU until a VM exit.
    pub fn run(&mut self) -> ArmVcpuResult<ArmVmExit> {
        unsafe {
            core::arch::asm!("msr daifset, #2");
        }

        let exit_reason = unsafe {
            self.restore_vm_system_regs();
            self.run_guest()
        };

        let trap_kind = TrapKind::try_from(exit_reason as u8).expect("Invalid TrapKind");
        let result = self.vmexit_handler(trap_kind);

        unsafe {
            core::arch::asm!("msr daifclr, #2");
        }

        result
    }

    /// Binds this vCPU to the current physical CPU.
    pub fn bind(&mut self) -> ArmVcpuResult {
        Ok(())
    }

    /// Unbinds this vCPU from the current physical CPU.
    pub fn unbind(&mut self) -> ArmVcpuResult {
        Ok(())
    }

    /// Sets a general-purpose register.
    pub fn set_gpr(&mut self, idx: usize, val: usize) {
        self.ctx.set_gpr(idx, val);
    }

    /// Injects an interrupt into the guest vCPU.
    pub fn inject_interrupt(&mut self, vector: usize) -> ArmVcpuResult {
        H::inject_virtual_interrupt(vector as u8)
    }

    /// Sets the guest return value.
    pub fn set_return_value(&mut self, val: usize) {
        // Return value is stored in x0.
        self.ctx.set_argument(val);
    }
}

// Private function
impl<H: ArmHostOps> ArmVcpu<H> {
    fn init_hv(&mut self, config: ArmVcpuSetupConfig) {
        self.ctx.spsr = (SPSR_EL1::M::EL1h
            + SPSR_EL1::I::Masked
            + SPSR_EL1::F::Masked
            + SPSR_EL1::A::Masked
            + SPSR_EL1::D::Masked)
            .value;
        self.init_vm_context(config);
    }

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 47.7s)

_Thinking:_

FOUND IT! This is the smoking gun:

```rust
pub fn run(&mut self) -> ArmVcpuResult<ArmVmExit> {
    unsafe {
        core::arch::asm!("msr daifset, #2");   // disable ALL IRQs (I bit) + F + A + D
    }
    let exit_reason = unsafe {
        self.restore_vm_system_regs();
        self.run_guest()   // <-- enters guest with DAIF.I SET (IRQs masked)
    };
    ...
    unsafe {
        core::arch::asm!("msr daifclr, #2");   // re-enable after exit
    }
    result
}
```

The hypervisor runs the guest with **physical IRQs masked (DAIF.I = 1)**. So when the injector on CPU 0 sends a physical IPI to CPU 3, the IPI cannot interrupt the hypervisor while it's in the guest — the IRQ stays pending at the GIC. The guest continues running. No ExternalInterrupt exit, no drain.

The IRQ is delivered only when:
1. The guest exits for another reason (WFI, MMIO, etc.) → `daifclr` → pending IPI fires in the exit path → `dispatch_host_irq` → handled → but by then the run loop continues... wait, after the exit, the IPI is dispatched during `after_external_interrupt`... hmm, actually the IPI is dispatched when the IRQ is unmasked after run_guest returns. The handler runs `dispatch_host_irq` → the IPI handler → likely does scheduler stuff. Then the loop continues → drain! So an exit for ANY reason should drain the queue...

Hmm wait — but then why drain=0? Let me reconsider: after `run()` returns (WFI exit), `daifclr` → pending IPI delivered → IPI handler → returns → loop top → drain_and_inject → drains! So in emulated B, each WFI exit should have drained... but only 1 drain happened with 31 runs.

Hmm, so that means most WFI exits happened BEFORE any enqueue. After the injector started, the guest stopped exiting. Why would the guest stop exiting? Because... Zephyr's tick timer! Before the injector starts, the guest idles on WFI → exits → sleeps in host wait queue → wakes on... hmm, but running=31 — those would be the WFI wake cycles during boot. Once Zephyr's tick timer is up and running, the guest's WFI doesn't exit (pending vIRQ from the emulated GIC timer → WFI returns immediately inside the guest) → guest never exits again → queue fills → overflow.

That's consistent! The 31 runs = boot phase (guest starting up, WFI exits, wait queue sleeps). Then the guest settles into "timer keeps WFI from exiting" state. From then on: enqueue → notify (task running in guest → no-op) → IPI to CPU 3 (masked by DAIF.I → pending, never delivered until guest exit) → guest never exits → no drain → overflow.

Then the single drain=1/inject=1: the guest did exit once more (why? maybe a WFI during some window, or the first timer... whatever) — and at that exit, the queue had 1 entry (the first one at t=0.84s — the exit probably happened right at the start of the injector).

So THE ROOT CAUSE of the whole A/B failure is now clear and consistent with ALL evidence (A1 running=1, B-passthrough running=1 drain=0 overflow, B-emulated running=31 drain=1):

**The physical IPI can never wake the vCPU because `ArmVcpu::run()` runs the guest with DAIF.I masked. The IPI stays pending at the GIC until the guest happens to exit. A guest that runs continuously (Zephyr timer preventing WFI exits) never exits, so the queue never drains.**

The doc's TODO #3 "检查 ArmVcpu::run() 对 ExternalInterrupt/WFI exit 的处理" — the check has now been done, and the answer is: `run()` masks IRQs during guest execution (`msr daifset, #2`), so physical IPIs are never delivered while the guest runs. 

Wait — but hmm, that seems like a fundamental design issue. How does the hypervisor ever get external interrupts while running a guest? The masked period is the whole guest execution. External interrupts are only handled on guest exits. That means any host IRQ during guest execution is deferred until the guest exits. That's a known design choice in some simple hypervisors (delayed IRQ delivery), but it breaks the "IPI kick" mechanism entirely.

Hmm, but wait — QEMU's GIC emulation: if IRQs are masked in the CPU state (DAIF.I), QEMU won't deliver the IRQ; it stays pending. When the guest exits (WFI etc.) and the hypervisor clears DAIF.I, the pending IRQ fires immediately in the exit path. So the delivery is deferred — matches.

So the fix options for B:
1. **Unmask IRQs during guest run** (daifclr before run_guest): then the physical IPI would trap the guest immediately → ExternalInterrupt exit → dispatch_host_irq → IPI handler → loop continues → drain. This is the "real" kick. Risk: interrupts at arbitrary points in the run loop, correctness issues — but this is what KVM-style hypervisors do (with proper handling). The code already handles ExternalInterrupt exits (Defer → after_external_interrupt → dispatch_host_irq). Hmm — but the arm_vcpu run() sets daifset and then clr after exit — if IRQs were unmasked, run_guest would be interrupted while in the guest → return from run_guest with... what exit reason? With QEMU, the EL2 trap → QEMU exit → ExternalInterrupt. The arm_vcpu's run_guest handles trap kinds — ExternalInterrupt is a valid trap kind (ArmVmExit::ExternalInterrupt exists). So unmasking might just work: interrupt in guest → exit ExternalInterrupt → dispatch → continue → drain.

   But careful: unmasking IRQs inside run() means the IRQ can arrive while the hypervisor code (not guest) is executing — e.g., between daifclr... it's already masked during the whole run. The unmask would need to happen right before run_guest and the IRQ fires during guest execution only... Actually `daifclr` then `run_guest()` — there's a tiny window where the hypervisor runs with IRQs enabled (restore_vm_system_regs, the eret) — the IRQ could fire during `restore_vm_system_regs` — that's the classic "interrupt during guest entry" problem. KVM handles this with proper irq-disabled entry sequence. For QEMU TCG this is simpler.

   Actually — maybe simpler: unmask IRQs only while in guest mode? The `msr daifclr` inside run() before run_guest... The IRQ fires only when the CPU is in EL2 with IRQs unmasked and guest in EL1 — QEMU models: IRQ delivery to a CPU running guest → exits guest → TrapKind::ExternalInterrupt. Fine.

2. **Don't rely on IPI kick at all — rely on the wait queue**: the problem is the vCPU task is NOT in the wait queue when the guest runs (it's inside run()). The notify is a no-op. To make the notify effective, the run loop must be interruptible — which requires IRQs unmasked (option 1) — or the guest must exit periodically (it doesn't).

3. **Alternative: use a timer/self-IPI in the guest?** No — that changes the experiment.

So the fix is essentially: make the guest execution interruptible by the IPI, i.e., run with IRQs unmasked, OR explicitly unmask just before guest entry and treat ExternalInterrupt properly (which is already handled!). This is a small, surgical change in arm_vcpu's run() — but it's a HOST-WIDE behavior change (affects all guests, interrupt latency semantics) — needs careful review.

Hmm wait, actually — is that right? Let me double-check the current semantics: `run()` does `daifset` → `run_guest()` → ... → `daifclr`. During run_guest (guest mode EL1), IRQs are masked → physical IRQs deferred. But wait, does QEMU even deliver physical IRQs to a guest-mode CPU when IRQ is masked? The IRQ line stays asserted in the GIC; when the CPU next unmasks IRQs (daifclr after exit), the IRQ fires → ExternalInterrupt handler in the hypervisor → dispatch_host_irq.

So currently: guest runs with IRQs fully masked. The ONLY way the vCPU returns to the run loop is a guest-initiated exit (WFI, MMIO, sysreg, etc.). WFI is the guest idle exit — but Zephyr's WFI doesn't exit when a vIRQ is pending (timer). So a busy/idle-but-timer-driven guest never exits. → queue never drains → overflow. → matches ALL evidence.

So my conclusion is solid: **root cause = guest execution runs with physical IRQs masked (DAIF.I=1 in `ArmVcpu::run()`), so the host IPI kick can never preempt the guest; drain only happens on guest exits, which don't occur in steady state.**

Now the doc's TODO list:
1. "在 emulated GIC 下先建立最小可用闭环" — the minimal closed loop requires the kick to work — which requires IRQ unmasking.
2. "GIC LR Group 设置..." — secondary; there's no point debugging LR injection until the drain happens reliably.
3. "检查 ArmVcpu::run() 对 ExternalInterrupt/WFI exit 的处理" — DONE: run() masks DAIF.I; ExternalInterrupt is handled (Defer → dispatch_host_irq → Continue → drain) — but never triggered because masked.
4. Small smoke counts — fine.
5. Closed-loop signals — after fix.

So next steps:

**Step 1 (根因确认)**: 在 guest 运行期间收到物理 IPI 后不产生 exit 的直接证据。I can verify by reading QEMU... or by an experiment: unmask IRQs in run() and see if B works. Actually a cleaner first experiment: trace `guest_exit` events in the run loop — count exits with reasons after injector start. If exits stop after ~t=0.85s, confirms the theory. But we already have evidence: running=31 all early (need to verify timing — let me check the timestamps of running events in B1-emu log!).

Let me check the timestamps of the 31 running events to confirm they're all before/around the injector start.

**Step 2 (修复)**: modify `ArmVcpu::run()` (or add a config/API) to unmask IRQs during guest execution, so the physical IPI triggers ExternalInterrupt exit → existing path dispatches IPI → loop top drains. This is a cross-kernel-driver-ish / arch change — actually it's axvm + arm_vcpu — arch-platform-porting skill territory. The change is small but has system-wide IRQ semantics implications (this is a design decision: interrupt delivery to hypervisor while guest runs).

Hmm wait, actually, let me think again about whether unmasking is safe/correct here. In run(): `msr daifset` then `restore_vm_system_regs(); run_guest()`. If we change to keep IRQs unmasked (or unmask right before run_guest), then when the physical IPI arrives during guest execution:
- QEMU: guest-mode EL1 with pending IRQ → guest exit (like KVM external interrupt) → back to EL2, exit reason ExternalInterrupt → vmexit_handler → ArmVmExit::ExternalInterrupt → run loop Defer → after_external_interrupt → dispatch_host_irq(vector) → IPI handler → Continue → loop top → drain_and_inject → re-enter guest. 

The race: the IRQ might fire while in EL2 code (between daifclr and eret, or in vmexit_handler). Since we'd unmask only... hmm, the `daifclr` in run() currently happens AFTER vmexit_handler. To get the effect we need, the IRQs must be unmasked while in the guest. Two approaches:
a. `daifclr` before run_guest and mask again immediately on exit (the current code's ordering reversed): but then the exit path (vmexit_handler) runs with IRQs enabled — interrupts during exit handling — needs the handler to be re-entrant/safe; the subsequent `daifset` prevents re-entrance during handling... The current code masks at entry and unmasks after handling — which is exactly "IRQs off during guest + during handling, on outside". To fix, we want: IRQs OFF during entry sequence, ON in guest, OFF on exit, ON after. That's `daifset; restore; daifclr; eret` — the eret goes to EL1... IRQ state in EL1 is governed by PSTATE.I (SPSR) — hmm, actually in QEMU, "guest IRQ" delivery for a physical IRQ: when the CPU is in EL1 (guest) and IRQ line asserted → since EL1's PSTATE.I is clear (guest's own masking), the CPU traps to EL2 (or delivers to EL1 if...). In a virtualized setting, physical IRQ while guest runs → trap to EL2 (the hypervisor decides). With QEMU TCG + GIC emulation: physical IRQ → guest exit → ExternalInterrupt. Yes.

So the change: in run(), do `daifclr` before run_guest instead of after vmexit_handler... but then between `daifclr` and `eret` (still in EL2 executing restore/eret), an IRQ could fire → that's OK-ish? It would deliver an IRQ while in EL2 — the EL2 vector → whatever ArceOS's EL2 IRQ handling is... it may not be set up for "IRQ during hypervisor execution outside run()" — actually ArceOS does handle IRQs at EL2 normally (the hypervisor itself is interrupt-driven — IPIs, timers... wait, but then how does ArceOS handle its own interrupts if the vCPU task runs with DAIF.I set? Because the vCPU task is just a task like any other! ArceOS is an SMP OS; the vCPU task on CPU 3 runs with IRQs masked for the whole guest run — the OTHER tasks get IRQs normally. The daifset/daifclr is per-task context: it masks IRQs for the vCPU task's execution. Hmm wait, `msr daifset, #2` sets DAIF.I (bit 2 = I? Actually DAIF bits: D=9, A=8, I=7, F=6 — the immediate #2 means I bit. Yes #2 → I mask). So during run_guest, IRQs masked.

Hmm wait, actually — does ArceOS even receive the IPI on CPU 3? If the vCPU task has IRQs masked (DAIF.I=1) while running the guest, and QEMU delivers the physical IRQ → QEMU exits the guest → EL2 IRQ handler... The trap goes to the EL2 vector table. What does ArceOS do there? The IRQ is pending; the EL2 handler (run_guest_panic? or normal IRQ handling at EL2)... In QEMU with the IRQ masked, QEMU does NOT exit the guest — the IRQ is simply not delivered until unmasked. Right — masked IRQs don't cause exits at all in QEMU (the GIC line stays asserted; when unmasked, delivery happens). So no exit, no trap, the IPI just waits.

OK so the fix is clear. And importantly — this changes IRQ delivery semantics for ALL guests of this hypervisor, which per AGENTS.md is arch/platform territory (arch-platform-porting skill) and needs design review. For the experiment, maybe gate it behind the experiment feature or a config.

Alternative fix within the experiment's scope (less invasive): **have the injector run on the vCPU's pCPU** (CPU 3) — no, the whole point of B is cross-CPU.

Alternative: instead of physical IPI, use the **timer** or... no.

Alternative: **make the guest exit**: e.g., set an emulated vIRQ that the guest... no, doesn't cause exit either.

Actually — hmm, one more alternative that doesn't touch the IRQ masking design: **the vCPU task could poll the queue on a host timer interrupt** — no, same masking problem.

Or: **the wait-queue notify works only when the task is asleep**. What if the run loop, instead of running the guest indefinitely, exits the guest periodically? No — the run loop is driven by guest exits.

So the correct fix is: allow the IPI to interrupt guest execution (unmask DAIF.I around guest entry). This is exactly what real hypervisors do. Then:
- B path: enqueue → notify (maybe no-op) → IPI → guest exit ExternalInterrupt → dispatch_host_irq (IPI handler acks) → loop top → drain → inject → re-enter. 
- The IPI handler needs to be tolerant: `dispatch_host_irq(vector)` — for the IPI vector, ArceOS's IPI handler runs scheduler-related logic. Fine.

Wait — but does the ExternalInterrupt exit path handle the *pending IPI* correctly? When QEMU exits the guest due to the physical IRQ, run_guest returns ExternalInterrupt → daifclr → IRQ fires immediately (delivered to EL2 handler) → ... hmm, actually there's a subtlety: after run_guest returns with ExternalInterrupt, the code does `msr daifclr` → the pending IRQ is delivered → EL2 vector → ArceOS IRQ handling → dispatch_host_irq(vector) — wait no, that's done explicitly in after_external_interrupt via the vmexit path, not via the EL2 vector. In QEMU: the IRQ is already consumed as the exit reason (ExternalInterrupt vector passed back) — QEMU's GIC delivers the IRQ as the exit reason; the IRQ is considered handled? Then daifclr → no more pending IRQ → after_external_interrupt → dispatch_host_irq(vector) → the IPI handler runs → done. So no double-delivery. OK good.

So the fix is basically: change `ArmVcpu::run()` to unmask IRQs during guest execution. But this affects ALL uses of the vCPU — that's a significant semantic change; the current design deliberately masks IRQs during guest run (maybe to avoid re-entrancy issues in the run loop). For the experiment, we could:
- Option A: change `run()` unconditionally (arch-wide fix — the "right" design per KVM).
- Option B: gate behind a feature/config for the experiment.
- Option C: add a `kick` API: when the injector wants to kick, it writes... no, IPI is the kick.

Hmm, but wait — there's another consideration. Why was DAIF.I set during run_guest in the first place? Maybe because the vCPU task is a normal task and preemption... Actually, hmm — WAIT. Let me reconsider. Is it possible that IRQs are masked during guest execution because ArceOS relies on the WFI exits and handles IRQs only between guest runs (a "lazy" hypervisor)? If we unmask, we need the EL2 IRQ path to be safe with arbitrary code running... The run loop code after run() returns — daifclr happens right after run_guest returns and vmexit_handler. If an IRQ fires during vmexit_handler (EL2), it goes to the EL2 vector — which ArceOS has set up (the OS runs at EL2 and handles IRQs normally — interrupts while running arbitrary OS code are the NORMAL case!). The vCPU task's run() is the exception (masked). So unmasking during guest execution is actually the norm-compatible behavior. The concern is only the tiny window between daifclr and eret (entry sequence) — an IRQ could fire while still in EL2 entry code → delivered to EL2 vector → fine (ArceOS handles IRQs at EL2 anytime).

So Option A is safe-ish and matches how ArceOS works elsewhere. This is an arch change — per AGENTS.md I should follow the arch-platform-porting skill for arch changes. But the user hasn't asked me to implement yet — they asked "what should we do next". 

Let me now verify the B1-emu running-event timestamps to confirm the "all exits early" theory before proposing. Then present:

1. 根因 (with the evidence chain)
2. 修复方案 (unmask IRQs in ArmVcpu::run — with the safety analysis, maybe gated)
3. Then re-run smoke → verify loop → then A/B with correct definitions.

Also the doc fix (A definition) can be folded in later.

Let me check the emu log timestamps quickly.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -o \"ts_ns=[0-9]* cpu=[0-9]* vm=2 vcpu=0 event=running\" /tmp/ab-B1-emu.log | head -3; grep -o \"ts_ns=[0-9]* cpu=[0-9]* vm=2 vcpu=0 event=running\" /tmp/ab-B1-emu.log | tail -3; echo \"--- running 事件时间戳分布（相对首个 enqueue）:\"; grep -o \"ts_ns=[0-9]*\" /tmp/ab-B1-emu.log | head -1; grep -o \"event=running\" /tmp/ab-B1-emu.log | wc -l; echo ---; grep -o \"ts_ns=[0-9]* cpu=[0-9]* vm=2 vcpu=0 event=enqueue\" /tmp/ab-B1-emu.log | head -1; grep -o \"ts_ns=[0-9]* cpu=[0-9]* vm=2 vcpu=0 event=\\(running\\|drain\\|inject\\)\" /tmp/ab-B1-emu.log"
}
```

**Output:**
```
ts_ns=811618992 cpu=3 vm=2 vcpu=0 event=running
ts_ns=814124256 cpu=3 vm=2 vcpu=0 event=running
ts_ns=817155632 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823742496 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823768992 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823809840 cpu=3 vm=2 vcpu=0 event=running
--- running 事件时间戳分布（相对首个 enqueue）:
ts_ns=811618992
31
---
ts_ns=818941056 cpu=0 vm=2 vcpu=0 event=enqueue
ts_ns=811618992 cpu=3 vm=2 vcpu=0 event=running
ts_ns=814124256 cpu=3 vm=2 vcpu=0 event=running
ts_ns=817155632 cpu=3 vm=2 vcpu=0 event=running
ts_ns=817286336 cpu=3 vm=2 vcpu=0 event=running
ts_ns=817415696 cpu=3 vm=2 vcpu=0 event=running
ts_ns=817995840 cpu=3 vm=2 vcpu=0 event=running
ts_ns=818119216 cpu=3 vm=2 vcpu=0 event=running
ts_ns=819275120 cpu=3 vm=2 vcpu=0 event=running
ts_ns=819355216 cpu=3 vm=2 vcpu=0 event=drain
ts_ns=819383872 cpu=3 vm=2 vcpu=0 event=inject
ts_ns=820616144 cpu=3 vm=2 vcpu=0 event=running
ts_ns=820851808 cpu=3 vm=2 vcpu=0 event=running
ts_ns=820910416 cpu=3 vm=2 vcpu=0 event=running
ts_ns=820998896 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821304032 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821401104 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821434928 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821491920 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821528960 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821573552 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821599840 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821639408 cpu=3 vm=2 vcpu=0 event=running
ts_ns=821853456 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823393744 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823451776 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823545920 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823596720 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823623616 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823663296 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823699584 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823742496 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823768992 cpu=3 vm=2 vcpu=0 event=running
ts_ns=823809840 cpu=3 vm=2 vcpu=0 event=running

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 34.9s)

_Thinking:_

CONFIRMED. The evidence is now conclusive:

- All 31 `running` events (vCPU exits / run-loop iterations) happened between **ts=811.6ms and 823.8ms** — a ~12ms window during guest startup.
- The first enqueue happened at **ts=818.9ms** — the last 12 running events happened at 820.6–823.8ms, i.e., 5 running events AFTER the first enqueue — but only ONE drain (819.3ms) found data.
- After **ts=823.8ms** (≈ 5ms after the first enqueue), **the guest never exits again**. The queue fills to 64 and overflows 235 times over the remaining ~2.2 seconds.

So: the guest boot phase generates WFI exits for ~12ms; once the guest settles (Zephyr timer running → WFI no longer exits), the vCPU NEVER returns to the host. Physical IRQs (the IPI) cannot preempt the guest because `ArmVcpu::run()` runs the guest with `DAIF.I` masked (`msr daifset, #2` at virtualization/arm_vcpu/src/vcpu.rs:175).

Wait — one more check: after ts=823.8ms, the queue had 64 pending and the injector kept enqueueing with overflow. The notify events — did they happen? Yes: notify=65 all from CPU 0. The per-vCPU wait queue notify is a no-op because the task is running the guest, not sleeping. The IPI is masked. So nothing wakes the vCPU.

The drain=1 at 819.3ms was a lucky WFI exit right after the first enqueue (818.9ms).

And A1: running=1 — the vCPU entered the guest at 832.4ms and after the first enqueue at 840.2ms never exited. In A1 the boot phase apparently didn't produce WFI exits (different GIC mode/config — A1 probably had a busier startup or the guest ran with timer earlier).

This is the definitive root cause. The single fix that makes B work: **the physical IPI must be able to interrupt the guest execution** — i.e., run the guest with physical IRQs unmasked (or unmask immediately before guest entry), so the IPI generates an ExternalInterrupt exit → existing `after_external_interrupt` → `dispatch_host_irq` → run loop continues → `drain_and_inject_dispatched_interrupts` → inject.

Now let me formulate the next-step plan. The user asked "接下来我们要怎么做". I should present:

1. Root cause (with the evidence chain I've built).
2. The fix (in arm_vcpu run(): unmask DAIF.I during guest execution) — with caveats about the IRQ-delivery semantic change and where to gate it.
3. Verification plan (small smoke: 1/4/16 samples, then 300).
4. Then re-run A1→B1→A2→B2 and update the doc (A definition fix).

Since this is an arch-level change, AGENTS.md says to use the arch-platform-porting skill when changing architecture boot logic, SMP, etc. This is arm_vcpu run/IRQ masking — that's arch platform territory. I should probably load the skill before implementing. But the user only asked "what should we do next" — I should propose the plan and ask for confirmation, or just present the plan concisely.

Let me present the plan concisely and ask whether to proceed. Actually per the guidance — "If the user asks you how to approach something, you should do your best to answer their question first, and not immediately jump into taking actions." So present the plan, ask if they want me to start.

The plan:

**Step 0 (根因已定位)** — evidence summary.

**Step 1: 验证根因（最小改动冒烟）**
- 在 `ArmVcpu::run()` 的 guest 运行期间打开物理 IRQ（`msr daifclr, #2` 移到 run_guest 之前），或按实验 feature gating。
- Actually, to be careful: modify so that IRQs are unmasked during guest execution. Then B emulated smoke → expect drain≈65+, guest receives some vIRQs.

Hmm wait — should we unmask for real or is there a subtlety about QEMU delivering the IRQ? With TCG/QEMU, when guest runs with PSTATE.I clear, and a physical IRQ is asserted by the emulated GIC, QEMU forces a guest exit (external interrupt). The exit reason ExternalInterrupt → handled. Good.

But one concern: the vCPU task in ArceOS — `daifclr` during the whole guest run means the task is preemptible by... IRQs are fine. Preemption by scheduler? ArceOS scheduler preemption happens on IRQ/timer — the timer IRQ would now also interrupt the guest... the ArceOS timer tick (preemptive scheduling) — if the vCPU task gets preempted by the scheduler mid-guest, then... the run loop is just a task; preemption switches to another task; when the vCPU task resumes, it continues the run loop (the guest state is saved). The run() call doesn't atomically hold anything. Actually that's already possible today?? No — today IRQs are masked during run(), so the vCPU task runs to completion of run() without preemption. If we unmask, the vCPU task could be preempted by the scheduler tick — switching to another task on the same CPU — and the IPI kick would arrive, the scheduler switches... Hmm, actually the IPI handler in ArceOS: on receiving IPI, typically marks tasks ready and triggers a schedule. The vCPU task isn't ready-waiting; it's running. The IPI would just be acked. The run loop continues. OK.

But preemption mid-guest introduces new latency jitter into the experiment itself... The ArceOS tick might be configured at some period (10ms?). The IPI interrupts the guest → exit → drain → inject → re-enter guest. The tick timer could also interrupt → exit → dispatch timer → continue. Both are external interrupts; fine.

Alternative safer fix: keep IRQs masked for normal operation, but have the IPI specifically... you can't selectively unmask the IPI without unmasking all IRQs (DAIF is a mask for all IRQs). So the design decision: either (a) guests run with IRQs unmasked (standard hypervisor behavior, KVM-style), or (b) some other kick mechanism.

(b) alternatives:
- Scheduler-level: IPI handler wakes the vCPU task... but the task isn't sleeping. If the IPI handler could FORCE a yield/preempt the vCPU task — preemption requires IRQ delivery to the CPU — but IRQs are masked! Dead end — any host-interrupt-based kick requires IRQs unmasked.
- Guest-side: have the guest poll? No.
- So (a) is the way. And actually with QEMU, IRQ masking during guest is just delaying; real hardware also requires unmasked IRQs for IPIs to work.

So the fix: unmask DAIF.I during guest execution in arm_vcpu::run(). Actually, better placement: the current code masks IRQs for the WHOLE run() (including vmexit_handler and restore). The minimal-risk change: 
```rust
pub fn run(&mut self) -> ArmVcpuResult<ArmVmExit> {
    unsafe { core::arch::asm!("msr daifset, #2"); }   // hmm, actually maybe keep
    ...
}
```
The change: after entering... hmm. The sequence would be: save/restore regs, then `daifclr`, then `eret` to guest. On exit (trap to EL2), IRQs... the trap entry — QEMU returns with the exit reason; at that point IRQs are... the CPU is in EL2 with PSTATE from hypervisor context (IRQ state as it was). If we `daifclr`'d before eret, after the trap back (run_guest returns), IRQs are still unmasked (the trap to EL2 happens with EL2's PSTATE...). In QEMU TCG, `run_guest` is a synchronous function call — the "guest" runs inside it. When the guest exits, run_guest returns with the exit reason, and the EL2 PSTATE is whatever it was before the eret (unmasked, since we cleared). So during vmexit_handler, IRQs are unmasked → an IRQ during exit handling → EL2 vector → normal handling. That's the normal OS behavior anyway. Then vmexit_handler runs; then the run loop processes the exit. 

Hmm — but the concern: re-entrancy of the run loop itself. If an IRQ fires during the run loop (between run() returning and the next run() call — the loop code like drain_and_inject, trace, etc.) → EL2 handler → fine, it's normal OS context.

What about the `daifset` at entry — keep it? The purpose was to mask during entry sequence (restore_vm_system_regs before eret). Recommended sequence:
```rust
unsafe { core::arch::asm!("msr daifset, #2"); }  // mask during entry setup
let exit_reason = unsafe {
    self.restore_vm_system_regs();
    // unmask before entering guest
    core::arch::asm!("msr daifclr, #2");
    self.run_guest()
};
```
Wait — `run_guest()` is a function call — the unmask happens before the call; then inside run_guest, the eret... between `daifclr` and the eret inside run_guest there's a window in EL2 with IRQs unmasked — an IRQ there → EL2 vector → handled → return → continue eret → guest. Fine (normal OS behavior).

And after run_guest returns (guest exit), IRQs remain unmasked — the original code's `daifclr` after exit is no longer needed, but the exit path now runs with IRQs enabled — normal OS behavior — OK.

Hmm — but wait, actually, I should double check whether QEMU even delivers the IRQ into the guest properly... The whole thing is QEMU emulating GICv3: physical IRQ line → if the target CPU's IRQ mask (PSTATE.I at EL1... the physical IRQ to an EL1 guest — the guest's own PSTATE.I governs delivery to EL1; but we're virtualizing: the physical IRQ while in EL1 guest → QEMU KVM-style: the IRQ is delivered to EL2 (exit), NOT to the guest, since it's a physical IRQ (not a vIRQ routed via LR). Hmm — actually in QEMU's GIC emulation with EL2: when a physical IRQ is pending and the CPU is in EL1 (guest mode), QEMU checks `cpu->irq` masking via the EL1 PSTATE... no wait. QEMU models this via `arm_cpu_exec_interrupt` / the GIC raising the CPU IRQ line: if the CPU is at EL2 with IRQ masked (DAIF.I of EL2 context), delivery is delayed. If the CPU is in guest (EL1) — QEMU's `cpu_get_tb_cpu_state` computes IRQ masking from PSTATE... The physical IRQ delivered while at EL1: QEMU would enter the EL1 vector (the guest's IRQ handler)?! No — because EL2 exists (hypervisor mode), QEMU delivers the IRQ to EL2 (the hypervisor), since... hmm, actually in QEMU, when EL2 is in use and IRQ arrives at a CPU executing EL1 guest code, QEMU routes the IRQ to EL2 (like hardware: IRQ to EL2 when SCR_EL3 / HCR_EL2.IMO... the routing depends on HCR_EL2.IMO/AMO/FMO and the current EL). 

In arm_vcpu, HCR_EL2 is configured (init_vm_context) — if HCR_EL2.IMO = 0, physical IRQs at EL1 → EL1's own vector (guest handles). If IMO=1, IRQ → EL2. For a hypervisor that wants to intercept physical IRQs... KVM sets IMO=0 and the IRQ goes to the guest's EL1 vector (vGIC handles it as a virtual IRQ — the guest thinks it's a vIRQ!). That's the passthrough model!

Hmm, this matters: if HCR_EL2.IMO=0, then a physical IRQ while guest runs → delivered to the GUEST's EL1 vector directly, and QEMU would NOT give us an ExternalInterrupt exit — it would just inject into the guest... no wait — with QEMU TCG, "delivered to the guest's EL1 vector" means the CPU executes the guest's IRQ handler with our physical IRQ — the guest (Zephyr) doesn't know IRQ 48 or the IPI vector... Actually the physical IPI vector (SGI) delivered to the guest → guest's GIC view: does the guest's GIC (the vGIC... there's no vGIC in passthrough, the physical GIC is shared) — the guest sees the SGI as its own interrupt → Zephyr's ISR for that SGI vector → guest handles → no exit → no drain. Hmm — but wait, this contradicts the current observed behavior where physical IRQs during guest execution DON'T get delivered at all (they're masked via DAIF.I — the daifset masks IRQs for the whole guest run, so even the guest... no wait — DAIF.I is EL2's PSTATE — the mask applies to EL2 context; when entering EL1 guest via eret, PSTATE is restored from SPSR_EL2 — the guest's own DAIF (Zephyr sets PSTATE.I=0 for EL1). Hmm, so `msr daifset, #2` in the hypervisor masks IRQ in EL2 context; the GUEST's EL1 has its own PSTATE.I — so a physical IRQ while guest runs: QEMU decides routing based on HCR_EL2.IMO: if IMO=1 → IRQ to EL2 — but EL2's PSTATE.I is set (daifset) → QEMU delays delivery until EL2 unmasks. If IMO=0 → IRQ to EL1 → delivered directly to guest even with EL2's daifset? The masking... EL1 PSTATE.I governs.

The observed evidence (no exits after guest settles, no delivery to guest either — guest received 0/300 and the IPI never caused any visible guest action) suggests: IRQs during guest execution are simply not delivered anywhere (masked at EL2, not routed to guest). So HCR_EL2.IMO is probably set (physical IRQs route to EL2) + DAIF.I masked → deferred. 

OK so my fix proposal stands: unmask IRQs during guest run → IRQ routes to EL2 → QEMU exits guest with ExternalInterrupt → existing path. 

But hold on — I should verify HCR_EL2.IMO and how ExternalInterrupt exits are produced in arm_vcpu. Let me check init_vm_context in arm_vcpu and how the QEMU side produces the ExternalInterrupt trap kind. Actually, the vmexit_handler already handles TrapKind::ExternalInterrupt (ArmVmExit::ExternalInterrupt) — so QEMU CAN produce it. Whether it does with IMO settings — need to check hcr config.

Let me check quickly.

**Tool: bash**

**Input:**
```json
{
  "command": "grep -n \"hcr_el2\\|HCR_EL2\\|imo\\|IMO\\|fmo\\|FMO\" virtualization/arm_vcpu/src/vcpu.rs virtualization/arm_vcpu/src/types.rs | head; echo ===; grep -n \"TrapKind\\|ExternalInterrupt\" virtualization/arm_vcpu/src/vcpu.rs | head; echo ===; sed -n 290,320p virtualization/arm_vcpu/src/vcpu.rs"
}
```

**Output:**
```
virtualization/arm_vcpu/src/vcpu.rs:257:        let mut hcr_el2 =
virtualization/arm_vcpu/src/vcpu.rs:258:            HCR_EL2::VM::Enable + HCR_EL2::TSC::EnableTrapEl1SmcToEl2 + HCR_EL2::RW::EL1IsAarch64;
virtualization/arm_vcpu/src/vcpu.rs:261:            // Set HCR_EL2.IMO will trap IRQs to EL2 while enabling virtual IRQs.
virtualization/arm_vcpu/src/vcpu.rs:266:            hcr_el2 += HCR_EL2::IMO::EnableVirtualIRQ + HCR_EL2::FMO::EnableVirtualFIQ;
virtualization/arm_vcpu/src/vcpu.rs:269:        self.guest_system_regs.hcr_el2 = hcr_el2.into();
===
23:    exception::{TrapKind, handle_exception_sync},
183:        let trap_kind = TrapKind::try_from(exit_reason as u8).expect("Invalid TrapKind");
367:    /// - `exit_reason`: The reason why the VM-Exit happened in [`TrapKind`].
373:    fn vmexit_handler(&mut self, exit_reason: TrapKind) -> ArmVcpuResult<ArmVmExit> {
387:            TrapKind::Synchronous => handle_exception_sync(&mut self.ctx),
388:            TrapKind::Irq => Ok(ArmVmExit::ExternalInterrupt {
===

/// Private functions related to vcpu runtime control flow.
impl<H: ArmHostOps> ArmVcpu<H> {
    /// Save host context and run guest.
    ///
    /// When a VM-Exit happens when guest's vCpu is running,
    /// the control flow will be redirected to this function through `return_run_guest`.
    #[unsafe(naked)]
    unsafe extern "C" fn run_guest(&mut self) -> usize {
        // Fixes: https://github.com/arceos-hypervisor/arm_vcpu/issues/22
        //
        // The original issue seems to be caused by an unexpected compiler optimization that takes
        // the dummy return value `0` of `run_guest` as the actual return value. By replacing the
        // original `run_guest` with the current naked one, we eliminate the dummy code path of the
        // original version, and ensure that the compiler does not perform any unexpected return
        // value optimization.
        core::arch::naked_asm!(
            // Save host context.
            save_regs_to_stack!(),
            // Save the host stack top and SP_EL0 to `self.host`.
            //
            // 'extern "C"' here specifies the aapcs64 calling convention, according to which
            // the first and only parameter, the pointer of self, should be in x0.
            "mov x9, sp",
            "add x10, x0, {host_stack_top_offset}",
            "str x9, [x10]",
            "mrs x9, sp_el0",
            "str x9, [x10, #8]",
            // Save host task TLS before the final assembly window installs the
            // guest value. No Rust executes with guest TPIDR_EL0 live.
            "mrs x9, tpidr_el0",

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 4.2s)

**Tool: bash**

**Input:**
```json
{
  "command": "sed -n 250,275p virtualization/arm_vcpu/src/vcpu.rs; echo ===; sed -n 355,400p virtualization/arm_vcpu/src/vcpu.rs"
}
```

**Output:**
```
        if self.guest_system_regs.vtcr_el2 == 0 {
            let pa_bits = pa_bits();
            let levels = max_gpt_level(pa_bits);
            let gpa_bits = if levels == 3 { 39 } else { 48 };
            self.guest_system_regs.vtcr_el2 = vtcr_for_config(levels, gpa_bits, pa_bits);
        }

        let mut hcr_el2 =
            HCR_EL2::VM::Enable + HCR_EL2::TSC::EnableTrapEl1SmcToEl2 + HCR_EL2::RW::EL1IsAarch64;

        if !config.passthrough_interrupt {
            // Set HCR_EL2.IMO will trap IRQs to EL2 while enabling virtual IRQs.
            //
            // We must choose one of the two:
            // - Enable virtual IRQs and trap physical IRQs to EL2.
            // - Disable virtual IRQs and pass through physical IRQs to EL1.
            hcr_el2 += HCR_EL2::IMO::EnableVirtualIRQ + HCR_EL2::FMO::EnableVirtualFIQ;
        }

        self.guest_system_regs.hcr_el2 = hcr_el2.into();

        // Set VMPIDR_EL2, which provides the value of the Virtualization Multiprocessor ID.
        // This is the value returned by Non-secure EL1 reads of MPIDR.
        let mut vmpidr = 1 << 31;
        // Note: mind CPU cluster here.
        vmpidr |= self.mpidr;
===
                ic  iallu
                tlbi	alle2
                tlbi	alle1         // Flush tlb
                dsb	nsh
                isb"
            );
        }
    }

    /// Handle VM-Exits.
    ///
    /// Parameters:
    /// - `exit_reason`: The reason why the VM-Exit happened in [`TrapKind`].
    ///
    /// Returns:
    /// - [`ArmVmExit`]: a wrappered VM-Exit reason needed to be handled by the hypervisor.
    ///
    /// This function may panic for unhandled exceptions.
    fn vmexit_handler(&mut self, exit_reason: TrapKind) -> ArmVcpuResult<ArmVmExit> {
        trace!(
            "ArmVcpu vmexit_handler() esr:{:#x} ctx:{:#x?}",
            exception_class_value(),
            self.ctx
        );

        unsafe {
            // Store guest system regs. Guest SP_EL0 was already saved into `self.ctx`
            // by the EL2 assembly before host SP_EL0 was restored.
            self.guest_system_regs.store();
        }

        let result = match exit_reason {
            TrapKind::Synchronous => handle_exception_sync(&mut self.ctx),
            TrapKind::Irq => Ok(ArmVmExit::ExternalInterrupt {
                vector: H::fetch_pending_host_irq().unwrap_or(0) as u64,
            }),
            _ => panic!("Unhandled exception {:?}", exit_reason),
        };

        match result {
            Ok(ArmVmExit::SysRegRead { addr, reg }) => {
                if let Some(exit_reason) =
                    self.builtin_sysreg_access_handler(addr, false, 0, reg)?
                {
                    return Ok(exit_reason);
                }

```

---

## Assistant (Build · DeepSeek V4 Flash Free · 26.7s)

_Thinking:_

Now I have the complete picture:

- Emulated mode (`passthrough_interrupt = false`): `HCR_EL2.IMO = EnableVirtualIRQ` → physical IRQs during guest trap to EL2 → `TrapKind::Irq` → `ArmVmExit::ExternalInterrupt { vector: H::fetch_pending_host_irq() }` — the IPI would be fetched and returned. **BUT** the whole `run()` is bracketed by `daifset, #2` / `daifclr, #2` — IRQs masked in EL2 context — so the IRQ never gets delivered → QEMU never generates the Irq trap → the guest keeps running.

So in emulated mode, the mechanism exists (IMO traps IRQ to EL2, ExternalInterrupt exit handled, `dispatch_host_irq` → IPI handler, loop continues, drain!) — it just never fires because DAIF.I is masked.

- Passthrough mode (`passthrough_interrupt = true`): IMO not set → physical IRQs go directly to EL1 guest → guest handles → no exit → no drain. That's why B passthrough (running=1, drain=0) and the doc's §3.3 says "host IPI 没有可靠地让 guest vCPU 返回 AxVM host-side drain/inject 路径". In passthrough, the IPI design can't work as a host kick at all — unless the GIC routes the IPI as a *virtual* interrupt... in passthrough there's no vGIC LR emulation... hmm, actually passthrough still has the vCPU interface for the guest's own GIC accesses (they pass through). The host IPI (SGI from CPU 0 to CPU 3) — physical → goes to the guest's EL1 vector as its own interrupt (Zephyr sees SGI, probably ignores it) → no exit → broken by design for the "kick" purpose.

So the concrete fix plan for B:

**Emulated mode (the doc's TODO #1: 最小闭环):**
1. Unmask IRQs during guest execution: move `daifclr` before `run_guest()` (i.e., `msr daifclr, #2` right before the eret into guest; keep the entry masked). The cleanest minimal change in `ArmVcpu::run()`:
   ```rust
   pub fn run(&mut self) -> ArmVcpuResult<ArmVmExit> {
       unsafe { core::arch::asm!("msr daifset, #2"); }
       let exit_reason = unsafe {
           self.restore_vm_system_regs();
           // unmask host IRQs so physical IPIs/timers can trap out of the guest
           core::arch::asm!("msr daifclr, #2");
           self.run_guest()
       };
       let trap_kind = ...;
       let result = self.vmexit_handler(trap_kind);
       unsafe { core::arch::asm!("msr daifset, #2"); }  // mask again during host-side run loop? 
       ...
   }
   ```
   Hmm — wait, should we mask after exit? The original design masks during the whole run (guest + exit handling). If we unmask during guest, the exit handling can be interrupted — that's fine (normal OS). But there's a subtlety: the IRQ fires while we're at EL2 in the exit path — the EL2 vector handles it and... the pending "second" IRQ gets delivered asynchronously. The run loop then drains. Should be OK.

   Actually simpler and closer to original intent: keep the structure but unmask immediately before run_guest:
   ```rust
   unsafe { core::arch::asm!("msr daifset, #2"); }
   let exit_reason = unsafe {
       self.restore_vm_system_regs();
       core::arch::asm!("msr daifclr, #2");  // NEW: let physical IRQs (IPI) trap out of the guest
       self.run_guest()
   };
   // daifclr already happened... hmm the daifclr after exit is now a no-op (already cleared)
   ```
   After run_guest returns, IRQs are unmasked (cleared before eret, trap returns keep EL2 PSTATE... wait — when QEMU traps the IRQ to EL2, does EL2 PSTATE.I stay as it was at eret time (cleared)? The trap to EL2 happens in the context of the eret'd PSTATE... the EL2 vector entry — the PSTATE at the EL2 vector is the current one (cleared). Yes, IRQs remain unmasked through the exit path — that's normal hypervisor behavior.

   BUT: one important consideration — the run loop in ops.rs calls `vcpu.run()` repeatedly; between runs, the vCPU task executes with IRQs unmasked (normal OS context) — already the case today (daifclr after run). So no change there.

   Risk: an IRQ arriving while in EL2 entry sequence (between daifclr and eret inside run_guest's naked asm) → EL2 vector → ArceOS IRQ handler → returns → continue eret. Fine, normal.

   Risk: preemption — with IRQs unmasked, the ArceOS scheduler tick can preempt the vCPU task mid-guest → another task runs on CPU 3 → the guest context is suspended in the vCPU task's stack — resumed later. This is normal for this system? Hmm — today IRQs are masked so the vCPU task cannot be preempted mid-run(). Unmasking changes that. For the experiment this adds jitter but works. Actually — wait, would the scheduler preempting the vCPU task be a problem for QEMU guest state? The guest registers are in the task's context struct; the EL2 guest execution state (the "run_guest" stack frame) is in the task's stack — preemption = context switch saves/restores the task's stack — fine.

2. Then the IPI handler on CPU 3: `dispatch_host_irq(vector)` → ArceOS IPI handler — needs to not break the run loop. The IPI handler typically triggers scheduling... The vCPU task isn't sleeping — it's running — so the IPI handler just acks and returns; the run loop continues → drain → inject. 

   Hmm wait — one thing: `TrapKind::Irq` → `H::fetch_pending_host_irq()` — the IRQ is fetched from the GIC (acked) — the vector returned — `ExternalInterrupt` → `after_external_interrupt` → `dispatch_host_irq(vector)` → runs the ArceOS IRQ handler for the IPI vector. Need to confirm the IPI vector's handler is registered in axvisor. `send_ipi` uses `ipi_irq()` from ax_hal — the handler is registered by the scheduler (axsync/axtask). OK.

3. After the fix: emulated smoke → expect drain≈enqueue, guest receives vIRQ 48. Then the LR/Group questions (TODO #2) become relevant only after the loop works — the guest-side delivery (GIC LR settings for vector 48, Zephyr GIC config) is the second half of the loop.

**Passthrough mode**: the kick cannot work via host IPI (IRQ goes to guest). For the A/B experiment, use emulated for both? But then B is "emulated queue + notify + IPI" — that's the deliverable. Passthrough would need a different kick design (e.g., use a dedicated physical IRQ that the guest doesn't receive... in passthrough ALL IRQs go to the guest). Hmm — actually wait: in passthrough mode, `inject_vcpu_interrupt` → `inject_interrupt_with_trigger` → writes to GIC LR — but in passthrough there's no LR emulation... Let me not go down this hole. The doc's own TODO says: "在 emulated GIC 下先建立最小可用闭环" — agree. Passthrough for the vIRQ path is a later concern.

So the plan:

1. **根因已确认**（证据链）：`ArmVcpu::run()` 以 `daifset` 屏蔽物理 IRQ 运行 guest（arm_vcpu/src/vcpu.rs:175）；emulated 模式虽有 `HCR_EL2.IMO` + `ExternalInterrupt` 处理（会 `fetch_pending_host_irq` → `dispatch_host_irq` → loop 继续 → drain），但因为 IRQ 被屏蔽，IPI 永远到不了 EL2 → guest 不退出 → 队列永不 drain。A1/B-passthrough/B-emulated 三种现象全部一致（B-emu 的 31 次 running 全部在 811-823ms 启动窗口内，首个 enqueue 818ms 后 5ms 内 guest 再也不退出）。

2. **修复**：emulated 模式下 guest 运行期间打开物理 IRQ（把 `daifclr` 移到 `run_guest()` 之前），让 IPI 触发 `ExternalInterrupt` exit → 现有链路自动完成 drain/inject。这是 arch 级改动（arm_vcpu + axvm run loop 语义），需要按 arch-platform-porting 技能评审。

3. **验证**：先小样本 smoke（1/4/16），确认 `received == expected`；再 300。同时确认 TODO#2 的 GIC LR 问题只在闭环建立后处理。

4. **A/B 重新定义**：A 与 B 的差异需要按实际情况重新定义（A=无限队列+VM级notify+task.cpu_id；B=有界队列+per-vCPU notify+affinity mask），修复文档 §1/§3.1，再跑 A1→B1→A2→B2。

5. 统计脚本和 trace 需要加接收端事件（ipi_received）以便后续区分。

Now — the user asked "接下来我们要怎么做". I should present this plan concisely and ask if they want me to proceed with the fix. Let me present the root cause + plan, and ask for go-ahead. Per the guidance, since they asked "how to approach", answer first, and offer to start. I'll keep it reasonably tight but this is a technical plan, detail is justified. I'll use the question tool? The instructions say to use question tool when clarification is needed — I can just present the plan and ask. Actually a short question at the end is fine, or use the question tool. I'll present the plan in text and ask whether to proceed with step 1+2 (the fix). Keep it structured and short.

根因已经可以确认了（我把 arm_vcpu 的代码和日志时间戳交叉验证过），下一步方案如下：

## 根因（证据链完整）

`ArmVcpu::run()` 在运行 guest 期间用 `msr daifset, #2` 屏蔽了全部物理 IRQ（virtualization/arm_vcpu/src/vcpu.rs:173-190）。而 emulated 模式的 IRQ 路径本来是通的：`HCR_EL2.IMO` 已设置 → 物理 IRQ 会 trap 到 EL2 → `TrapKind::Irq` → `ExternalInterrupt` → `dispatch_host_irq` → run loop 继续 → **drain**。但 IRQ 被 DAIF.I 屏蔽后根本到不了 EL2，guest 永不退出。

时间戳证据（B1-emu.log）：31 次 `running` 全部集中在 811–823ms 启动窗口；首个 enqueue 在 818.9ms；823.8ms 之后 **guest 再也不退出**，队列积满 64、overflow 235 次。A1（running=1）、B-passthrough（running=1）是同一机制的变体：notify 对运行中的 vCPU 是 no-op，IPI 被 DAIF.I 屏蔽。这也解释了为什么 B-emu 那唯一一次 drain=1 是撞上启动窗口的 WFI 退出。

## 修复方案（按顺序）

1. **打通 kick 链路**：emulated 下把 `daifclr` 移到 `run_guest()` 之前，让 guest 运行期间物理 IRQ 可 trap 到 EL2。这是 arch 级语义改动（影响所有 guest 的中断送达时机、vCPU task 可被调度器抢占），我会按 `arch-platform-porting` 技能先评审再改，并验证 ExternalInterrupt 退出路径的 `fetch_pending_host_irq` + IPI handler 注册。
2. **小样本闭环验证**：先 1/4/16 次 smoke 确认 `received == expected`，再恢复 300。闭环建立后再处理 GIC LR/Group（TODO #2）的 guest 侧问题。
3. **给 trace 加接收端事件**（`ipi_received`），把"IPI 到了但没引起 exit"这种盲区堵上。
4. **重新定义 A/B 并修文档**：A 的实际路径是"无限队列 + VM 级 notify + `task.cpu_id()`"，B 是"有界队列 + per-vCPU notify + affinity mask"，§1/§3.1 需要改写，之后才跑 A1→B1→A2→B2。
5. passthrough 模式暂时搁置——它的 IRQ 直通设计决定了 host IPI 永远到不了 EL2，作为实验第二阶段再解决。

要我现在从第 1 步开始动手吗？还是你想先只做只读验证（比如先出个最小改动跑一次冒烟确认 kick 生效）？

---

