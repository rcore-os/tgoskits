# PR #1775 继续推进至全 CI 通过

## 已核实的当前状态
- PR #1775 head = `e5097c13e`（分支 `codex/refactor-ax-task-from-1596`），与 `origin/dev@d1fa14a4c` 处于 CONFLICTING
- dev 在 merge-base `35aaf4003` 之后新增 4 个提交：
  - `6e6411980` fix(ax-mm): roll back failed populated mappings (#2079) — 与本分支 axmm/BootLinear 大页改动同文件重叠
  - `e41336f4c` Consolidate runtime documentation and update rootfs guide (#2087) — 文档+rootfs 指南
  - `1d5fbe1e2` feat(axvisor): axum management HTTP control plane (#1909) — 改 axvisor/axvm/axbuild test/CI checks，与本分支 axvm runtime 重构（VcpuThreadRegistry、owner-generation IRQ dispatcher）大面积重叠
  - `d1fa14a4c` docs(axvisor): emulated-devices (#2084) — 纯文档
- 最新 CI run `32101423191`（head e5097c13e）：**唯一真实失败 = Tests / Starry / loongarch64 QEMU · Suites**（Run command exit 1，约 6 分钟），触发 fail-fast 取消其余；已通过项含全部 ArceOS QEMU 四架构、多数 AxVisor、全部静态检查、std tests
- review：mai-team-app 最近三轮 CHANGES_REQUESTED；最新（8-17）阻塞项是 CI 失败归因；更早的 ax-task 测试发现链路问题已由 `6ad612f8df`（axtest 接入，`[lib] test=false` + 显式 axtest feature）处理
- 参考基准：`~/linux-src` = Linux v7.1（含 PREEMPT_RT 路径）；本地 16 核、四架构 QEMU 齐备

## 执行步骤

### 1. 开新分支（不动远端）
- `git switch -c codex/pr1775-ci origin/codex/refactor-ax-task-from-1596`
- 推送统一走 `git push origin HEAD:codex/refactor-ax-task-from-1596`（直接更新 PR #1775 head）

### 2. 同步 dev 并按新架构融合（提交/推送栅栏）
- 先读 dev 4 个新提交的 PR 描述与文档（#2079/#1909/#2087/#2084），确定语义重叠
- `git merge origin/dev` 解决冲突：
  - axmm backend/alloc.rs：融合 #2079 的失败映射回滚语义与本分支 BootLinear 大页路径，保持单一实现
  - axvisor/axvm/axbuild：保留本分支 VcpuThreadRegistry/owner-generation IRQ dispatcher/join-reap 生命周期，接入 #1909 的 config/machine 分锁、HTTP 控制面与新 CI check；删除被新架构替代的旧实现，不留双路径
  - 文档/rootfs：吸收 #2087 的文档基线；被新机制替代的旧特判（如旧 snapshot 特判）直接删除
  - Cargo.lock：其余冲突全部解决后由 Cargo 重新生成，不做手工拼接
- 验证：`cargo fmt --all`、`cargo metadata --locked --no-deps`、`python3 scripts/test/check_ci_paths.py` 等本地 CI 静态脚本、受影响 crate 的 `cargo xtask clippy --package ...`

### 3. 复现并根因修复 Starry loongarch64 失败
- 本地 `cargo xtask starry test qemu --arch loongarch64` 复现（必要时 `--keep-qemu-log`、单用例定位；执行时再重试一次 gh 拉取 CI 日志）
- 调度/同步语义问题对照 `~/linux-src` v7.1 对应路径（sched/core.c、rt.c、rtmutex.c、futex、irq_work 等），按 Linux 所有权模型修根因；禁止兜底、放宽超时、兼容旁路
- 一个根因一个提交；可行时先写确定性红绿回归
- 已知线索：`6ad612f8df` 评论提到 placement/switch-tail owner-rq 竞态（`0x504c0001`），复现中出现则优先按 set_task_cpu/finish_task_switch 语义修复

### 4. 双轨异步闭环（失败事件驱动，不忙等）
- 推送触发远端 CI 后，立即并行启动本地矩阵（后台任务，16 核分时复用）：
  - `cargo xtask starry test qemu --arch {x86_64,aarch64,riscv64,loongarch64}`
  - CI 中被取消的关键项：AxVisor aarch64 smoke+virtio-blk+axtest+timer stress、Incremental Clippy 对应命令
  - ArceOS 四架构（CI 已过，抽查即可）
- 每阶段本地超时 = dev 基线同阶段 1.5×（相同架构/配置/资源）；超时优先怀疑卡死、丢失唤醒、实现退化
- 每完成一项本地检测，立即检查：CI 新失败、未关闭 review threads、后台任务有效性
- 后台结果绑定启动时 commit；共享代码变更后在新 head 重跑相关项

### 5. 提交/推送检查点（每轮）
- 每轮提交与推送前 `git fetch origin dev`，有新提交则重复步骤 2 的语义分析、冲突处理与旧实现清理
- 推送后追加 PR 进度评论（本轮提交、验证、CI 状态、剩余问题），不把阶段日志写进 PR 正文；冲突适配等实质变化时同步 PR 正文
- 用 gh api 枚举未解决 review threads，逐条以当前代码证据回复并 resolve；最新 CHANGES_REQUESTED（CI 阻塞项）在当前 head CI 全绿后回复

### 6. 资源维护
- 每轮检查 target 占用；>100GB 且无后台任务共用同一 target 时才 `cargo clean`，否则等任务结束或用隔离 target

### 7. 完成标准
- 当前 head 的完整 CI 所有 required checks 终态 SUCCESS（排队/取消/跳过/旧 head 不算）
- 无未关闭 review threads、与最新 dev 无冲突、PR 描述与最终提交同步

## 风险与说明
- CI 日志当前无法通过 gh 拉取（--log-failed 为空），失败定位依赖本地复现
- 板级测试（OrangePi/RK3568/Phytium 等 self-hosted board check）无法本地复现；若失败按证据归因（区分基础设施/基线问题），不修改 runner 选择或门禁绕过