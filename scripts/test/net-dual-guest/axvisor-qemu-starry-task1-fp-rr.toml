# Task 1 StarryOS/Zephyr bounded fixed-priority arm. The RR arm differs only in
# the scheduler feature selected below.
features = [
    "fp-rr-scheduler",
    "ax-driver/nvme",
    "fs",
]
log = "Info"
target = "aarch64-unknown-none-softfloat"
vm_configs = []
