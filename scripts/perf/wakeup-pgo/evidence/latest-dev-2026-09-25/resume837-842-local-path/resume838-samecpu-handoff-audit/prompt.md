You are a read-only source explorer. Work at /home/zhourui/.codex/worktrees/03ae/tgoskits-dev, exact HEAD 69a33650763538692fafea27c869870ed0313642 on dev 05175ca38823b631a73777b0130226ddfa558439. Do not edit, run builds/tests/board experiments, or delegate.

Goal: trace the measured forward transaction of OTHER `thread_futex_same_cpu` from sender FUTEX_WAKE entry to receiver user return, and find a concrete semantic-preserving runtime mechanism that could close roughly 5.2 us p50 (G1/G2 14583.5 ns, Linux RT 8458 ns; 90% requires candidate <=9397 ns). Include exact file/function/line references, dynamic path conditions, safety invariants, and why the mechanism spans enough cost rather than a 100-300 ns leaf. If no such candidate is evidence-backed, explicitly say so and nominate a focused discriminator.

Prior audit constraints: `resume805` aligned benchmark window and found one required switch; global ~2.2 switches/attempt includes outside-window setup. `resume806` rejected reuse of Fair wake candidate across wake/schedule because `put_prev` changes set. `resume605-608` found hot enqueue/park phases but isolated fence/Arc tweaks too small. `resume628/629` instrumented receiver task tail and futex cleanup but overhead is significant. `resume811/813` PMU/markers perturb path proportions, so no causal native claim. Fair AVL shallow and old ClassEnqueue snapshot/inline, bitmap and V-key shortcuts had regressions or weak gains. Read /home/zhourui/.codex/artifacts/issue2308-perf/experiment-ledger/BY-DIRECTION.md and relevant current code. Do not recommend blindly repeating old experiments.

Preserve futex wait/wake ordering, task state publication, Fair vruntime accounting, scheduler preemption, cancellation/timeouts, signal and PI semantics. Explain a candidate's proof obligations and minimum native A/B gate: source-matched ordinary vs candidate cpufreq-off full20 with p50/p99/p99.9 <3% regressions and zero missed/not_parked. Return concise evidence, candidate or falsifying diagnostic, risks and next action. Do not make changes.
