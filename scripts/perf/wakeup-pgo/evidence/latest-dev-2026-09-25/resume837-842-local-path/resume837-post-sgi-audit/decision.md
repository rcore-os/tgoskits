# resume837: CPU1 post-SGI path audit

The independent read-only trace is `subagent/final.txt`. I checked its key ownership claims against the exact head `69a3365076`: `platforms/axplat-dyn/src/irq.rs::handle` resolves the SGI, `ax-plat::irq::dispatch_ipi_irq_on` acknowledges the controller before the registered `ipi_delivery::irq_handler` claims the coalesced software edge. The handler also services scheduler and hard-call work on that physical IPI. The idle path retains a preemption guard across WFI; IRQ return cannot perform the ordinary worker switch before that outer guard exits.

No single source-level operation after `begin_irq` is evidenced as a semantics-preserving, multi-microsecond removal. The apparent second scheduler-tick restart is guarded by `ClockEvent::restart_scheduler_tick_after_idle`: when the tick is already Running it returns `ClockEventAction::None`. A repeated complete scheduling pass was already contradicted by `resume794`; the descriptor-index change in `resume795` failed native full20 regression gates. The `resume833` dispatch enclosing time and `resume836` CPU0 issue-to-CPU1 entry histogram are instrumented mixed-event measurements and cannot be subtracted from the four cross-CPU native gaps.

A future receiver-bound generation probe could distinguish IRQ-handler, idle-exit and switch/return stages, but a global IPI slot alone does not identify which futex receiver belongs to the event. This audit does not authorize a source change or a native-gain claim. Preserve publication-before-doorbell, controller acknowledgement-before-edge-claim, hard-call draining and idle polling clear/recheck. Latest matched-frequency G1/G2 remains 11/20 at 90%; the four cross-CPU futex rows still require about 5.9-7.4 us p50 reductions.
