# resume870: invalid first A-B-B-A attempt

Source A `b292a098bb60ef604e7677c37cd95d926ff08200` and source B (checked-i64 Fair placement-lag fast path plus boundary test) both built successfully as ordinary uninstrumented AArch64 Starry release images with the same `build.toml`. A image SHA256 is `4903ed1d8b2c73dcf7dfedcf50d0762838fc8ce6c06059754f0ce839bb74d2c9`; B image SHA256 is `0b76686e32eafd90f81095ba5aca7e4ff5562870fb57748362dc03f62d4062f4`. `cargo fmt` and `cargo xtask clippy --package ax-task` (6/6) passed. The targeted `cargo xtask cross-test --arch x86_64 --package ax-task --features host-test --lib placement_lag_fast_path_matches_wide_arithmetic` compiled the test but failed to link because ax-task has unresolved runtime trait-ffi symbols in this standalone host configuration. It did not execute the assertion.

The first board A1 full20 boot on OrangePi-5-Plus-1 (session `493086bc-e2a8-4365-b281-a83bd09b0d85`) reached the frozen benchmark and passed the PLL/SMP checks, but OTHER `thread_futex_same_cpu` had 19999/20000 samples and `not_parked=1`, so A1 is globally invalid. Its raw log and serial remain in this directory. The copied resume818 collector did not automatically abort after this invalid row and began rebooting; it was interrupted before B1 ran. Its `finally` sent DELETE and a subsequent session GET returned not_found. No A/B comparison or native gain is valid.

Retry only in a new session and evidence directory, with the collector fixed to stop immediately after any invalid boot. Do not count A1 or splice its other 19 rows. The source candidate remains experimental and uncommitted in the main worktree pending a valid same-source comparison.
