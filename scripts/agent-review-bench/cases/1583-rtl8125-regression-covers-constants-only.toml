id = "1583-rtl8125-regression-covers-constants-only"
pr = 1583
title = "fix(realtek-rtl8125): advertise gigabit autonegotiation"
remote = "https://github.com/rcore-os/tgoskits.git"
base = "ba27d7b061b47d22b16a53c847b1a4abc2bece71"
head = "214ccd50c9df852efaa74822341294defa92e001"
source = "https://github.com/rcore-os/tgoskits/pull/1583#discussion_r3567595139"
fixed_by = "7b37887e616abdb4b6e98c1e07f533607f51e2b7"

[[expected]]
id = "rtl8125-regression-does-not-exercise-phy-sequence"
path = "drivers/net/realtek-rtl8125/src/hw.rs"
line = 550
severity = "major"
description = "The regression test only checks relationships among the newly added constants. It never calls hw_phy_config or observes PHY reads and writes, so deleting the production MII_CTRL1000 and MII_BMCR configuration still leaves the test passing and restores the original autonegotiation bug."
match_if = "A finding must identify that the added test validates constants rather than the production PHY configuration behavior, so it cannot fail when the new page selection/advertisement/restart sequence is removed, and require a recording/mock PHY boundary or equivalent deterministic production-path regression."
