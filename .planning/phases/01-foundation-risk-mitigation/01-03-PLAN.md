---
phase: 01-foundation-risk-mitigation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .claude/scripts/test_validate_deps.py
  - .claude/scripts/validate-deps.py
autonomous: true
gap_closure: true
requirements:
  - FND-02
user_setup: []

must_haves:
  truths:
    - "parse_version('unknown') returns a value that is >= any minimum version tuple, allowing plugins from dev sources to pass version checks"
    - "A pytest test case exists that verifies a plugin with version 'unknown' and a non-None minimum version requirement passes check_plugins()"
    - "All 5 test cases in test_validate_deps.py pass, including the new unknown-version-satisfies-minimum case"
  artifacts:
    - path: ".claude/scripts/test_validate_deps.py"
      provides: "Unit test covering unknown version acceptance"
      contains: "test_unknown_version_satisfies_minimum"
    - path: ".claude/scripts/validate-deps.py"
      provides: "Version parser that treats 'unknown' as satisfying any minimum"
      contains: "sys.maxsize"
  key_links:
    - from: ".claude/scripts/test_validate_deps.py"
      to: ".claude/scripts/validate-deps.py"
      via: "importlib.util dynamic module loading"
      pattern: "spec_from_file_location"
---

<objective>
Close verification gap: `parse_version("unknown")` returns a tuple that fails minimum version comparison.

The code fix changing `(0,)` to `(sys.maxsize,)` is already applied in the working tree but uncommitted. No test validates the specific behavior that a plugin with version "unknown" and a non-None `min_version` requirement passes validation. The existing `test_all_plugins_present` test exercises "unknown" against pr-review-toolkit which has `min_version=None`, so the version comparison path is never reached.

Purpose: Add the missing test case, verify all tests pass, and commit the fix + test together.

Output: One new test method in test_validate_deps.py, committed along with the already-applied code fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-risk-mitigation/01-VERIFICATION.md -- Gap 1: parse_version("unknown") returns (0,) which is < (5, 1, 0)
@.planning/phases/01-foundation-risk-mitigation/01-01-SUMMARY.md -- Plan 01 delivery: validate-deps.py design, test structure, decisions
@.claude/scripts/validate-deps.py -- Current state: parse_version("unknown") returns (sys.maxsize,) [uncommitted fix]
@.claude/scripts/test_validate_deps.py -- Current state: 4 test cases, none cover "unknown" version against a non-None min_version

**Existing test structure to follow:**
- `_make_mock_plugins(entries: dict)` creates temp JSON files matching real installed_plugins.json format.
- `_get_check_plugins()` loads validate-deps.py via `importlib.util.spec_from_file_location` and returns `check_plugins`.
- `TestValidateDeps(unittest.TestCase)` uses `unittest.mock.patch` to capture stderr for error-message assertions.
- Each test creates a mock path, calls `check_plugins(path)`, and cleans up with `os.unlink(path)` in a `finally` block.

**Key types and contracts from validate-deps.py:**
```python
REQUIRED_PLUGINS = {
    "superpowers@claude-plugins-official": {"min_version": "5.1.0", "purpose": "..."},
    "pr-review-toolkit@claude-plugins-official": {"min_version": None, "purpose": "..."},
}

def parse_version(ver_str: str) -> tuple: ...
def check_plugins(plugins_path: str | None = None) -> bool: ...
```

**Current working-tree state (uncommitted):**
The diff shows `return (0,)` changed to `return (sys.maxsize,)` in parse_version(). This change is NOT committed. The test file has NOT been updated to test this behavior.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test_unknown_version_satisfies_minimum to test_validate_deps.py</name>
  <files>.claude/scripts/test_validate_deps.py</files>
  <read_first>
    @.claude/scripts/test_validate_deps.py -- full file: existing test structure, _make_mock_plugins helper, _get_check_plugins loader, all 4 existing test methods (test_all_plugins_present, test_superpowers_missing, test_superpowers_version_too_low, test_pr_review_toolkit_missing)
    @.claude/scripts/validate-deps.py -- current parse_version implementation at line 26-41, check_plugins version comparison logic at line 93
  </read_first>
  <behavior>
    - Test 1: check_plugins with mock containing superpowers version "unknown" (min_version "5.1.0") and pr-review-toolkit version "unknown" (min_version None) returns True. This validates that "unknown" satisfies a non-None minimum version requirement.
    - Test 2: All 4 existing tests continue to pass (no regressions).
  </behavior>
  <action>
    Add a new test method `test_unknown_version_satisfies_minimum` to the `TestValidateDeps` class in `.claude/scripts/test_validate_deps.py`. Insert it after `test_all_plugins_present` and before `test_superpowers_missing`, so it appears as the second test in the class.

    The test must:
    1. Call `_get_check_plugins()` to load the module.
    2. Call `_make_mock_plugins()` with two entries: superpowers version "unknown" and pr-review-toolkit version "unknown". This is the key difference from `test_all_plugins_present` -- superpowers has min_version "5.1.0" (not None), so the version comparison branch IS reached.
    3. Call `check_plugins(mock_path)` and assert the result is True.
    4. Clean up the temp file with `os.unlink(path)` in a `finally` block.

    The mock entries dict:
    ```python
    {
        "superpowers@claude-plugins-official": {"version": "unknown"},
        "pr-review-toolkit@claude-plugins-official": {"version": "unknown"},
    }
    ```

    Do NOT modify any existing test methods. Do NOT modify the helper functions. The new method must follow the exact same pattern as `test_all_plugins_present` (load, create mock path, call check_plugins, assert True, finally cleanup).

    After adding the test, run the full suite: `python3 .claude/scripts/test_validate_deps.py -v`. All 5 tests must pass. This confirms the already-applied `(sys.maxsize,)` code fix works correctly for the "unknown satisfies minimum" case.
  </action>
  <verify>
    <automated>cd /home/rimuru/Projects/Code/homework/OS/tgoskits && python3 .claude/scripts/test_validate_deps.py -v 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - New method `test_unknown_version_satisfies_minimum` exists in `TestValidateDeps` class
    - The test calls `check_plugins()` with a mock where superpowers has version "unknown" and min_version "5.1.0"
    - The test asserts `check_plugins()` returns True
    - `python3 .claude/scripts/test_validate_deps.py -v` runs 5 tests, all pass (exit 0)
    - The test's XML output verifiable via: `python3 -c "import ast; t=ast.parse(open('.claude/scripts/test_validate_deps.py').read()); methods=[n.name for n in ast.walk(t) if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')]; print(methods)"` includes `test_unknown_version_satisfies_minimum`
  </acceptance_criteria>
  <done>Test case added, all 5 tests pass (GREEN), unknown version satisfies minimum version requirement</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit the parse_version fix and new test case</name>
  <files>.claude/scripts/validate-deps.py, .claude/scripts/test_validate_deps.py</files>
  <read_first>
    @.claude/scripts/validate-deps.py -- verify parse_version returns (sys.maxsize,) for "unknown"
    @.claude/scripts/test_validate_deps.py -- verify new test method exists and all 5 tests pass
  </read_first>
  <action>
    Stage both files and create a single commit with a conventional-commits message describing both the fix and the test addition.

    The commit message must use the pattern: `fix(01): parse_version('unknown') returns maxsize instead of (0,) to satisfy minimum version checks`

    Body: mention that (0,) < (5,1,0) was blocking dev-source plugins, the fix uses sys.maxsize so "unknown" satisfies any minimum version, and a new test case validates this specific behavior.

    Stage only these two files:
    ```bash
    git add .claude/scripts/validate-deps.py .claude/scripts/test_validate_deps.py
    ```

    Before committing, verify:
    1. `python3 .claude/scripts/test_validate_deps.py -v` runs 5 tests with 0 failures
    2. `python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('v', '.claude/scripts/validate-deps.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.parse_version('unknown') >= m.parse_version('5.1.0'); print('OK: unknown >= 5.1.0')"` prints "OK: unknown >= 5.1.0"
  </action>
  <verify>
    <automated>cd /home/rimuru/Projects/Code/homework/OS/tgoskits && python3 .claude/scripts/test_validate_deps.py -v 2>&1 && python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('v','.claude/scripts/validate-deps.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.parse_version('unknown') >= m.parse_version('5.1.0'); print('OK: unknown >= 5.1.0')" && git log -1 --oneline; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --cached --name-only` lists only `.claude/scripts/validate-deps.py` and `.claude/scripts/test_validate_deps.py`
    - `git log -1 --oneline` shows a new commit with message containing "parse_version" and/or "unknown"
    - `git show --stat HEAD` confirms both files were changed in the commit
    - `python3 .claude/scripts/test_validate_deps.py -v` runs 5 tests, all pass
  </acceptance_criteria>
  <done>Both files committed together, parse_version("unknown") now returns (sys.maxsize,), test suite covers this case</done>
</task>

</tasks>

<verification>
- `python3 .claude/scripts/test_validate_deps.py -v` -- 5 tests pass, including test_unknown_version_satisfies_minimum
- `python3 -c "import importlib.util; spec=importlib.util.spec_from_file_location('v','.claude/scripts/validate-deps.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.parse_version('unknown') >= m.parse_version('5.1.0'))"` -- prints True
- `git log -1 --oneline` -- shows new commit with the fix
</verification>

<success_criteria>
1. parse_version("unknown") returns (sys.maxsize,) which is >= any realistic version tuple
2. A plugin with version "unknown" and a non-None minimum version requirement (e.g., "5.1.0") passes check_plugins()
3. test_validate_deps.py has 5 tests, all passing, including a dedicated test for unknown-version acceptance
4. The fix is committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-risk-mitigation/01-03-SUMMARY.md`
</output>
