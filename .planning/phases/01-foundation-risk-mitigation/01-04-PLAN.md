---
phase: 01-foundation-risk-mitigation
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - .claude/agents/bug-hunt.md
  - .planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - FND-03
user_setup: []

must_haves:
  truths:
    - "bug-hunt preamble explicitly documents that complex debugging is routed through superpowers:systematic-debugging skill instead of spawning the debugger agent"
    - "01-02-SUMMARY.md accurately reflects that debugger agent spawn was intentionally replaced by skill-based routing, not preserved as originally claimed"
    - "The design rationale for skill-over-spawn is documented in both locations (preamble and SUMMARY) so future maintainers understand the tradeoff"
  artifacts:
    - path: ".claude/agents/bug-hunt.md"
      provides: "bug-hunt agent with clarified preamble documenting skill-based debug routing"
      contains: "### Dependency Check"
    - path: ".planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md"
      provides: "Corrected summary documenting the intentional debugger-to-skill design change"
      contains: "systematic-debugging"
  key_links:
    - from: ".claude/agents/bug-hunt.md preamble Agents section"
      to: "superpowers:systematic-debugging skill reference in Skills section"
      via: "explicit design rationale text"
      pattern: "systematic-debugging"
    - from: ".planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md"
      to: ".claude/agents/bug-hunt.md"
      via: "documented deviation from PLAN 02"
      pattern: "debugger"
---

<objective>
Close verification gap: bug-hunt preamble says "None" for Agents instead of listing `debugger` as specified in PLAN 02. The verification report identifies this as a NOT_WIRED key link and a FAILED truth.

Root cause: The implementation team replaced agent-spawning with skill invocation (`superpowers:systematic-debugging` skill handles complex debugging) but did not properly document this intentional design change. The 01-02-SUMMARY.md inaccurately claims "debugger and test-gen agent spawn references preserved (not removed)" when in fact zero occurrences of "debugger" exist in bug-hunt.md.

The approach: Document the intentional design decision (skill-based routing instead of agent spawn) in the preamble with an explicit rationale sentence, and correct the 01-02-SUMMARY.md to accurately reflect what was delivered.

Purpose: Eliminate the ambiguity about whether this is a bug or an intentional design choice. Future maintainers and Phase 3 (cross-plugin integration) planners need to know that bug-hunt uses skill-based debugging rather than agent delegation.

Output: Updated bug-hunt preamble with explicit design rationale, corrected 01-02-SUMMARY.md with accurate key-decision text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-risk-mitigation/01-VERIFICATION.md -- Gap 2: "Debugger spawn references in bug-hunt...validated by the preamble" FAILED, reason documented
@.planning/phases/01-foundation-risk-mitigation/01-02-PLAN.md -- Task 2 specification: bug-hunt preamble MUST list `debugger` in Agents section with text "(complex debugging, crashes, multi-core races)"
@.planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md -- Claims "debugger and test-gen agent spawn references preserved" but this is inaccurate
@.claude/agents/bug-hunt.md -- Current preamble (lines 21-38), body text showing superpowers:systematic-debugging usage (line 44-48)

**Current bug-hunt preamble Agents section (lines 32-33):**
```markdown
**Agents** (must be spawnable):
- None (complex debugging handled by `superpowers:systematic-debugging` skill)
```

**Current 01-02-SUMMARY.md key-decisions (lines 37-41):**
```yaml
key-decisions:
  - "Preamble section heading uses ### Dependency Check..."
  - "All 6 preamble blocks share identical structure..."
  - "security-auditor removal is definitive per D-09..."
  - "debugger and test-gen agent spawn references preserved (not removed)"   <-- INACCURATE
```

**Bug-hunt body text showing skill-based approach (lines 46-48):**
> "Before implementing fixes, invoke `superpowers:systematic-debugging` to follow a structured debugging process rather than guessing."
> "For complex debugging scenarios (crashes, memory corruption, multi-core races), invoke `superpowers:systematic-debugging` with the crash context for root cause analysis."

**Architectural context:**
Using a skill (same context window, inline) rather than spawning an agent (separate context window, overhead) is a legitimate design tradeoff for debugging. Skills operate within the agent's own context, avoiding the cost of sub-agent initialization. However, this deviation from PLAN 02 must be documented so Phase 3 planners know bug-hunt's actual dependency model.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Document the debugger-to-skill routing decision in bug-hunt preamble and correct 01-02-SUMMARY.md</name>
  <files>.claude/agents/bug-hunt.md, .planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md</files>
  <read_first>
    @.claude/agents/bug-hunt.md -- full file: current preamble lines 21-38, body text lines 44-48 showing systematic-debugging usage
    @.planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md -- full file: key-decisions block lines 37-41, Deviations from Plan section lines 96-98
    @.planning/phases/01-foundation-risk-mitigation/01-VERIFICATION.md -- Gap 2 details: NOT_WIRED key link, FAILED truth #10
  </read_first>
  <action>
    Make two targeted edits:

    **(A) Update bug-hunt preamble Agents section (`.claude/agents/bug-hunt.md` line 32-33):**

    Replace the current Agents section text:
    ```
    **Agents** (must be spawnable):
    - None (complex debugging handled by `superpowers:systematic-debugging` skill)
    ```
    with:
    ```
    **Agents** (must be spawnable):
    - None
    ```
    AND add a new paragraph immediately after the `AGENT ABORTED:` block quote, before "Do NOT proceed with degraded capabilities":

    ```
    Design note: Complex debugging (crashes, memory corruption, multi-core races) is routed through the `superpowers:systematic-debugging` skill listed above rather than spawning the `debugger` agent. Skill invocation operates within the same context window, avoiding sub-agent initialization overhead. If future phases require isolated agent delegation for debugging (e.g., parallel multi-repro analysis), the `debugger` agent can be added to this section at that time.
    ```

    The full updated preamble block (lines 21-38) will be:
    ```markdown
    ### Dependency Check

    Before executing any work, verify these dependencies are available:

    **Skills** (must resolve via installed plugins):
    - `superpowers:systematic-debugging` — structured debugging process
    - `superpowers:verification-before-completion` — confirm fixes before marking complete

    **Tools** (must be present in this context):
    - Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch

    **Agents** (must be spawnable):
    - None

    If any item above is missing, ABORT with:
    > "AGENT ABORTED: bug-hunt missing: LIST. Fix: claude plugins install NAMES"

    Design note: Complex debugging (crashes, memory corruption, multi-core races) is routed through the `superpowers:systematic-debugging` skill listed above rather than spawning the `debugger` agent. Skill invocation operates within the same context window, avoiding sub-agent initialization overhead. If future phases require isolated agent delegation for debugging (e.g., parallel multi-repro analysis), the `debugger` agent can be added to this section at that time.

    Do NOT proceed with degraded capabilities. Silent dependency failures in OS kernel workflows are a BLOCK-level risk.
    ```

    The preamble structure (### heading, **Skills**, **Tools**, **Agents**, AGENT ABORTED:) remains identical to all other agents.

    **(B) Correct 01-02-SUMMARY.md key-decisions (`.planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md` line 41):**

    Replace the inaccurate line:
    ```
      - "debugger and test-gen agent spawn references preserved (not removed)"
    ```
    with:
    ```
      - "debugger agent spawn replaced by superpowers:systematic-debugging skill invocation; test-gen agent spawn preserved"
    ```

    Also update the Deviations from Plan section (lines 96-98). Currently says "None - plan executed exactly as written." Add a new bullet:
    ```yaml
    - "bug-hunt Agents section: PLAN 02 specified `debugger` spawn target; implementation uses superpowers:systematic-debugging skill instead. This is an intentional design decision documented in the preamble's Design note."
    ```
    Specifically, change:
    ```
    None - plan executed exactly as written.
    ```
    to:
    ```
    - bug-hunt Agents section: PLAN 02 specified `debugger` spawn target; implementation uses superpowers:systematic-debugging skill instead. Intentional design decision (skill invocation avoids sub-agent context-switch overhead for debugging). Documented in bug-hunt preamble Design note.
    ```

    Do NOT modify any other content in either file. Do NOT change the preamble structure, headings, or abort message format.
  </action>
  <verify>
    <automated>cd /home/rimuru/Projects/Code/homework/OS/tgoskits && grep -A2 'must be spawnable' .claude/agents/bug-hunt.md && echo "---" && grep -c "Design note" .claude/agents/bug-hunt.md && echo "---" && grep "systematic-debugging skill invocation" .planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md && echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - bug-hunt.md preamble Agents section says "- None" (no parenthetical explanation on the same line)
    - bug-hunt.md preamble contains a "Design note:" paragraph between the AGENT ABORTED block quote and "Do NOT proceed with degraded capabilities"
    - Design note text contains "superpowers:systematic-debugging" AND "debugger" (both terms appear)
    - Design note text explains the tradeoff (skill = same context window, agent = isolated)
    - 01-02-SUMMARY.md `key-decisions` no longer contains the string "debugger and test-gen agent spawn references preserved"
    - 01-02-SUMMARY.md `key-decisions` contains "debugger agent spawn replaced by superpowers:systematic-debugging skill invocation"
    - 01-02-SUMMARY.md Deviations section no longer says "None - plan executed exactly as written" -- it now documents the bug-hunt deviation
    - Preamble structural consistency test passes: `bash .claude/scripts/test_preamble_consistency.sh` exits 0
  </acceptance_criteria>
  <done>bug-hunt preamble documented with design rationale, 01-02-SUMMARY.md accurately reflects the delivery</done>
</task>

</tasks>

<verification>
- `grep -A2 'must be spawnable' .claude/agents/bug-hunt.md` -- shows "- None"
- `grep 'Design note' .claude/agents/bug-hunt.md` -- matches 1 occurrence
- `grep 'debugger agent spawn replaced' .planning/phases/01-foundation-risk-mitigation/01-02-SUMMARY.md` -- matches
- `bash .claude/scripts/test_preamble_consistency.sh` -- exits 0
</verification>

<success_criteria>
1. bug-hunt preamble Agents section is clean ("- None") with design rationale in a separate Design note paragraph
2. Design note explains WHY skill-based routing was chosen over agent spawn (context window, overhead)
3. Design note documents that `debugger` agent can be re-added in future phases if needed
4. 01-02-SUMMARY.md accurately reflects the debugger-to-skill design change
5. No ambiguity remains about whether the "None" for Agents is a bug or intentional
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-risk-mitigation/01-04-SUMMARY.md`
</output>
