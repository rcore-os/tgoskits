---
phase: 02-self-evolve-enhancement
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - .claude/agents/self-evolve.md
autonomous: true
requirements:
  - SE-07
  - SE-08
must_haves:
  truths:
    - "D1 section invokes plugin-dev:plugin-structure skill for path/layout verification"
    - "D6 section invokes plugin-dev:plugin-settings skill for settings/config consistency"
    - "D7 section invokes plugin-dev:hook-development skill for hook structure validation"
    - "A brainstorming phase runs once before the first audit round, using superpowers:brainstorming methodology"
    - "superpowers:systematic-debugging is applied when quality issues are found during audit rounds"
    - "The existing D5 (Anti-patterns) section is unchanged"
    - "The existing D1-D7 round structure is preserved — skills are invoked inline, not as restructured workflow"
  artifacts:
    - path: ".claude/agents/self-evolve.md"
      provides: "Skill-invocation references in D1/D6/D7, new brainstorming phase, systematic-debugging cross-cut"
      contains: "plugin-dev:plugin-structure"
      contains: "plugin-dev:plugin-settings"
      contains: "plugin-dev:hook-development"
      contains: "superpowers:brainstorming"
      contains: "superpowers:systematic-debugging"
      contains: "Brainstorming Phase"
  key_links:
    - from: "self-evolve.md D1 section"
      to: "plugin-dev:plugin-structure skill"
      via: "inline skill invocation"
      pattern: "Invoke.*plugin-dev:plugin-structure"
    - from: "self-evolve.md D6 section"
      to: "plugin-dev:plugin-settings skill"
      via: "inline skill invocation"
      pattern: "Invoke.*plugin-dev:plugin-settings"
    - from: "self-evolve.md D7 section"
      to: "plugin-dev:hook-development skill"
      via: "inline skill invocation"
      pattern: "Invoke.*plugin-dev:hook-development"
    - from: "self-evolve.md brainstorming phase"
      to: "audit round workflow"
      via: "before Step 1: Audit"
      pattern: "before.*round.*1|prior.*to.*audit"
    - from: "self-evolve.md Step 3: Fix"
      to: "superpowers:systematic-debugging"
      via: "root-cause investigation gate"
      pattern: "systematic-debugging.*root.cause|Iron Law"
---

<objective>
Add skill invocation references to the D1 (Paths), D6 (Logical Completeness / Integration), and D7 (Hooks) audit dimensions. Add a new brainstorming phase before audit rounds using superpowers:brainstorming. Integrate superpowers:systematic-debugging as a cross-cutting tool applied when quality issues are found during audit rounds.

Purpose: D1, D6, and D7 currently rely on manual inspection. plugin-dev skills provide structured methodology for each dimension. The brainstorming phase enables self-evolve to ideate improvements before auditing, not just fix issues. Systematic-debugging ensures root causes are identified before fixes are applied, preventing symptom-level patches.

Output: self-evolve.md with skill references in D1, D6, D7 sections; a new brainstorming phase before the audit round workflow; and systematic-debugging integration in the Fix step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-self-evolve-enhancement/02-CONTEXT.md
@.planning/phases/02-self-evolve-enhancement/02-RESEARCH.md
@.claude/agents/self-evolve.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plugin-dev skill invocation references to D1, D6, and D7 sections</name>
  <files>.claude/agents/self-evolve.md</files>
  <read_first>
Read the current D1 (Paths), D6 (Logical Completeness), and D7 (Hook Integration) sections in `.claude/agents/self-evolve.md`. Also review RESEARCH.md section "Skills to Load in self-evolve Frontmatter" for the purpose of each skill, and "Architecture Patterns: System Architecture Diagram" for how skills map to dimensions.
  </read_first>
  <action>
Add skill invocation references to three audit dimensions. Each addition is an inline invocation within the existing section structure — do NOT restructure the sections, do NOT remove existing checklist items. The skill invocations ADD to the existing content; they do NOT replace it.

**D1: Path Reference Correctness (lines 55-75)**

Add at the start of the D1 checklist, before the first `- Every ...` item:
```
Before running path checks, invoke `plugin-dev:plugin-structure`:
- Review file/directory layout against plugin-dev's expected structure
- Verify manifest structure, naming conventions, and auto-discovery patterns
- Flag structural deviations that path checks alone would miss
```
Keep all existing D1 checklist items and the bash verification command block unchanged.

**D6: Logical Completeness (lines 153-161, note: line numbers may shift after Plan 02 modifications)**

Add at the start of the D6 checklist, before the first `- After reporting an error...` item:
```
Before evaluating logical completeness, invoke `plugin-dev:plugin-settings`:
- Review settings.json structure and completeness
- Check for .local.md settings pattern (if present)
- Validate settings YAML frontmatter and gitignore of sensitive values
- Verify defaults are documented and config is parseable
```
Keep all existing D6 checklist items unchanged. Note: the RESEARCH.md A1 assumption confirms plugin-settings (not mcp-integration) is the correct skill — TGOSKits has no MCP servers, `.mcp.json` does not exist, `settings.json` is empty `{}`.

**D7: Hook Integration (lines 163-170, note: line numbers may shift)**

Add at the start of the D7 checklist, before the first `- Commands that run Docker...` item:
```
Before evaluating hook integration, invoke `plugin-dev:hook-development`:
- Review hooks/hooks.json structure and completeness against plugin-dev's hook model
- Check hook scripts for security patterns: path traversal, input validation, unsafe shell expansion
- Verify ${CLAUDE_PLUGIN_ROOT} usage in hook script paths
- Validate hook events are properly matched to triggers
- Check for missing cleanup/error-handling hooks
```
Keep all existing D7 checklist items unchanged.

**General rules for all three additions:**
- Skills are invoked by name in body prose (e.g., "invoke `plugin-dev:plugin-structure`") — this is how Claude Code resolves skill references at runtime.
- The existing checklist items and bash commands are the fallback when plugin-dev is unavailable (per D-05). The skill invocations provide structured methodology; the existing checks provide the concrete verification steps.
- Per D-03: skills are loaded selectively by audit dimension, not all at once. Each dimension only invokes its assigned skill(s).
- The D1/D6/D7 sections must remain self-contained so each audit round can execute them independently.
</action>
  <verify>
Verify skill invocation references are present in the correct sections:
```bash
# D1: plugin-structure invocation
grep -c "plugin-dev:plugin-structure" .claude/agents/self-evolve.md
sed -n '/^### D1:/,/^### D2/p' .claude/agents/self-evolve.md | grep -c "plugin-dev:plugin-structure"

# D6: plugin-settings invocation
grep -c "plugin-dev:plugin-settings" .claude/agents/self-evolve.md
sed -n '/^### D6:/,/^### D7/p' .claude/agents/self-evolve.md | grep -c "plugin-dev:plugin-settings"

# D7: hook-development invocation
grep -c "plugin-dev:hook-development" .claude/agents/self-evolve.md
sed -n '/^### D7:/,/^---$/p' .claude/agents/self-evolve.md | grep -c "plugin-dev:hook-development"

# Existing checklist items preserved (sample checks)
grep -c "Every file path referenced" .claude/agents/self-evolve.md
grep -c "After reporting an error.*blocks progress" .claude/agents/self-evolve.md
grep -c "Commands that run Docker" .claude/agents/self-evolve.md

# D5 unchanged (anti-patterns)
grep -c "^### D5:" .claude/agents/self-evolve.md
```
All grep counts must be >= 1. D6/D7 checks should find the invocations within their respective sections.
</verify>
<acceptance_criteria>
  <behavior>D1 section invokes plugin-dev:plugin-structure before its path checklist, listing layout/structure/manifest verification items. D6 section invokes plugin-dev:plugin-settings before its completeness checklist, covering settings.json, .local.md pattern, YAML frontmatter, and gitignore. D7 section invokes plugin-dev:hook-development before its hook checklist, covering hooks.json structure, security patterns, variable usage, and event matching. All existing checklist items are preserved as the concrete verification layer. D5 is completely unchanged. Each skill invocation follows the "invoke `plugin-dev:skill-name`" prose pattern.</behavior>
  <test-command>grep -n "plugin-dev:plugin-structure\|plugin-dev:plugin-settings\|plugin-dev:hook-development" .claude/agents/self-evolve.md</test-command>
  <source-assertion>plugin-dev:plugin-structure appears within the D1 section, plugin-dev:plugin-settings within D6, plugin-dev:hook-development within D7. Each existing checklist is preserved below the skill invocation. D5 section has no plugin-dev references.</source-assertion>
</acceptance_criteria>
<done>D1 invokes plugin-dev:plugin-structure before its checklist. D6 invokes plugin-dev:plugin-settings before its checklist. D7 invokes plugin-dev:hook-development before its checklist. All existing checklist items preserved. D5 unchanged. Skill invocations follow selective-by-dimension loading per D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add brainstorming phase and systematic-debugging cross-cut to workflow</name>
  <files>.claude/agents/self-evolve.md</files>
  <read_first>
Read the current Workflow section in `.claude/agents/self-evolve.md` (lines 174-237: from "## Workflow" to the Rules section). Pay attention to the round structure (Step 1: Audit, Step 2: Classify & Report, Step 3: Fix, Step 4: Validate, Step 5: Report). Also read RESEARCH.md Open Question #2 ("When in the round does the brainstorming phase run?") and its recommendation: run once before round 1.
  </read_first>
  <action>
Make two additions to the self-evolve workflow:

**1. Add Brainstorming Phase before audit rounds (SE-07)**

Insert a new `### Brainstorming Phase (before first round)` section between the "## Workflow" header and "### Round N". This phase runs ONCE before round 1 (per RESEARCH.md OQ #2 recommendation: once before the first audit, not between rounds).

The section content:
```
### Brainstorming Phase (before first round)

Before starting the audit rounds, invoke `superpowers:brainstorming` to identify potential improvements:

1. **Explore**: Survey the TGOSKits plugin's current state — agents, commands, hooks, skills, scripts. What patterns exist? What feels manual or fragile?

2. **Clarify**: Identify the highest-value improvement targets. Which dimensions (D1-D7) consistently produce the most findings? Where would automation or structural changes yield the biggest quality gain?

3. **Propose**: Generate 3-5 concrete improvement ideas. For each: what would change, what files would be affected, what's the expected quality improvement, and what's the risk?

4. **Prioritize**: Rank proposals by (value / effort). Tag each with the audit dimension(s) it addresses.

Output the prioritized proposals as context for the audit rounds. The audit itself may discover additional issues, but the brainstorming output provides a strategic lens — issues that align with brainstormed proposals get elevated attention.

Note: This phase produces IDEAS, not changes. All actual modifications happen during audit rounds. If no significant improvements are identified, the phase produces a brief "no strategic changes identified" note.
```

**2. Add systematic-debugging to Step 3: Fix (SE-08)**

In the existing "#### Step 3: Fix" section (currently: "Fix ALL BLOCK items first, then WARN items. Apply minimal, targeted changes — fix only the issue, no refactoring."), add before the existing text:

```
Before applying any fix, invoke `superpowers:systematic-debugging` for root-cause analysis:
- For each BLOCK or recurring WARN finding, investigate WHY the issue exists, not just WHAT the symptom is.
- Apply the Iron Law: no fixes without root cause investigation. A fix that only addresses the symptom will reappear in a later round.
- Document the root cause alongside the fix in the round report.
```

Keep the existing "Fix ALL BLOCK items first, then WARN items. Apply minimal, targeted changes — fix only the issue, no refactoring." text unchanged after the new addition.

**3. Update the existing Step 5 report format**

In "#### Step 5: Report round summary", add a note that the round report should mention when systematic-debugging was applied:
```
> "Round N complete: fixed X BLOCK, Y WARN. Z items deferred. Root causes investigated: R. Moving to round N+1."
```

**Rules for these additions:**
- The brainstorming phase is a NEW section in the workflow, not a modification to existing sections.
- The systematic-debugging reference is an ADDITION to Step 3, not a replacement.
- Per RESEARCH.md Pitfall 4 (Cyclic Audit Loop Breakage): keep the D1-D7 round structure intact. The brainstorming phase is outside the loop (before round 1). The systematic-debugging reference enhances Step 3 without restructuring it.
- The brainstorming skill and systematic-debugging skill are loaded in the frontmatter (Plan 01) and invoked here by name.
</action>
  <verify>
Verify workflow additions:
```bash
# Brainstorming phase section exists
grep -c "### Brainstorming Phase" .claude/agents/self-evolve.md

# Brainstorming is before Round N section
grep -n "### Brainstorming Phase\|### Round N" .claude/agents/self-evolve.md

# Systematic-debugging in Step 3: Fix
grep -c "superpowers:systematic-debugging" .claude/agents/self-evolve.md
sed -n '/#### Step 3: Fix/,/#### Step 4:/p' .claude/agents/self-evolve.md | grep -c "systematic-debugging"

# Iron Law reference
grep -c "Iron Law" .claude/agents/self-evolve.md

# Root cause mention in Step 5 report
sed -n '/#### Step 5:/,/### /p' .claude/agents/self-evolve.md | grep -c "root cause"

# Existing round structure preserved
grep -c "#### Step 1: Audit" .claude/agents/self-evolve.md
grep -c "#### Step 2: Classify" .claude/agents/self-evolve.md
grep -c "#### Step 3: Fix" .claude/agents/self-evolve.md
grep -c "#### Step 4: Validate" .claude/agents/self-evolve.md
grep -c "#### Step 5: Report" .claude/agents/self-evolve.md
```
All grep counts must be >= 1. The line number check should confirm brainstorming appears before Round N.
</verify>
<acceptance_criteria>
  <behavior>A new "Brainstorming Phase" section exists between "## Workflow" header and "### Round N", running once before the first audit round using superpowers:brainstorming's explore/clarify/propose/prioritize methodology. The existing "#### Step 3: Fix" is enhanced with a superpowers:systematic-debugging invocation for root-cause analysis before fixes, citing the Iron Law ("no fixes without root cause investigation"). The Step 5 round summary format includes root-cause tracking. The existing D1-D7 round structure is intact (Pitfall 4 avoidance).</behavior>
  <test-command>sed -n '/^## Workflow/,/^## Rules/p' .claude/agents/self-evolve.md</test-command>
  <source-assertion>The workflow section contains: Brainstorming Phase (explore/clarify/propose/prioritize), Round N with all 5 steps, systematic-debugging in Step 3 with Iron Law reference, root-cause tracking in Step 5 report format, and the existing Final Report section unchanged.</source-assertion>
</acceptance_criteria>
<done>Brainstorming phase added before Round N with explore/clarify/propose/prioritize methodology. Step 3: Fix enhanced with systematic-debugging root-cause analysis (Iron Law). Step 5 report format updated with root-cause tracking. Existing round structure and Final Report section preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-evolve brainstorming phase -> plugin improvement proposals | Brainstorming output is advisory — proposals are not auto-applied |
| self-evolve Step 3 Fix -> systematic-debugging investigation | Root cause analysis reads plugin files (read-only); fixes are applied via Edit/Write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing | Brainstorming phase proposals | mitigate | Proposals are explicitly advisory ("produces IDEAS, not changes"); all actual modifications happen during audit rounds with explicit user confirmation gates |
| T-02-08 | Denial of Service | Systematic-debugging root-cause loop | mitigate | Per existing self-evolve rules: "If the same WARN issue persists for 2 consecutive rounds without a clear fix, defer it" — prevents infinite investigation loops |
| T-02-09 | Information Disclosure | Skill invocations in D1/D6/D7 | accept | Skills read plugin files within `.claude/` scope (already accessible); no new data access paths introduced |
</threat_model>

<verification>
After both tasks complete, verify overall workflow integrity:
```bash
# Audit dimensions D1-D7 all present (6 header sections: D1, D2+D3, D4, D5, D6, D7)
grep -c "^### D[1-7]" .claude/agents/self-evolve.md

# Workflow structure complete
grep -c "^### Brainstorming Phase" .claude/agents/self-evolve.md
grep -c "^### Round N" .claude/agents/self-evolve.md
grep -c "^### Final Report" .claude/agents/self-evolve.md

# Rules section preserved
grep -c "^## Rules" .claude/agents/self-evolve.md

# All skills referenced in body (matching frontmatter skills from Plan 01)
for skill in plugin-structure skill-development agent-development plugin-settings hook-development brainstorming systematic-debugging verification-before-completion; do
  count=$(grep -c "$skill" .claude/agents/self-evolve.md)
  echo "$skill: $count references"
done

# Verify no mcp-integration references (should use plugin-settings per A1)
grep -c "mcp-integration" .claude/agents/self-evolve.md || echo "0 (correct)"
```
</verification>

<success_criteria>
1. D1 section invokes plugin-dev:plugin-structure before its path checklist
2. D6 section invokes plugin-dev:plugin-settings before its completeness checklist
3. D7 section invokes plugin-dev:hook-development before its hook checklist
4. All existing checklist items in D1, D6, D7 are preserved unchanged
5. D5 (Anti-patterns) section is completely unchanged
6. Brainstorming phase runs once before the first audit round with explore/clarify/propose/prioritize methodology
7. Step 3: Fix invokes systematic-debugging for root-cause analysis with Iron Law reference
8. Step 5 round report includes root-cause tracking
9. Existing round structure (5 steps) and Final Report section are preserved
10. No references to plugin-dev:mcp-integration (correctly uses plugin-settings per A1)
</success_criteria>

<output>
After completion, create `.planning/phases/02-self-evolve-enhancement/02-03-SUMMARY.md`
</output>
