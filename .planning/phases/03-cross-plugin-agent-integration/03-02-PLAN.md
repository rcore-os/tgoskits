---
phase: 03-cross-plugin-agent-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/driver-audit.md
autonomous: true
requirements:
  - CPI-11
  - CPI-15
must_haves:
  truths:
    - "driver-audit frontmatter skills list includes superpowers:systematic-debugging alongside existing skills"
    - "driver-audit Dependency Check preamble lists pr-review-toolkit:type-design-analyzer in the Agents section"
    - "driver-audit body has a new E. Trait Design Review section after the four-layer audit that spawns type-design-analyzer conditionally"
    - "type-design-analyzer spawn happens only when audit scope includes trait definitions or capability interfaces (CPI-15, D-06)"
    - "type-design-analyzer spawn uses full pr-review-toolkit: prefix per Pitfall 1 avoidance"
    - "Fallback documented: if sub-agent unavailable, warn and perform manual trait review"
    - "No structural changes to driver-audit workflow — new skill and spawn are additive per D-07"
    - "Existing skills (cross-kernel-driver, superpowers:verification-before-completion) remain unchanged"
  artifacts:
    - path: ".claude/agents/driver-audit.md"
      provides: "Updated frontmatter skills, preamble Agents section, and new Trait Design Review body section"
      contains: "superpowers:systematic-debugging"
      contains: "pr-review-toolkit:type-design-analyzer"
      contains: "### E. Trait Design Review"
  key_links:
    - from: "driver-audit.md preamble Agents section"
      to: "driver-audit.md body ### E. Trait Design Review"
      via: "sub-agent spawn reference in body prose"
      pattern: "pr-review-toolkit:type-design-analyzer"
    - from: "driver-audit.md frontmatter skills:"
      to: "driver-audit.md body ### Global Capabilities"
      via: "skill name reference in body prose"
      pattern: "systematic-debugging"
---

<objective>
Add superpowers:systematic-debugging skill (CPI-11) to driver-audit frontmatter, add pr-review-toolkit:type-design-analyzer to the preamble Agents section (CPI-15, D-06), and add a new Trait Design Review body section that conditionally spawns type-design-analyzer for trait/capability interface review.

Purpose: driver-audit is the second-simplest integration in this phase — one skill addition and one conditional sub-agent spawn. The systematic-debugging skill provides root-cause analysis for layering violations. The type-design-analyzer sub-agent reviews trait/capability interfaces when the audit scope covers trait definitions.

Output: driver-audit.md with 3 skills in frontmatter (2 existing + 1 new), an expanded preamble Agents section listing type-design-analyzer, and a new E. Trait Design Review body section after the existing four-layer audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-plugin-agent-integration/03-CONTEXT.md
@.planning/phases/03-cross-plugin-agent-integration/03-RESEARCH.md
@.claude/agents/driver-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add systematic-debugging skill to frontmatter and type-design-analyzer to preamble Agents section</name>
  <files>.claude/agents/driver-audit.md</files>
  <read_first>
Read the current `.claude/agents/driver-audit.md` frontmatter block (lines 1-12) and Dependency Check preamble (lines 15-31). The current `skills:` field has two entries: `cross-kernel-driver`, `superpowers:verification-before-completion`. The current Agents section says "None".
  </read_first>
  <action>
Two changes to driver-audit.md, both in the top portion of the file:

**Change 1 — Frontmatter skills:**
Expand the `skills:` field to include `superpowers:systematic-debugging` (CPI-11). Per D-05, skills are loaded via frontmatter only. The final list (in order):
1. `cross-kernel-driver` (existing, keep first)
2. `superpowers:verification-before-completion` (existing, keep second)
3. `superpowers:systematic-debugging` (NEW — CPI-11: layering violation root-cause analysis)

Keep `tools:` (Read, Grep, Glob), `name:`, and `description:` unchanged.

**Change 2 — Preamble Agents section:**
Change the Agents section from:
```
**Agents** (must be spawnable):
- None
```
To:
```
**Agents** (must be spawnable):
- `pr-review-toolkit:type-design-analyzer` — trait/capability interface design review (spawned conditionally during audit when trait definitions are in scope; fallback: manual trait review)

Per D-06, spawn references appear in preamble Agents section plus body description. Use the full `pr-review-toolkit:` prefix per RESEARCH.md Pitfall 1 (avoid collision with any standalone type-design-analyzer plugin).
```

Add the new skill to the Skills section of the preamble:
```
- `superpowers:systematic-debugging` — root-cause analysis for layering violations
```

List it before the existing `superpowers:verification-before-completion` entry in the preamble Skills list (matching the pattern where new skills are listed first).

Do NOT change the ABORT gate text or tools section. The preamble structure (Skills/Tools/Agents with ABORT message) must follow the Phase 01 standardized pattern.
  </action>
  <verify>
Verify frontmatter and preamble changes:
```bash
# Frontmatter: new skill present
grep -c "superpowers:systematic-debugging" .claude/agents/driver-audit.md

# Preamble Agents: type-design-analyzer listed
grep -c "pr-review-toolkit:type-design-analyzer" .claude/agents/driver-audit.md

# Preamble Agents: fallback documented
grep -c "fallback.*manual.*trait" .claude/agents/driver-audit.md

# Preamble Skills: systematic-debugging listed
sed -n '/^### Dependency Check/,/^# /p' .claude/agents/driver-audit.md | grep -c "systematic-debugging"

# ABORT gate preserved
grep -c "AGENT ABORTED" .claude/agents/driver-audit.md

# Existing skills preserved
grep -c "cross-kernel-driver" .claude/agents/driver-audit.md
grep -c "superpowers:verification-before-completion" .claude/agents/driver-audit.md
```
All grep counts must be >= 1.
</verify>
<acceptance_criteria>
  <behavior>Frontmatter skills list has 3 entries in order: cross-kernel-driver, superpowers:verification-before-completion, superpowers:systematic-debugging. Preamble Skills section lists systematic-debugging. Preamble Agents section lists pr-review-toolkit:type-design-analyzer with fallback documentation. ABORT gate preserved. No other frontmatter fields changed.</behavior>
  <test-command>sed -n '/^### Dependency Check/,/^# /p' .claude/agents/driver-audit.md</test-command>
  <source-assertion>The Dependency Check preamble contains Skills (3 entries), Tools (3 entries, unchanged), and Agents (1 entry with pr-review-toolkit:type-design-analyzer and fallback). The ABORT gate text is preserved.</source-assertion>
</acceptance_criteria>
<done>driver-audit.md frontmatter has 3 skills. Preamble Skills lists systematic-debugging. Preamble Agents lists pr-review-toolkit:type-design-analyzer with fallback documentation. ABORT gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add E. Trait Design Review section to driver-audit body for conditional type-design-analyzer spawn</name>
  <files>.claude/agents/driver-audit.md</files>
  <read_first>
Read the current body of `.claude/agents/driver-audit.md`, specifically the Global Capabilities section (lines 34-40) and the workflow sections A through D (lines 79-171). Also read RESEARCH.md Code Example "driver-audit: Sub-Agent Spawn for Trait Review" for the exact spawn text to use.
  </read_first>
  <action>
Per D-07 (additive only, no structural changes), add two enhancements to the driver-audit body:

**Enhancement 1 — Global Capabilities:**
After the existing sentence "For MMIO/DMA API usage questions, use `context7` MCP to look up the `mmio-api` and `dma-api` crate documentation." add:
```
For root-cause analysis of layering violations, invoke `superpowers:systematic-debugging` — trace the violation through all four layers (Driver Core -> Capability Boundary -> OS Glue -> Runtime) to identify the exact architectural boundary breach rather than treating symptoms.
```

**Enhancement 2 — New E. Trait Design Review section (after D. Runtime checks, before Step 3: Generate AUDIT.md):**
Insert a new section `### E. Trait Design Review` between the existing D. Runtime checks block and the Step 3: Generate AUDIT.md section. Use the text from RESEARCH.md Code Example "driver-audit: Sub-Agent Spawn for Trait Review":

```
### E. Trait Design Review (NEW — CPI-15)

After checking the four layers, if the audit scope includes trait definitions or capability interfaces, spawn `pr-review-toolkit:type-design-analyzer`:

> "Review the trait definition [trait name] in [file:line]. Evaluate encapsulation, invariant expression, and whether the interface is minimal and complete."

Incorporate findings into the AUDIT.md report as an additional section:
```markdown
## Trait Design Review

### <file>:<line> — <trait name>
**Encapsulation**: <rating>
**Invariant Expression**: <rating>
**Usefulness**: <rating>
**Enforcement**: <rating>
**Recommendation**: <finding summary>
```

If `pr-review-toolkit:type-design-analyzer` is unavailable, warn and perform manual trait design review:
- Check that trait methods are minimal (no unnecessary methods)
- Verify trait bounds are not overly restrictive
- Confirm trait is at the correct abstraction layer (Driver Core vs Capability Boundary)
- Check for missing Send/Sync bounds if the trait is used across thread boundaries
```
  </action>
  <verify>
Verify the new section and Global Capabilities reference:
```bash
# New section exists
grep -c "### E. Trait Design Review" .claude/agents/driver-audit.md

# type-design-analyzer spawn referenced in body
grep -c "pr-review-toolkit:type-design-analyzer" .claude/agents/driver-audit.md

# Fallback path documented in body
grep -c "unavailable.*manual trait" .claude/agents/driver-audit.md

# systematic-debugging referenced in Global Capabilities
sed -n '/^## Global Capabilities/,/^## The Four Layers/p' .claude/agents/driver-audit.md | grep -c "systematic-debugging"

# Existing sections preserved
grep -c "^### A. Driver Core checks" .claude/agents/driver-audit.md
grep -c "^### B. Capability Boundary checks" .claude/agents/driver-audit.md
grep -c "^### C. OS Glue checks" .claude/agents/driver-audit.md
grep -c "^### D. Runtime checks" .claude/agents/driver-audit.md
grep -c "^### Step 3: Generate AUDIT.md" .claude/agents/driver-audit.md
grep -c "^### Step 4: Report to user" .claude/agents/driver-audit.md
```
All grep counts must be >= 1.
</verify>
<acceptance_criteria>
  <behavior>Global Capabilities references superpowers:systematic-debugging for layering violation root-cause analysis. New E. Trait Design Review section is positioned after D. Runtime checks and before Step 3. Section documents conditional spawn of pr-review-toolkit:type-design-analyzer for trait definitions in audit scope, with fallback to manual trait review when sub-agent is unavailable. All existing A-D sections and workflow Steps 1-4 are preserved.</behavior>
  <test-command>sed -n '/^### D. Runtime checks/,/^### Step 3: Generate AUDIT.md/p' .claude/agents/driver-audit.md</test-command>
  <source-assertion>The E. Trait Design Review section is placed between D. Runtime checks and Step 3: Generate AUDIT.md. It documents the conditional spawn, fallback manual review, and the markdown template for incorporating findings into AUDIT.md.</source-assertion>
</acceptance_criteria>
<done>driver-audit body has Global Capabilities reference to systematic-debugging. New E. Trait Design Review section added between D and Step 3 with conditional type-design-analyzer spawn and fallback documentation. All existing workflow sections preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver-audit -> type-design-analyzer sub-agent | Spawn instructions passed as text; sub-agent reads trait source code from the audit scope |
| driver-audit frontmatter -> plugin resolution | Skill names declared in frontmatter must resolve against installed plugins at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | driver-audit frontmatter skills list | mitigate | superpowers:systematic-debugging is from official marketplace, installed via `claude plugins install` |
| T-03-04 | Spoofing | type-design-analyzer sub-agent spawn | mitigate | Full prefix `pr-review-toolkit:type-design-analyzer` avoids collision with any standalone type-design-analyzer plugin (Pitfall 1) |
| T-03-05 | Denial of Service | Sub-agent unavailability | mitigate | Fallback to manual trait review documented; driver-audit continues functionality (graceful degradation per Phase 01/02 pattern) |
</threat_model>

<verification>
After both tasks complete, verify the entire plan:
```bash
# Frontmatter skill count (3 total)
sed -n '/^skills:/,/^tools:/p' .claude/agents/driver-audit.md | grep -c "^- "

# Preamble Agents has exactly 1 entry (was None, now type-design-analyzer)
sed -n '/^\*\*Agents\*\*/,/^$/p' .claude/agents/driver-audit.md | grep -c "pr-review-toolkit"

# Body has systematic-debugging reference in Global Capabilities
sed -n '/^## Global Capabilities/,/^## The Four Layers/p' .claude/agents/driver-audit.md | grep -c "systematic-debugging"

# Body has E. Trait Design Review section
grep -c "^### E. Trait Design Review" .claude/agents/driver-audit.md

# No unintended removal of existing sections
grep -c "^### Step 1: Determine scope" .claude/agents/driver-audit.md
grep -c "^### Step 2: Per-file audit" .claude/agents/driver-audit.md
```
</verification>

<success_criteria>
1. driver-audit.md frontmatter has 3 skills (2 existing + 1 new) with correct YAML indentation
2. Dependency Check preamble Agents section lists pr-review-toolkit:type-design-analyzer with fallback documentation
3. Preamble Skills section lists systematic-debugging
4. Body Global Capabilities references systematic-debugging for layering violation analysis
5. Body has new E. Trait Design Review section between D. Runtime checks and Step 3
6. E. Trait Design Review documents conditional spawn and fallback manual review
7. Full `pr-review-toolkit:` prefix used per Pitfall 1 avoidance
8. No structural changes to existing A-D audit sections or Steps 1-4 workflow
9. Frontmatter YAML parses correctly (no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-plugin-agent-integration/03-02-SUMMARY.md`
</output>
