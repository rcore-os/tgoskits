---
phase: 03-cross-plugin-agent-integration
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - .claude/agents/impl.md
autonomous: true
requirements:
  - CPI-08
  - CPI-13
  - CPI-14
must_haves:
  truths:
    - "impl frontmatter skills list includes superpowers:systematic-debugging alongside existing skills"
    - "impl Dependency Check preamble lists pr-review-toolkit:type-design-analyzer and pr-review-toolkit:code-simplifier in the Agents section"
    - "impl body has a 'When introducing new types or traits' section in Phase 4 that conditionally spawns type-design-analyzer (CPI-13, D-06)"
    - "impl body has a 'Post-implementation code polish' section in Phase 4 that spawns code-simplifier after all implementation phases complete (CPI-14)"
    - "type-design-analyzer spawn uses full pr-review-toolkit: prefix per Pitfall 1 avoidance"
    - "code-simplifier spawn uses full pr-review-toolkit: prefix per Pitfall 1 avoidance (avoids collision with standalone code-simplifier plugin)"
    - "Both sub-agent spawns have fallback: if unavailable, warn and continue with manual review"
    - "systematic-debugging referenced in Global Capabilities for implementation debugging"
    - "No structural changes to impl 6-phase workflow — new skill and spawns are additive per D-07"
    - "Existing skills (starry-test-suit, arceos-test-adapter, superpowers:verification-before-completion) remain unchanged"
  artifacts:
    - path: ".claude/agents/impl.md"
      provides: "Updated frontmatter skills, preamble Agents section, new sub-agent spawn sections in Phase 4, and Global Capabilities reference"
      contains: "superpowers:systematic-debugging"
      contains: "pr-review-toolkit:type-design-analyzer"
      contains: "pr-review-toolkit:code-simplifier"
      contains: "When introducing new types or traits"
      contains: "Post-implementation code polish"
  key_links:
    - from: "impl.md preamble Agents section"
      to: "impl.md body Phase 4: IMPLEMENT"
      via: "sub-agent spawn references in body prose"
      pattern: "pr-review-toolkit:(type-design-analyzer|code-simplifier)"
    - from: "impl.md frontmatter skills:"
      to: "impl.md body Global Capabilities"
      via: "skill name reference in body prose"
      pattern: "systematic-debugging"
---

<objective>
Add superpowers:systematic-debugging skill (CPI-08) to impl frontmatter, add pr-review-toolkit:type-design-analyzer (CPI-13) and pr-review-toolkit:code-simplifier (CPI-14) to the preamble Agents section (D-06), and add two new sub-agent spawn sections in Phase 4: one for new type/trait review and one for post-implementation code polish.

Purpose: impl is medium complexity — one skill plus two conditional sub-agent spawns. The systematic-debugging skill provides structured debugging during implementation. The type-design-analyzer spawns when new types or traits are introduced (before method implementation). The code-simplifier spawns after all implementation phases complete (before CI loop). Both bracket the implementation lifecycle.

Output: impl.md with 4 skills in frontmatter (3 existing + 1 new), an expanded preamble Agents section listing type-design-analyzer and code-simplifier, and two new sub-agent spawn sections in Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-plugin-agent-integration/03-CONTEXT.md
@.planning/phases/03-cross-plugin-agent-integration/03-RESEARCH.md
@.claude/agents/impl.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add systematic-debugging skill to frontmatter and type-design-analyzer + code-simplifier to preamble Agents</name>
  <files>.claude/agents/impl.md</files>
  <read_first>
Read the current `.claude/agents/impl.md` frontmatter block (lines 1-17) and Dependency Check preamble (lines 19-37). The current `skills:` field has three entries: `starry-test-suit`, `arceos-test-adapter`, `superpowers:verification-before-completion`. The current Agents section lists `test-gen` only. The current Skills section in the preamble lists only verification-before-completion.
  </read_first>
  <action>
Three changes to the top portion of impl.md:

**Change 1 — Frontmatter skills (CPI-08):**
Expand the `skills:` field to include `superpowers:systematic-debugging`. Per D-05, skills are loaded via frontmatter only. The final list (in order):
1. `starry-test-suit` (existing)
2. `arceos-test-adapter` (existing)
3. `superpowers:verification-before-completion` (existing)
4. `superpowers:systematic-debugging` (NEW — CPI-08: structured debugging during implementation)

Keep `tools:` (Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch), `name:`, and `description:` unchanged. Per RESEARCH.md Pitfall 2, impl gets exactly 1 new skill — do not exceed.

**Change 2 — Preamble Skills section:**
Add the new skill:
```
- `superpowers:systematic-debugging` — structured debugging process for implementation issues
- `superpowers:verification-before-completion` — confirm phase outputs before moving on
```

List systematic-debugging before verification-before-completion (pattern: new entries first).

**Change 3 — Preamble Agents section (CPI-13, CPI-14, D-06):**
Replace the current Agents section with:
```
**Agents** (must be spawnable):
- `test-gen` — test case generation
- `pr-review-toolkit:type-design-analyzer` — type/trait design review (spawned conditionally when introducing new types or traits; fallback: manual type design review)
- `pr-review-toolkit:code-simplifier` — post-implementation code polish (spawned once after all implementation phases complete; fallback: manual simplification pass)

Per D-06, spawn references appear in preamble Agents section plus body description. Use the full `pr-review-toolkit:` prefix per RESEARCH.md Pitfall 1 (avoids collision with standalone code-simplifier plugin version 1.0.0). The existing `test-gen` agent entry is preserved as the first entry.
```

Keep the ABORT gate text unchanged. Keep the warning message "Do NOT proceed with degraded capabilities" unchanged.
  </action>
  <verify>
Verify all three changes:
```bash
# Frontmatter: systematic-debugging present
grep -c "superpowers:systematic-debugging" .claude/agents/impl.md

# Preamble Skills: systematic-debugging listed
sed -n '/^### Dependency Check/,/^# /p' .claude/agents/impl.md | grep -c "systematic-debugging"

# Preamble Agents: type-design-analyzer listed
grep -c "pr-review-toolkit:type-design-analyzer" .claude/agents/impl.md

# Preamble Agents: code-simplifier listed
grep -c "pr-review-toolkit:code-simplifier" .claude/agents/impl.md

# Preamble Agents: fallback documented for both
grep -c "fallback.*manual.*type" .claude/agents/impl.md
grep -c "fallback.*manual.*simplif" .claude/agents/impl.md

# Existing test-gen agent entry preserved
grep -c "test-gen.*test case generation" .claude/agents/impl.md

# ABORT gate preserved
grep -c "AGENT ABORTED" .claude/agents/impl.md

# Frontmatter skill count
sed -n '/^skills:/,/^tools:/p' .claude/agents/impl.md | grep -c "^- "
```
All grep counts must be >= 1. Frontmatter skill count must be 4.
</verify>
<acceptance_criteria>
  <behavior>Frontmatter skills list has 4 entries in the specified order. Preamble Skills lists systematic-debugging before verification-before-completion. Preamble Agents lists 3 entries: test-gen (existing), pr-review-toolkit:type-design-analyzer (new, with fallback), pr-review-toolkit:code-simplifier (new, with fallback). ABORT gate and warning text preserved.</behavior>
  <test-command>sed -n '/^### Dependency Check/,/^# /p' .claude/agents/impl.md</test-command>
  <source-assertion>The Dependency Check preamble contains Skills (2 entries: systematic-debugging, verification-before-completion), Tools (8 entries), and Agents (3 entries: test-gen, type-design-analyzer, code-simplifier with fallback annotations).</source-assertion>
</acceptance_criteria>
<done>impl.md frontmatter has 4 skills. Preamble Skills lists systematic-debugging. Preamble Agents lists test-gen, type-design-analyzer, and code-simplifier with fallback documentation. ABORT gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add systematic-debugging to Global Capabilities and sub-agent spawn sections to Phase 4</name>
  <files>.claude/agents/impl.md</files>
  <read_first>
Read the current body of `.claude/agents/impl.md`, specifically the Global Capabilities section (lines 39-49) and Phase 4: IMPLEMENT (lines 266-335). Also read RESEARCH.md Code Examples "impl: Sub-Agent Spawn for New Types" and "impl: Post-implementation code polish" for the exact spawn invocation text.
  </read_first>
  <action>
Per D-07 (additive only, no structural changes), add three enhancements to the impl body:

**Enhancement 1 — Global Capabilities (CPI-08):**
After the existing sentence "Before completing each phase, invoke `superpowers:verification-before-completion` to confirm outputs are correct before moving on." add:
```
During implementation debugging, invoke `superpowers:systematic-debugging` to follow a structured debugging process: define the expected behavior, isolate the component, identify root cause, fix, and verify — rather than trial-and-error patching.
```

**Enhancement 2 — Phase 4: New sub-section between 4a and existing content (CPI-13):**
Insert a new `### 4a.5: Type/Trait Design Review` section after the existing 4a "Implement P0 items" bullet list but before the `★ Insight` separator. This section conditionally spawns type-design-analyzer for new types:

```
### 4a.5: Type/Trait Design Review (NEW — CPI-13)

If the implementation introduces new types or traits (structs with custom invariants, enums, trait definitions, or trait impls), spawn `pr-review-toolkit:type-design-analyzer` BEFORE writing method implementations:

> "Review the new type/trait [name] defined in [file:line]. Evaluate encapsulation, invariant expression, usefulness, and enforcement on the 4-axis rating scale."

Incorporate type-design-analyzer findings before proceeding with method implementations. If the sub-agent identifies encapsulation violations or unenforced invariants, fix the type design before writing methods — changing a type after methods are written is more expensive.

If `pr-review-toolkit:type-design-analyzer` is unavailable, warn and perform manual type design review:
- Verify the type's fields are appropriately private (encapsulation)
- Check that invariants are enforced by constructor/accessor methods
- Ensure the type's public API is minimal and complete
- Verify Send/Sync bounds if the type crosses thread boundaries
```

**Enhancement 3 — Phase 4: New sub-section after 4c (CPI-14):**
Insert a new `### 4c.5: Post-Implementation Code Polish` section between the existing 4c "Implement P2 stubs" and 4d "Build and run on StarryOS". This section spawns code-simplifier after implementation:

```
### 4c.5: Post-Implementation Code Polish (NEW — CPI-14)

After all P0/P1/P2 implementation is complete but BEFORE building and running on StarryOS (Phase 4d), spawn `pr-review-toolkit:code-simplifier`:

> "Review the recently modified files in this implementation for code simplification opportunities. Focus on: unnecessary nesting, redundant error handling, verbose pattern matching that could use combinators, and code that could leverage standard library APIs. Preserve all functionality and POSIX/Linux semantics while improving clarity."

Apply refinements from code-simplifier output, then proceed to Phase 4d (Build and run on StarryOS).

If `pr-review-toolkit:code-simplifier` is unavailable, warn and proceed with a manual simplification pass:
- Check for deeply nested `if let` / `match` chains that could use `map`/`and_then`/`ok_or`
- Verify error propagation uses `?` operator consistently
- Look for repeated code blocks that could be extracted into helper functions
```

Do NOT change the existing 4a/4b/4c/4d/4e section structure or numbering. Do NOT modify Phase 1, 2, 3, 5, 6, or any other section. The new sections are additive per D-07.
  </action>
  <verify>
Verify all three enhancements:
```bash
# systematic-debugging in Global Capabilities
sed -n '/^## Global Capabilities/,/^## Input Modes/p' .claude/agents/impl.md | grep -c "systematic-debugging"

# New type-design-analyzer section exists
grep -c "### 4a.5: Type/Trait Design Review" .claude/agents/impl.md

# type-design-analyzer spawn instruction present
grep -c "pr-review-toolkit:type-design-analyzer" .claude/agents/impl.md

# New code-simplifier section exists
grep -c "### 4c.5: Post-Implementation Code Polish" .claude/agents/impl.md

# code-simplifier spawn instruction present
grep -c "pr-review-toolkit:code-simplifier" .claude/agents/impl.md

# Both fallback blocks present
grep -c "unavailable.*manual type" .claude/agents/impl.md
grep -c "unavailable.*manual.*simplif" .claude/agents/impl.md

# Existing sections preserved
grep -c "^### 4a: Implement P0 items" .claude/agents/impl.md
grep -c "^### 4d: Build and run on StarryOS" .claude/agents/impl.md
grep -c "^### 4e: Verify with syscall-diff" .claude/agents/impl.md
grep -c "^## Phase 6: PR" .claude/agents/impl.md
```
All grep counts must be >= 1. pr-review-toolkit references should each appear twice (preamble + body).
</verify>
<acceptance_criteria>
  <behavior>Global Capabilities references superpowers:systematic-debugging for structured implementation debugging. Phase 4 has a new 4a.5 section that conditionally spawns type-design-analyzer for new types/traits before method implementation. Phase 4 has a new 4c.5 section that spawns code-simplifier after implementation but before QEMU build. Both sections include fallback manual review paths. All existing Phase 4 sections (4a, 4b, 4c, 4d, 4e) and all other phases (1-6) are preserved.</behavior>
  <test-command>grep -n "^### 4[a-e]" .claude/agents/impl.md</test-command>
  <source-assertion>Phase 4 sections are numbered: 4a, 4a.5, 4b, 4c, 4c.5, 4d, 4e. New sections are placed at logical insertion points (after 4a for type review, after 4c for code polish).</source-assertion>
</acceptance_criteria>
<done>impl body has systematic-debugging in Global Capabilities. Phase 4 has new 4a.5 Type/Trait Design Review section that spawns type-design-analyzer before method implementation. Phase 4 has new 4c.5 Post-Implementation Code Polish section that spawns code-simplifier after implementation. Both have fallback documentation. All existing phases preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| impl -> type-design-analyzer sub-agent | Spawn instructions passed as text; sub-agent reviews new type/trait definitions in source files |
| impl -> code-simplifier sub-agent | Spawn instructions passed as text; sub-agent reviews modified files for simplification |
| impl frontmatter -> plugin resolution | Skill names declared in frontmatter must resolve against installed plugins at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Spoofing | impl frontmatter skills list | mitigate | superpowers:systematic-debugging is from official marketplace, installed via `claude plugins install` |
| T-03-11 | Spoofing | type-design-analyzer sub-agent spawn | mitigate | Full prefix `pr-review-toolkit:type-design-analyzer` avoids collision; spawn instructions are explicit and scoped |
| T-03-12 | Spoofing | code-simplifier sub-agent spawn | mitigate | Full prefix `pr-review-toolkit:code-simplifier` avoids collision with standalone code-simplifier@claude-plugins-official plugin (Research Pitfall 1) |
| T-03-13 | Denial of Service | Sub-agent unavailability | mitigate | Both sections include manual fallback review paths; impl continues functionality with warnings |
| T-03-14 | Tampering | code-simplifier output | mitigate | code-simplifier runs before QEMU build (4d) and syscall-diff verification (4e) — any behavioral changes would be caught by the diff comparison |
</threat_model>

<verification>
After both tasks complete, verify the entire plan:
```bash
# Frontmatter skill count (4 total)
sed -n '/^skills:/,/^tools:/p' .claude/agents/impl.md | grep -c "^- "

# Preamble Agents count (3 entries)
sed -n '/^\*\*Agents\*\*/,/^$/p' .claude/agents/impl.md | grep -c "^- "

# Both new Phase 4 sections present
grep -c "^### 4a.5" .claude/agents/impl.md
grep -c "^### 4c.5" .claude/agents/impl.md

# All 6 phases preserved
test $(grep -c "^## Phase [1-6]:" .claude/agents/impl.md) -eq 6

# Full prefix for both sub-agents
grep -c "pr-review-toolkit:type-design-analyzer" .claude/agents/impl.md
grep -c "pr-review-toolkit:code-simplifier" .claude/agents/impl.md
```
Expected: skill count = 4, agents count in preamble = 3, both new sections = 1 each, phases = 6.
</verification>

<success_criteria>
1. impl.md frontmatter has 4 skills (3 existing + 1 new) with correct YAML indentation
2. Preamble Skills lists systematic-debugging
3. Preamble Agents lists test-gen, type-design-analyzer, and code-simplifier with fallback documentation
4. Global Capabilities references systematic-debugging for implementation debugging
5. Phase 4 has new 4a.5 section for conditional type-design-analyzer spawn
6. Phase 4 has new 4c.5 section for post-implementation code-simplifier spawn
7. Both sub-agent sections include fallback manual review paths
8. Full `pr-review-toolkit:` prefix used for both sub-agents per Pitfall 1 avoidance
9. All existing 6 phases and section structure preserved per D-07
10. Frontmatter YAML parses correctly (no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-plugin-agent-integration/03-04-SUMMARY.md`
</output>
