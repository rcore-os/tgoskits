---
phase: 03-cross-plugin-agent-integration
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - .claude/agents/bug-hunt.md
autonomous: true
requirements:
  - CPI-06
  - CPI-07
  - CPI-12
must_haves:
  truths:
    - "bug-hunt frontmatter skills list includes superpowers:test-driven-development and superpowers:dispatching-parallel-agents alongside existing skills"
    - "bug-hunt Dependency Check preamble lists pr-review-toolkit:silent-failure-hunter in the Agents section"
    - "bug-hunt Phase 2 (REPRO) is enhanced with TDD RED phase: write failing repro test, confirm failure, revise if needed"
    - "bug-hunt Phase 3 (FIX) is enhanced with TDD GREEN phase: apply fix, run repro test, confirm pass"
    - "bug-hunt Phase 4 (VERIFY) is enhanced with TDD REFACTOR phase: clean up test/fix code, re-run"
    - "bug-hunt spawns silent-failure-hunter conditionally for bugs classified as error-handling issues (CPI-12)"
    - "silent-failure-hunter spawn uses full pr-review-toolkit: prefix per Pitfall 1 avoidance"
    - "Fallback documented for silent-failure-hunter: if unavailable, proceed with manual error-handling analysis"
    - "TDD phases fit into existing repro-fix-verify flow without structural changes per D-04"
    - "No structural changes to bug-hunt workflow — skills and spawn are additive per D-07"
  artifacts:
    - path: ".claude/agents/bug-hunt.md"
      provides: "Updated frontmatter skills, preamble Agents section, TDD-enhanced REPRO/FIX/VERIFY phases, and conditional silent-failure-hunter spawn"
      contains: "superpowers:test-driven-development"
      contains: "superpowers:dispatching-parallel-agents"
      contains: "pr-review-toolkit:silent-failure-hunter"
      contains: "RED Phase"
      contains: "GREEN Phase"
      contains: "REFACTOR Phase"
  key_links:
    - from: "bug-hunt.md preamble Agents section"
      to: "bug-hunt.md body Phase 2: REPRO"
      via: "sub-agent spawn reference in body prose"
      pattern: "pr-review-toolkit:silent-failure-hunter"
    - from: "bug-hunt.md frontmatter skills:"
      to: "bug-hunt.md body Phase 2/3/4 (REPRO/FIX/VERIFY)"
      via: "skill name reference in body prose"
      pattern: "test-driven-development"
    - from: "bug-hunt.md frontmatter skills:"
      to: "bug-hunt.md body"
      via: "skill name reference for parallel execution"
      pattern: "dispatching-parallel-agents"
---

<objective>
Add superpowers:test-driven-development (CPI-06) and superpowers:dispatching-parallel-agents (CPI-07) skills to bug-hunt frontmatter, add pr-review-toolkit:silent-failure-hunter to the preamble Agents section (CPI-12, D-06), enhance the REPRO/FIX/VERIFY phases with TDD RED/GREEN/REFACTOR structure (per D-04), and add conditional silent-failure-hunter spawn for error-handling bug investigations.

Purpose: bug-hunt is medium complexity. Per D-04, TDD fits naturally into the existing "repro -> fix -> verify" flow as RED/GREEN/REFACTOR phases without structural change. The dispatching-parallel-agents skill provides parallel test execution for multiple repro scenarios. The silent-failure-hunter sub-agent targets error-handling bugs specifically.

Output: bug-hunt.md with 7 skills in frontmatter (5 existing + 2 new), an updated preamble Agents section listing silent-failure-hunter, TDD-enhanced REPRO/FIX/VERIFY phases, and a conditional silent-failure-hunter spawn in Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-plugin-agent-integration/03-CONTEXT.md
@.planning/phases/03-cross-plugin-agent-integration/03-RESEARCH.md
@.claude/agents/bug-hunt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TDD and parallel-agents skills to frontmatter, silent-failure-hunter to preamble Agents</name>
  <files>.claude/agents/bug-hunt.md</files>
  <read_first>
Read the current `.claude/agents/bug-hunt.md` frontmatter block (lines 1-19) and Dependency Check preamble (lines 21-41). The current `skills:` field has five entries: `starry-test-suit`, `cross-kernel-driver`, `arceos-test-adapter`, `superpowers:systematic-debugging`, `superpowers:verification-before-completion`. The current Agents section says "None" with a Design note about the debugger agent. The current Skills section in the preamble lists systematic-debugging and verification-before-completion.
  </read_first>
  <action>
Three changes to the top portion of bug-hunt.md:

**Change 1 — Frontmatter skills (CPI-06, CPI-07):**
Expand the `skills:` field to include 2 additional superpowers skills. Per D-05 and RESEARCH.md Pitfall 2 (no agent gets more than 2 new skills), the final list (in order):
1. `starry-test-suit` (existing)
2. `cross-kernel-driver` (existing)
3. `arceos-test-adapter` (existing)
4. `superpowers:systematic-debugging` (existing)
5. `superpowers:verification-before-completion` (existing)
6. `superpowers:test-driven-development` (NEW — CPI-06: RED/GREEN/REFACTOR in repro-fix-verify)
7. `superpowers:dispatching-parallel-agents` (NEW — CPI-07: parallel repro scenario execution)

Keep `tools:` (Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch), `name:`, and `description:` unchanged.

**Change 2 — Preamble Skills section:**
Add the two new skills to the preamble Skills list, preserving alphabetical-by-plugin order:
```
- `superpowers:dispatching-parallel-agents` — parallel test execution for multiple independent repro scenarios
- `superpowers:systematic-debugging` — structured debugging process
- `superpowers:test-driven-development` — RED/GREEN/REFACTOR methodology for repro-fix-verify
- `superpowers:verification-before-completion` — confirm fixes before marking complete
```

**Change 3 — Preamble Agents section (CPI-12, D-06):**
Replace the current Agents section (including the Design note) with:
```
**Agents** (must be spawnable):
- `pr-review-toolkit:silent-failure-hunter` — error handling audit for bugs classified as error-handling issues (spawned conditionally during REPRO phase; fallback: manual error-handling analysis)

Design note: Complex debugging (crashes, memory corruption, multi-core races) is routed through the `superpowers:systematic-debugging` skill listed above rather than spawning the `debugger` agent. Skill invocation operates within the same context window, avoiding sub-agent initialization overhead. Error-handling bug investigations additionally spawn `pr-review-toolkit:silent-failure-hunter` for specialized silent-failure pattern detection. If future phases require broader agent delegation for debugging (e.g., parallel multi-repro analysis), the `debugger` agent can be added to this section at that time.
```

Preserve the existing Design note's rationale about skill-vs-agent routing, extending it to mention silent-failure-hunter. Keep the ABORT gate text unchanged. Use the full `pr-review-toolkit:` prefix per RESEARCH.md Pitfall 1.
  </action>
  <verify>
Verify all three changes:
```bash
# Frontmatter: both new skills present
grep -c "superpowers:test-driven-development" .claude/agents/bug-hunt.md
grep -c "superpowers:dispatching-parallel-agents" .claude/agents/bug-hunt.md

# Preamble Skills: both new skills listed
sed -n '/^### Dependency Check/,/^# /p' .claude/agents/bug-hunt.md | grep -c "test-driven-development"
sed -n '/^### Dependency Check/,/^# /p' .claude/agents/bug-hunt.md | grep -c "dispatching-parallel-agents"

# Preamble Agents: silent-failure-hunter listed
grep -c "pr-review-toolkit:silent-failure-hunter" .claude/agents/bug-hunt.md

# Preamble Agents: fallback documented
grep -c "fallback.*manual.*error-handling" .claude/agents/bug-hunt.md

# Design note preserved with silent-failure-hunter mention
grep -c "silent-failure-hunter" .claude/agents/bug-hunt.md

# ABORT gate preserved
grep -c "AGENT ABORTED" .claude/agents/bug-hunt.md

# Existing skills preserved
grep -c "superpowers:systematic-debugging" .claude/agents/bug-hunt.md
grep -c "superpowers:verification-before-completion" .claude/agents/bug-hunt.md
```
All grep counts must be >= 1. test-driven-development and dispatching-parallel-agents should appear exactly once each (frontmatter only at this point).
</verify>
<acceptance_criteria>
  <behavior>Frontmatter skills list has 7 entries in the specified order. Preamble Skills section lists all 4 superpowers skills (systematic-debugging, dispatching-parallel-agents, test-driven-development, verification-before-completion) plus 3 project-local skills. Preamble Agents section lists pr-review-toolkit:silent-failure-hunter with fallback documentation. Design note is preserved and extended. ABORT gate unchanged.</behavior>
  <test-command>sed -n '/^### Dependency Check/,/^# /p' .claude/agents/bug-hunt.md</test-command>
  <source-assertion>The Dependency Check preamble contains Skills (7 entries), Tools (8 entries), Agents (1 entry with silent-failure-hunter and fallback). The Design note explains skill-vs-agent routing and mentions silent-failure-hunter.</source-assertion>
</acceptance_criteria>
<done>bug-hunt.md frontmatter has 7 skills. Preamble Skills lists 4 superpowers skills. Preamble Agents lists pr-review-toolkit:silent-failure-hunter with fallback and Design note extension. ABORT gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance REPRO/FIX/VERIFY phases with TDD structure and add conditional silent-failure-hunter spawn</name>
  <files>.claude/agents/bug-hunt.md</files>
  <read_first>
Read the current body of `.claude/agents/bug-hunt.md`, specifically Phase 2: REPRO (lines 271-274), Phase 3: FIX (lines 301-343), Phase 4: VERIFY (lines 345-359), and the Global Capabilities section (lines 44-52). Also read RESEARCH.md Code Example "bug-hunt: TDD Integration into Repro-Fix-Verify" for the exact TDD phase text and silent-failure-hunter spawn invocation to use.
  </read_first>
  <action>
Per D-04 (TDD fits into existing flow) and D-07 (additive only), enhance three phases of the bug-hunt workflow. The existing Phase 2/3/4 sections are PRESERVED — TDD labels and silent-failure-hunter spawn are added alongside existing content.

**Enhancement 1 — Global Capabilities:**
After the existing sentence "Before implementing fixes, invoke `superpowers:systematic-debugging` to follow a structured debugging process rather than guessing." add:
```
Apply `superpowers:test-driven-development` throughout the repro-fix-verify cycle: write a failing repro test (RED), apply the minimal fix (GREEN), then clean up test and fix code (REFACTOR). For bugs with multiple independent repro scenarios, invoke `superpowers:dispatching-parallel-agents` to run them concurrently rather than sequentially.
```

**Enhancement 2 — Phase 2: REPRO header and RED Phase:**
Replace the Phase 2 section header from `## Phase 2: REPRO (Reproduction)` to `## Phase 2: REPRO (Reproduction) — RED Phase` and add TDD guidance at the start of the phase. Keep the existing subsection structure.

The new header and lead-in:
```
## Phase 2: REPRO (Reproduction) — RED Phase

Apply `superpowers:test-driven-development` RED phase methodology:

### For each confirmed discrepancy:

1. **Classify the bug** using the table in Phase 1.
```

The existing "1. Classify the bug" bullet becomes item 1, existing "2. Write a minimal test case" becomes item 2 (renumbered to continue after the TDD guidance), existing "3. Validate on Linux" remains.

After the existing "3. Validate on Linux" step, insert TDD RED verification and the conditional silent-failure-hunter spawn:
```
4. **RED verification:** Run the repro test on the target OS via QEMU to confirm it fails (RED). If the test passes on the target OS, the bug is not reproduced — revise the test. A test that passes under the buggy code is NOT a valid reproducer.

5. **For concurrency bugs or bugs classified as error-handling issues** (root cause: validation-bug, resource-bug where the resource is an error code), additionally spawn `pr-review-toolkit:silent-failure-hunter`:

> "Analyze the error handling patterns in [affected function/file] at [line range]. Identify silent failures, inadequate error handling, or inappropriate fallback behavior."

If `pr-review-toolkit:silent-failure-hunter` is unavailable, warn and proceed with manual error-handling analysis focused on: checking that all `Result`/`Option` paths are handled, error types are propagated correctly, and fallback behavior is explicit rather than silent.
```

**Enhancement 3 — Phase 3: FIX header and GREEN Phase:**
Change the header from `## Phase 3: FIX` to `## Phase 3: FIX — GREEN Phase`. After the header, before the existing step 1, add:
```
Apply the fix following `superpowers:test-driven-development` GREEN phase: make the minimal change that causes the repro test to pass. Run the repro test to confirm it now passes (GREEN). Do not refactor or clean up during this phase — the goal is the smallest correct fix.
```

Keep the existing steps 1-4 and Synchronization Boundary Audit content intact after this addition.

**Enhancement 4 — Phase 4: VERIFY header and REFACTOR Phase:**
Change the header from `## Phase 4: VERIFY` to `## Phase 4: VERIFY — REFACTOR Phase`. After the header, before the existing CI command, add:
```
Apply `superpowers:test-driven-development` REFACTOR phase: clean up test code and fix code. Remove debugging artifacts, improve variable names, extract repeated logic. Re-run the repro test to confirm it still passes after cleanup. Keep the test minimal — it should be the shortest program that reliably triggers the bug.
```

Keep the existing CI command and lockdep verification intact after this addition.

**Enhancement 5 — Phase 5: REPORT:**
No changes needed — CPI requirements do not modify Phase 5.

Do NOT change any other sections: Phase 1 (HUNT), bug classification tables, Synchronization Boundary Audit, Rules, Concurrency Detection Tools, or Lockdep sections. All changes are additive per D-07.
  </action>
  <verify>
Verify TDD phases and silent-failure-hunter spawn:
```bash
# RED Phase header
grep -c "RED Phase" .claude/agents/bug-hunt.md

# GREEN Phase header
grep -c "GREEN Phase" .claude/agents/bug-hunt.md

# REFACTOR Phase header
grep -c "REFACTOR Phase" .claude/agents/bug-hunt.md

# TDD skill referenced in Global Capabilities
sed -n '/^## Global Capabilities/,/^## Bug Classification/p' .claude/agents/bug-hunt.md | grep -c "test-driven-development"

# Parallel agents referenced in Global Capabilities
sed -n '/^## Global Capabilities/,/^## Bug Classification/p' .claude/agents/bug-hunt.md | grep -c "dispatching-parallel-agents"

# silent-failure-hunter spawn in Phase 2
grep -c "pr-review-toolkit:silent-failure-hunter" .claude/agents/bug-hunt.md

# Fallback for unavailable sub-agent
grep -c "unavailable.*manual.*error-handling" .claude/agents/bug-hunt.md

# Existing Phase 5 preserved
grep -c "^## Phase 5: REPORT" .claude/agents/bug-hunt.md

# Existing Synchronization Boundary Audit preserved
grep -c "Synchronization Boundary Audit" .claude/agents/bug-hunt.md
```
All grep counts must be >= 1. silent-failure-hunter should appear twice (preamble + body).
</verify>
<acceptance_criteria>
  <behavior>Phase 2 header is "REPRO (Reproduction) — RED Phase" with TDD guidance and RED verification step. Phase 3 header is "FIX — GREEN Phase" with minimal-fix guidance. Phase 4 header is "VERIFY — REFACTOR Phase" with cleanup guidance. Global Capabilities references both TDD and dispatching-parallel-agents. Phase 2 includes conditional silent-failure-hunter spawn for error-handling bugs with fallback documentation. Existing Phase 1, Phase 5, bug classification, Synchronization Boundary Audit, and lockdep sections are all preserved.</behavior>
  <test-command>grep -n "^## Phase [2-5]:" .claude/agents/bug-hunt.md</test-command>
  <source-assertion>Phase headers show: Phase 2 with RED, Phase 3 with GREEN, Phase 4 with REFACTOR. Phase 5 unchanged. All phases are additive (existing content preserved, new TDD guidance and spawn inserted alongside).</source-assertion>
</acceptance_criteria>
<done>bug-hunt body has TDD RED/GREEN/REFACTOR phases integrated into REPRO/FIX/VERIFY workflow. Phase 2 includes conditional silent-failure-hunter spawn with fallback. Global Capabilities references both new skills. All existing workflow sections preserved per D-04 and D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bug-hunt -> silent-failure-hunter sub-agent | Spawn instructions passed as text; sub-agent audits error-handling patterns in bug-affected code |
| bug-hunt frontmatter -> plugin resolution | Skill names declared in frontmatter must resolve against installed plugins at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing | bug-hunt frontmatter skills list | mitigate | Both superpowers skills are from official marketplace, installed via `claude plugins install` |
| T-03-07 | Spoofing | silent-failure-hunter sub-agent spawn | mitigate | Full prefix `pr-review-toolkit:silent-failure-hunter` avoids collision with any standalone plugin (Pitfall 1) |
| T-03-08 | Denial of Service | Sub-agent unavailability | mitigate | Fallback to manual error-handling analysis documented; bug-hunt continues functionality (graceful degradation per Phase 01/02 pattern) |
| T-03-09 | Elevation of Privilege | Parallel agent dispatching | mitigate | dispatching-parallel-agents skill guides concurrent execution patterns; does not grant new execution permissions beyond existing agent capabilities |
</threat_model>

<verification>
After both tasks complete, verify the entire plan:
```bash
# Frontmatter skill count (7 total)
sed -n '/^skills:/,/^tools:/p' .claude/agents/bug-hunt.md | grep -c "^- "

# Preamble Agents has exactly 1 entry (silent-failure-hunter)
sed -n '/^\*\*Agents\*\*/,/^$/p' .claude/agents/bug-hunt.md | grep -c "pr-review-toolkit"

# All three TDD phase labels present
grep -c "RED Phase" .claude/agents/bug-hunt.md
grep -c "GREEN Phase" .claude/agents/bug-hunt.md
grep -c "REFACTOR Phase" .claude/agents/bug-hunt.md

# Phase headers preserved
test $(grep -c "^## Phase [1-5]:" .claude/agents/bug-hunt.md) -eq 5

# silent-failure-hunter appears twice (preamble + body)
test $(grep -c "pr-review-toolkit:silent-failure-hunter" .claude/agents/bug-hunt.md) -ge 2
```
</verification>

<success_criteria>
1. bug-hunt.md frontmatter has 7 skills (5 existing + 2 new) with correct YAML indentation
2. Preamble Skills lists 4 superpowers skills including test-driven-development and dispatching-parallel-agents
3. Preamble Agents lists pr-review-toolkit:silent-failure-hunter with fallback and Design note extension
4. Phase 2 REPRO header is "— RED Phase" with TDD guidance and conditional silent-failure-hunter spawn
5. Phase 3 FIX header is "— GREEN Phase" with minimal-fix guidance
6. Phase 4 VERIFY header is "— REFACTOR Phase" with cleanup guidance
7. Global Capabilities references both TDD and dispatching-parallel-agents
8. All existing workflow phases (1-5) and sections preserved per D-04 and D-07
9. Full `pr-review-toolkit:` prefix used per Pitfall 1 avoidance
10. Frontmatter YAML parses correctly (no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-plugin-agent-integration/03-03-SUMMARY.md`
</output>
