---
phase: 03-cross-plugin-agent-integration
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - .claude/agents/pr-review.md
autonomous: true
requirements:
  - CPI-01
  - CPI-02
  - CPI-03
  - CPI-04
  - CPI-05
  - CPI-16
must_haves:
  truths:
    - "pr-review frontmatter skills list includes superpowers:receiving-code-review alongside existing skills"
    - "pr-review Dependency Check preamble lists code-reviewer, pr-test-analyzer, and silent-failure-hunter in the Agents section"
    - "pr-review body has a Step 0 eligibility check that gates sub-agent spawns (CPI-02, D-03) — skip closed/draft/reviewed PRs before any sub-agent runs"
    - "pr-review body has Step 1 that always spawns code-reviewer as a pre-filter before OS-specific review (CPI-01, D-01)"
    - "code-reviewer output feeds into OS-specific review to avoid duplication per RESEARCH.md Pitfall 3"
    - "pr-review body has Step 2 with conditional spawns: pr-test-analyzer for test-change/non-concurrency PRs (CPI-04), silent-failure-hunter for error-handling PRs (CPI-05)"
    - "Conditional spawn triggers are explicit: test changes = *.test.*/*.spec.*/test-*/tests/ OR non-concurrency label; error-handling = try/catch/Result/Option/unwrap/expect/? operator per RESEARCH.md Pitfall 5"
    - "pr-review body has a Confidence Assessment section after findings with explicit 0-100 score as secondary axis alongside BLOCK/WARN/INFO (CPI-03, D-02)"
    - "Confidence score is explicitly assigned by the reviewer (not computed via formula) per D-02"
    - "All 3 sub-agent spawns use full pr-review-toolkit: prefix per RESEARCH.md Pitfall 1"
    - "All sub-agent spawns have fallback: if unavailable, warn and perform manual equivalent analysis"
    - "Eligibility check handles gh CLI unavailability: warn and assume eligible per RESEARCH.md Pitfall 4"
    - "No structural changes to existing Review Dimensions, BLOCK/WARN/INFO format, Workflow steps, Safety Checklist, Synchronization Boundary Audit, Test Validity Audit, or Post-Mortem sections per D-07"
    - "Existing skills (review-open-prs, starry-test-suit, arceos-test-adapter, superpowers:verification-before-completion, superpowers:systematic-debugging) remain unchanged"
  artifacts:
    - path: ".claude/agents/pr-review.md"
      provides: "Updated frontmatter, preamble Agents section, eligibility check (Step 0), code-reviewer pre-filter (Step 1), conditional spawns (Step 2), and Confidence Assessment section"
      contains: "superpowers:receiving-code-review"
      contains: "pr-review-toolkit:code-reviewer"
      contains: "pr-review-toolkit:pr-test-analyzer"
      contains: "pr-review-toolkit:silent-failure-hunter"
      contains: "### Step 0: Eligibility Check"
      contains: "### Step 1: Pre-Filter — Code Quality Review"
      contains: "### Step 2: Conditional Sub-Agent Spawns"
      contains: "Confidence Assessment"
  key_links:
    - from: "pr-review.md preamble Agents section"
      to: "pr-review.md body Steps 0-2"
      via: "sub-agent spawn references in body prose"
      pattern: "pr-review-toolkit:(code-reviewer|pr-test-analyzer|silent-failure-hunter)"
    - from: "pr-review.md Step 0 Eligibility Check"
      to: "pr-review.md Step 1 code-reviewer pre-filter"
      via: "eligibility gate — only eligible PRs proceed"
      pattern: "eligible.*proceed"
    - from: "pr-review.md Step 1 code-reviewer output"
      to: "pr-review.md Step 3 OS-specific review"
      via: "findings integration to avoid duplication"
      pattern: "code-reviewer.*output"
    - from: "pr-review.md frontmatter skills:"
      to: "pr-review.md body Global Capabilities"
      via: "skill name reference in body prose"
      pattern: "receiving-code-review"
---

<objective>
Add superpowers:receiving-code-review skill (CPI-16) to pr-review frontmatter. Add code-reviewer, pr-test-analyzer, and silent-failure-hunter to the preamble Agents section (CPI-01/04/05, D-06). Add 4 new body sections: Step 0 eligibility check (CPI-02, D-03), Step 1 code-reviewer pre-filter (CPI-01, D-01), Step 2 conditional sub-agent spawns (CPI-04, CPI-05), and Confidence Assessment after findings (CPI-03, D-02). Renumber existing Steps 1-6 to Steps 3-8.

Purpose: pr-review is the most complex integration — 6 requirements including structural workflow additions. The eligibility check gates all expensive sub-agent spawns. The code-reviewer runs as a pre-filter whose output feeds into the OS-specific review. Conditional spawns target specific PR characteristics. Confidence scoring adds a secondary quality axis.

Output: pr-review.md with 6 skills in frontmatter (5 existing + 1 new), preamble Agents with 3 sub-agents, and a reflowed 8-step workflow with eligibility check, code-reviewer pre-filter, conditional spawns, and confidence assessment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-plugin-agent-integration/03-CONTEXT.md
@.planning/phases/03-cross-plugin-agent-integration/03-RESEARCH.md
@.claude/agents/pr-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add receiving-code-review skill to frontmatter and 3 sub-agents to preamble Agents section</name>
  <files>.claude/agents/pr-review.md</files>
  <read_first>
Read the current `.claude/agents/pr-review.md` frontmatter block (lines 1-19) and Dependency Check preamble (lines 21-39). The current `skills:` field has five entries: `review-open-prs`, `starry-test-suit`, `arceos-test-adapter`, `superpowers:verification-before-completion`, `superpowers:systematic-debugging`. The current Agents section says "None". The current preamble Skills section lists verification-before-completion and systematic-debugging.
  </read_first>
  <action>
Three changes to the top portion of pr-review.md:

**Change 1 — Frontmatter skills (CPI-16):**
Expand the `skills:` field to include `superpowers:receiving-code-review`. Per D-05, skills are loaded via frontmatter only. The final list (in order):
1. `review-open-prs` (existing — project-local)
2. `starry-test-suit` (existing — project-local)
3. `arceos-test-adapter` (existing — project-local)
4. `superpowers:verification-before-completion` (existing)
5. `superpowers:systematic-debugging` (existing)
6. `superpowers:receiving-code-review` (NEW — CPI-16: structured evaluation of external review feedback)

Keep `tools:` (Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch), `name:`, and `description:` unchanged.

**Change 2 — Preamble Skills section:**
Add receiving-code-review to the preamble Skills list:
```
- `superpowers:receiving-code-review` — structured evaluation of external review comments
- `superpowers:systematic-debugging` — root-cause analysis for bug classification
- `superpowers:verification-before-completion` — confirm fixes before marking complete
```

List receiving-code-review first (new entry first pattern from Phase 02).

**Change 3 — Preamble Agents section (CPI-01, CPI-04, CPI-05, D-06):**
Replace the current Agents section (`**Agents** (must be spawnable):\n- None`) with:
```
**Agents** (must be spawnable):
- `pr-review-toolkit:code-reviewer` — general code quality pre-filter (always spawned per D-01; fallback: manual code quality review)
- `pr-review-toolkit:pr-test-analyzer` — test coverage audit (spawned conditionally when PR diff contains test changes or PR is labeled non-concurrency; fallback: manual test coverage analysis)
- `pr-review-toolkit:silent-failure-hunter` — error handling audit (spawned conditionally when PR diff contains error-handling patterns; fallback: manual error-handling analysis)

Per D-06, spawn references appear in preamble Agents section plus body description. Use the full `pr-review-toolkit:` prefix per RESEARCH.md Pitfall 1 (avoids collision with any standalone plugin agents). All three sub-agents have documented fallback paths for graceful degradation.
```

Keep the ABORT gate text unchanged. Keep the warning message "Do NOT proceed with degraded capabilities" unchanged.
  </action>
  <verify>
Verify all three changes:
```bash
# Frontmatter: receiving-code-review present
grep -c "superpowers:receiving-code-review" .claude/agents/pr-review.md

# Preamble Skills: receiving-code-review listed
sed -n '/^### Dependency Check/,/^# /p' .claude/agents/pr-review.md | grep -c "receiving-code-review"

# Preamble Agents: all 3 sub-agents listed
grep -c "pr-review-toolkit:code-reviewer" .claude/agents/pr-review.md
grep -c "pr-review-toolkit:pr-test-analyzer" .claude/agents/pr-review.md
grep -c "pr-review-toolkit:silent-failure-hunter" .claude/agents/pr-review.md

# Preamble Agents: fallback documented for all 3
grep -c "fallback" .claude/agents/pr-review.md

# ABORT gate preserved
grep -c "AGENT ABORTED" .claude/agents/pr-review.md

# Frontmatter skill count
sed -n '/^skills:/,/^tools:/p' .claude/agents/pr-review.md | grep -c "^- "
```
All grep counts must be >= 1. Frontmatter skill count must be 6.
</verify>
<acceptance_criteria>
  <behavior>Frontmatter skills list has 6 entries in the specified order. Preamble Skills lists 3 superpowers skills (receiving-code-review, systematic-debugging, verification-before-completion). Preamble Agents lists 3 pr-review-toolkit sub-agents (code-reviewer, pr-test-analyzer, silent-failure-hunter) each with fallback documentation. ABORT gate and warning text preserved.</behavior>
  <test-command>sed -n '/^### Dependency Check/,/^# /p' .claude/agents/pr-review.md</test-command>
  <source-assertion>The Dependency Check preamble contains Skills (3 superpowers entries), Tools (8 entries), and Agents (3 entries with pr-review-toolkit prefix and fallback). The ABORT gate is preserved.</source-assertion>
</acceptance_criteria>
<done>pr-review.md frontmatter has 6 skills. Preamble Skills lists receiving-code-review, systematic-debugging, verification-before-completion. Preamble Agents lists code-reviewer, pr-test-analyzer, silent-failure-hunter with fallback documentation. ABORT gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add Step 0 eligibility check and Step 1 code-reviewer pre-filter to pr-review body</name>
  <files>.claude/agents/pr-review.md</files>
  <read_first>
Read the current body of `.claude/agents/pr-review.md`, specifically the Workflow section (lines 81-152). Note the current Step 1 through Step 6 structure and the Global Capabilities section (lines 46-49). Also read RESEARCH.md Code Example "pr-review: Eligibility Check + Sub-Agent Spawn Flow (Body Section)" for the exact Step 0, Step 1, and Step 2 text to use.
  </read_first>
  <action>
Per D-07 (additive only, no structural changes) with one exception: the Workflow steps must be renumbered to accommodate the 3 new steps. Existing Step 1-6 become Steps 3-8. New Steps 0, 1, 2 are inserted before the existing workflow.

**Enhancement 1 — Global Capabilities:**
After the existing sentence "For syscall semantics verification, use `context7` MCP or web search to look up the relevant Linux man-pages (section 2) and POSIX specifications." add:
```
When receiving external review feedback on your own review output, invoke `superpowers:receiving-code-review` — evaluate each comment systematically: verify the claim against the code, acknowledge valid feedback, explain disagreements with evidence, and apply corrections where appropriate.
```

**Enhancement 2 — Insert new Steps 0, 1, 2 BEFORE the existing Step 1:**
Replace the `### Step 1: Get the diff` header with three new steps, then renumber the old Step 1 as Step 3:

```
### Step 0: Eligibility Check (NEW — CPI-02, D-03)

Before any review work, verify the PR is eligible for review. This check runs BEFORE spawning any sub-agents:

1. Check PR state via `gh pr view <number> --json state,isDraft,reviews`
2. If state is CLOSED or MERGED: stop and report "PR is closed/merged -- no review needed"
3. If isDraft is true: stop and report "PR is a draft -- no review needed until ready"
4. If already reviewed in this session (check for existing REVIEW.md on this branch): stop and report "Already reviewed in this session"

If `gh` CLI is unavailable or PR number not determinable: emit a warning and assume eligible (proceed). Per RESEARCH.md Pitfall 4, eligibility check must not fail on non-GitHub PRs -- only skip when PR state is definitively ineligible.

### Step 1: Pre-Filter -- Code Quality Review (NEW — CPI-01, D-01)

Always spawn `pr-review-toolkit:code-reviewer` as a pre-filter before OS-specific review. code-reviewer provides general code quality analysis (project guideline compliance, bug detection, style issues) with its own confidence scoring:

> "Review the current PR diff for general code quality, project guideline compliance, and bug detection. Report findings with confidence scores."

Wait for code-reviewer output. Integrate its findings into the overall review context.

**Avoiding duplication (RESEARCH.md Pitfall 3):** code-reviewer runs BEFORE the OS-specific review. When performing the OS-specific review in Step 3 (per-file review), reference code-reviewer's findings rather than re-discovering the same issues. If code-reviewer already flagged an issue, note it as "also found by code-reviewer" in your own findings rather than duplicating.

If `pr-review-toolkit:code-reviewer` is unavailable, warn and perform a manual general code quality review pass before proceeding to OS-specific review.

### Step 2: Conditional Sub-Agent Spawns (NEW — CPI-04, CPI-05)

Based on PR characteristics, conditionally spawn additional sub-agents:

**Spawn `pr-review-toolkit:pr-test-analyzer` if:**
- The PR diff includes test file changes matching: `*.test.*`, `*.spec.*`, `test-*`, `tests/`, or `test_` filenames; OR
- The PR is labeled as `non-concurrency`

> "Analyze test coverage quality and completeness for this PR diff. Identify critical gaps, missing edge cases, and test quality issues. Categorize findings as critical, major, or minor."

**Spawn `pr-review-toolkit:silent-failure-hunter` if:**
- The PR diff includes error-handling patterns: `try`, `catch`, `Result`, `Option`, `unwrap`, `expect`, `?` operator propagation, error type definitions, or error mapping/conversion code

> "Audit this PR diff for silent failures, inadequate error handling, and inappropriate fallback behavior. Classify each finding by severity: critical (data loss/undefined behavior), major (wrong error propagation), minor (inconsistent style)."

Per RESEARCH.md Pitfall 5, use these explicit trigger heuristics -- do NOT make subjective judgments about whether spawn is "needed." If the trigger patterns match, spawn. If the sub-agent is unavailable for either spawn, warn and perform the equivalent manual analysis.

### Step 3: Get the diff
```

Then renumber ALL subsequent steps:
- Old "### Step 2: Per-file review" → "### Step 4: Per-file review"
- Old "### Step 3: Generate REVIEW.md" → "### Step 5: Generate REVIEW.md"
- Old "### Step 4: Auto-fix BLOCK items" → "### Step 6: Auto-fix BLOCK items"
- Old "### Step 5: Re-verify" → "### Step 7: Re-verify"
- Old "### Step 6: Loop control" → "### Step 8: Loop control"

Update the REVI  EW.md generation (now Step 5, old Step 3) to note that code-reviewer, pr-test-analyzer, and silent-failure-hunter findings should be integrated into BLOCK/WARN/INFO dimensions. Match the existing markdown template format -- add a note above the template: "Integrate sub-agent findings into the appropriate sections below. Issues found by both code-reviewer and OS-specific review should cite code-reviewer rather than duplicating."

Do NOT alter any of the existing Review Dimensions table, Safety Checklist, Synchronization Boundary Audit, Test Validity Audit, or Post-Mortem sections. Only the Workflow steps 1-6 are affected by renumbering and the three new steps.

Per D-01, code-reviewer is always spawned (Step 1 pre-filter), while pr-test-analyzer and silent-failure-hunter are conditional based on PR characteristics (Step 2).
  </action>
  <verify>
Verify the new steps and renumbering:
```bash
# Step 0 eligibility check exists
grep -c "^### Step 0: Eligibility Check" .claude/agents/pr-review.md

# Step 1 code-reviewer pre-filter exists
grep -c "^### Step 1: Pre-Filter.*Code Quality Review" .claude/agents/pr-review.md

# Step 2 conditional spawns exists
grep -c "^### Step 2: Conditional Sub-Agent Spawns" .claude/agents/pr-review.md

# Old steps renumbered
grep -c "^### Step 3: Get the diff" .claude/agents/pr-review.md
grep -c "^### Step 4: Per-file review" .claude/agents/pr-review.md
grep -c "^### Step 5: Generate REVIEW.md" .claude/agents/pr-review.md
grep -c "^### Step 6: Auto-fix BLOCK items" .claude/agents/pr-review.md
grep -c "^### Step 7: Re-verify" .claude/agents/pr-review.md
grep -c "^### Step 8: Loop control" .claude/agents/pr-review.md

# Old Step numbers are GONE
! grep -q "^### Step [12]: " .claude/agents/pr-review.md || echo "Old numbers may still exist - check"
! grep -q "^### Step [3456]: " .claude/agents/pr-review.md || echo "Old numbers 3-6 may still exist - check"
# Actually renumbering means old 1-2 should not appear as headers anymore

# Eligibility check handles gh unavailability
grep -c "gh.*unavailable.*eligible" .claude/agents/pr-review.md

# Code-reviewer duplication avoidance documented
grep -c "duplication\|duplicate\|also found by" .claude/agents/pr-review.md

# Conditional spawn triggers documented
grep -c "test.*changes\|test.*file" .claude/agents/pr-review.md
grep -c "error-handling pattern" .claude/agents/pr-review.md

# All 3 sub-agent spawns in body
grep -c "pr-review-toolkit:code-reviewer" .claude/agents/pr-review.md
grep -c "pr-review-toolkit:pr-test-analyzer" .claude/agents/pr-review.md
grep -c "pr-review-toolkit:silent-failure-hunter" .claude/agents/pr-review.md

# Existing sections preserved
grep -c "^## Review Dimensions" .claude/agents/pr-review.md
grep -c "^## Safety Checklist" .claude/agents/pr-review.md
grep -c "Synchronization Boundary Audit" .claude/agents/pr-review.md
grep -c "Test Validity Audit" .claude/agents/pr-review.md
grep -c "^## Post-Mortem" .claude/agents/pr-review.md
```
All grep counts must be >= 1. The 8 steps must each appear exactly once.
</verify>
<acceptance_criteria>
  <behavior>Workflow has 8 steps: Step 0 (Eligibility Check with gh CLI fallback), Step 1 (code-reviewer pre-filter, always spawn, with duplication avoidance note), Step 2 (conditional pr-test-analyzer and silent-failure-hunter with explicit trigger heuristics), Steps 3-8 (renumbered from old Steps 1-6). Global Capabilities references receiving-code-review. REVIEW.md generation notes integration of sub-agent findings. All existing Review Dimensions, Safety Checklist, Synchronization Boundary Audit, Test Validity Audit, and Post-Mortem sections are preserved.</behavior>
  <test-command>grep -n "^### Step [0-8]:" .claude/agents/pr-review.md</test-command>
  <source-assertion>All 8 workflow steps are present and in order: Step 0 (Eligibility), Step 1 (Pre-Filter), Step 2 (Conditional Spawns), Step 3 (Get diff), Step 4 (Per-file review), Step 5 (Generate REVIEW.md), Step 6 (Auto-fix), Step 7 (Re-verify), Step 8 (Loop control).</source-assertion>
</acceptance_criteria>
<done>pr-review body has new Step 0 eligibility check (with gh CLI fallback), Step 1 code-reviewer pre-filter (with duplication avoidance), Step 2 conditional spawns (with explicit trigger heuristics), and Steps 3-8 from renumbered original workflow. Global Capabilities references receiving-code-review. All existing sections preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add Confidence Assessment section after findings in pr-review body</name>
  <files>.claude/agents/pr-review.md</files>
  <read_first>
Read the current body of `.claude/agents/pr-review.md`, specifically the Step 8: Loop control section (old Step 6, lines 152-157) and the Safety Checklist section (lines 158-168). Also read RESEARCH.md Code Example "Confidence Assessment" subsection for the exact factor table and score interpretation to use. The Confidence Assessment must be placed AFTER the Loop control step but BEFORE the Safety Checklist, so it appears after all review work is complete.
  </read_first>
  <action>
Per D-02 (confidence score explicitly assigned as secondary axis) and CPI-03, insert a Confidence Assessment section between Step 8: Loop control and the Safety Checklist.

Insert the following section:

```
## Confidence Assessment (NEW — CPI-03, D-02)

After completing all review steps, assign an explicit confidence score (0-100) reflecting the reviewer's confidence that all relevant issues have been identified. This is a **secondary axis** — it complements, not replaces, the BLOCK/WARN/INFO classification assigned to individual findings.

**How to determine the score (explicit assignment, NOT computed):**

Reviewers assign the score based on holistic assessment of these factors. The score is a judgment call, not a formula output:

| Factor | How it affects confidence |
|--------|--------------------------|
| PR complexity | Simple (single file/function) = higher confidence; Complex (multi-module, concurrency) = lower |
| Diff size | < 200 lines = higher; > 1000 lines = lower (cannot inspect every path in large diffs) |
| Test coverage | Existing tests + pr-test-analyzer findings confirm key paths = higher; missing test coverage for changed code = lower |
| Sub-agent alignment | code-reviewer findings align with manual OS-specific review = higher; contradict = lower |
| Ambiguity | Clear, well-defined semantics = higher; multiple possible interpretations or unclear edge-case behavior = lower |

**Score interpretation:**

| Range | Label | Meaning |
|-------|-------|---------|
| 90-100 | Comprehensive | High confidence all significant issues identified; thorough review of all paths |
| 70-89 | Thorough | Most issues likely identified; some edge cases or large-diff paths may have gaps |
| 50-69 | Moderate gaps | Key paths reviewed, but significant uncertainty remains due to complexity or ambiguity |
| < 50 | Shallow | Review was limited; consider re-review with narrower scope or additional reviewer |

**Confidence Assessment output format:**

After the REVIEW.md findings but before closing the review, add:

```
**Confidence Assessment:** [0-100]

[Brief justification referencing the factors above — 1-3 sentences explaining why this score was assigned.]
```

The confidence score appears in the REVIEW.md output after the BLOCK/WARN/INFO sections and before any closing notes. Per D-02, the score is an explicitly assigned human judgment — do NOT attempt to compute it via formula from sub-agent scores or metrics.
```

This section is additive per D-07 — it does not replace or modify any existing section. Place it exactly between `### Step 8: Loop control` section content and the `## Safety Checklist` header.
  </action>
  <verify>
Verify the Confidence Assessment section:
```bash
# Section header exists
grep -c "^## Confidence Assessment" .claude/agents/pr-review.md

# D-02: explicit assignment (not computed)
grep -c "explicit.*assign\|not.*computed\|not.*formula" .claude/agents/pr-review.md

# D-02: secondary axis
grep -c "secondary axis" .claude/agents/pr-review.md

# Factor table present
grep -c "PR complexity" .claude/agents/pr-review.md
grep -c "Diff size" .claude/agents/pr-review.md
grep -c "Test coverage" .claude/agents/pr-review.md
grep -c "Sub-agent alignment" .claude/agents/pr-review.md
grep -c "Ambiguity" .claude/agents/pr-review.md

# Score interpretation ranges
grep -c "90-100" .claude/agents/pr-review.md
grep -c "70-89" .claude/agents/pr-review.md
grep -c "50-69" .claude/agents/pr-review.md
grep -c "< 50" .claude/agents/pr-review.md

# Output format template
grep -c "Confidence Assessment:" .claude/agents/pr-review.md

# Positioned correctly (before Safety Checklist)
test $(grep -n "^## Confidence Assessment" .claude/agents/pr-review.md | cut -d: -f1) -lt $(grep -n "^## Safety Checklist" .claude/agents/pr-review.md | cut -d: -f1) && echo "OK: before Safety Checklist"

# Existing Safety Checklist preserved
grep -c "^## Safety Checklist" .claude/agents/pr-review.md
```
All grep counts must be >= 1. Confidence Assessment must appear before Safety Checklist.
</verify>
<acceptance_criteria>
  <behavior>Confidence Assessment section is placed after Step 8: Loop control and before Safety Checklist. Section documents: explicit assignment (not computed formula) per D-02, secondary axis role alongside BLOCK/WARN/INFO per D-02, 5 assessment factors (PR complexity, diff size, test coverage, sub-agent alignment, ambiguity), 4 score interpretation ranges with labels, and output format template. Score is explicitly assigned by reviewer judgment — no mathematical computation.</behavior>
  <test-command>sed -n '/^## Confidence Assessment/,/^## Safety Checklist/p' .claude/agents/pr-review.md</test-command>
  <source-assertion>The Confidence Assessment section contains: explicit assignment language (D-02), secondary axis description (D-02), 5-factor assessment table, 4-range score interpretation, and output format template with numeric score + justification.</source-assertion>
</acceptance_criteria>
<done>pr-review body has Confidence Assessment section after Step 8, before Safety Checklist. Documents explicit score assignment (D-02), secondary axis, 5 factors, 4 interpretation ranges, and output format. All existing sections preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pr-review -> code-reviewer sub-agent | Spawn instructions passed as text; sub-agent reviews PR diff for general code quality |
| pr-review -> pr-test-analyzer sub-agent | Conditional spawn based on PR diff contents and labels |
| pr-review -> silent-failure-hunter sub-agent | Conditional spawn based on error-handling pattern detection in PR diff |
| pr-review -> gh CLI (eligibility check) | PR metadata fetched from GitHub API; untrusted PR descriptions could contain injection text |
| pr-review frontmatter -> plugin resolution | Skill names must resolve against installed plugins at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Spoofing | pr-review frontmatter skills list | mitigate | superpowers:receiving-code-review is from official marketplace, installed via `claude plugins install` |
| T-03-16 | Spoofing | All 3 pr-review-toolkit sub-agent spawns | mitigate | Full `pr-review-toolkit:` prefix used for all spawns per Pitfall 1; no bare names that could collide with standalone plugins |
| T-03-17 | Tampering | Eligibility check (gh CLI) | mitigate | gh CLI output is read-only; PR metadata JSON parsing with try/except; on failure, assume eligible and warn per Pitfall 4 |
| T-03-18 | Information Disclosure | Sub-agent spawn instructions | accept | Spawn instructions reference PR diff and file paths — same data already visible to pr-review agent. No additional data exposure |
| T-03-19 | Denial of Service | All sub-agents unavailable | mitigate | Each sub-agent has documented manual fallback review path; pr-review continues functionality with warnings. Eligibility check runs before any spawns — wasted sub-agent spawns are avoided for ineligible PRs |
| T-03-20 | Elevation of Privilege | Conditional spawn triggers | accept | Spawn triggers based on static pattern matching in PR diff (test files, error-handling patterns). No privilege escalation path — sub-agents run with same permissions as parent agent |
</threat_model>

<verification>
After all three tasks complete, verify the entire plan:
```bash
# Frontmatter skill count (6 total)
sed -n '/^skills:/,/^tools:/p' .claude/agents/pr-review.md | grep -c "^- "

# Preamble Agents count (3 sub-agents)
sed -n '/^\*\*Agents\*\*/,/^$/p' .claude/agents/pr-review.md | grep -c "pr-review-toolkit"

# All 8 workflow steps present and ordered
grep -n "^### Step [0-8]:" .claude/agents/pr-review.md

# Confidence Assessment section present
grep -c "^## Confidence Assessment" .claude/agents/pr-review.md

# All 6 requirements referenced by text
grep -c "CPI-01\|code-reviewer.*pre-filter" .claude/agents/pr-review.md
grep -c "CPI-02\|Eligibility Check" .claude/agents/pr-review.md
grep -c "CPI-03\|Confidence Assessment" .claude/agents/pr-review.md
grep -c "CPI-04\|pr-test-analyzer" .claude/agents/pr-review.md
grep -c "CPI-05\|silent-failure-hunter" .claude/agents/pr-review.md
grep -c "CPI-16\|receiving-code-review" .claude/agents/pr-review.md

# D-01: conditional spawn language
grep -c "always spawn\|always spawned" .claude/agents/pr-review.md

# D-02: explicit assignment (not formula)
grep -c "not.*computed\|not.*formula\|explicitly assigned" .claude/agents/pr-review.md

# D-03: eligibility before spawns
test $(grep -n "^### Step 0" .claude/agents/pr-review.md | cut -d: -f1) -lt $(grep -n "^### Step 1" .claude/agents/pr-review.md | cut -d: -f1) && echo "OK: eligibility before code-reviewer"

# Existing sections preserved
grep -c "^## Review Dimensions" .claude/agents/pr-review.md
grep -c "^## Safety Checklist" .claude/agents/pr-review.md
grep -c "Synchronization Boundary Audit" .claude/agents/pr-review.md
grep -c "Test Validity Audit" .claude/agents/pr-review.md
grep -c "^## Post-Mortem" .claude/agents/pr-review.md
```
</verification>

<success_criteria>
1. pr-review.md frontmatter has 6 skills (5 existing + 1 new) with correct YAML indentation
2. Preamble Skills lists receiving-code-review, systematic-debugging, verification-before-completion
3. Preamble Agents lists 3 pr-review-toolkit sub-agents with fallback documentation
4. Step 0 eligibility check gates sub-agent spawns with gh CLI fallback (D-03, Pitfall 4)
5. Step 1 code-reviewer pre-filter always runs with duplication avoidance (D-01, Pitfall 3)
6. Step 2 conditional spawns have explicit trigger heuristics (CPI-04/05, Pitfall 5)
7. Steps 3-8 are correctly renumbered from original 1-6
8. Confidence Assessment section uses explicit score assignment (D-02)
9. All existing sections (Review Dimensions, Safety Checklist, Synchronization Boundary Audit, Test Validity Audit, Post-Mortem) preserved
10. Full `pr-review-toolkit:` prefix used for all sub-agent spawns (Pitfall 1)
11. All 6 CPI requirements (CPI-01 through CPI-05, CPI-16) and all 7 context decisions (D-01 through D-07) are implemented
12. Frontmatter YAML parses correctly (no syntax errors)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-plugin-agent-integration/03-05-SUMMARY.md`
</output>
